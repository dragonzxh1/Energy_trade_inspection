---
phase: 04-scoring-engine-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/server/repository.ts
  - src/lib/server/scoring.ts
  - src/lib/server/gleif.ts
  - src/lib/server/sync/acra.ts
  - src/lib/server/sync/companies-house.ts
  - src/lib/server/sync/opencorporates.ts
  - src/lib/server/sync/zefix.ts
  - src/lib/constants.ts
  - src/lib/types.ts
  - src/components/entity/ScoreGauge.tsx
autonomous: true
requirements:
  - SCORE-01

must_haves:
  truths:
    - "An entity with 10+ trade events receives a tradingTrackRecord score of 22 (max achievable)"
    - "An entity with 3–9 trade events receives a tradingTrackRecord score bonus of +5 (volume tier)"
    - "An entity with 0 trade events receives tradingTrackRecord score of 0 — no phantom points"
    - "No file in src/ contains the string 'phase2Pending: true' — the cleanup is complete"
    - "The ScoreGauge renders the Trading Track Record bar with a real score fraction, not '—'"
    - "The npm run type-check command exits 0 after all changes"
  artifacts:
    - path: "src/lib/server/repository.ts"
      provides: "Extended computeTradingTrackRecord() with volume tier logic"
      contains: "total >= 10"
    - path: "src/lib/constants.ts"
      provides: "SCORE_DIMENSIONS.tradingTrackRecord.phase2Pending removed/set false"
      contains: "tradingTrackRecord"
    - path: "src/lib/types.ts"
      provides: "ScoreDimension.phase2Pending field removed"
  key_links:
    - from: "src/lib/server/repository.ts computeTradingTrackRecord()"
      to: "trade_events table"
      via: "SQL COUNT query"
      pattern: "total >= 10"
    - from: "src/lib/server/repository.ts getEntityById()"
      to: "scoreBreakdown.tradingTrackRecord"
      via: "assignment at line ~737"
      pattern: "entity.scoreBreakdown.tradingTrackRecord"
---

<objective>
Activate the Trading Track Record scoring dimension (SCORE-01) by extending the existing
`computeTradingTrackRecord()` function with volume-tier bonuses, then sweep out every
`phase2Pending: true` reference across the codebase.

Purpose: Brings the total achievable Authenticity Score from 75 to up to 97 (capped at 100).
The Trading Track Record dimension has been a zero-valued placeholder since Phase 1.

Output: Extended `computeTradingTrackRecord()` awarding up to 22 pts, plus a complete
`phase2Pending` cleanup across 16 sites in 10 files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-scoring-engine-completion/04-CONTEXT.md
@.planning/phases/04-scoring-engine-completion/04-RESEARCH.md
</context>

<interfaces>
<!-- Key contracts the executor needs. Extracted from verified file reads. -->

From src/lib/server/repository.ts lines 214–262 (current computeTradingTrackRecord):
```typescript
export async function computeTradingTrackRecord(entityId: string): Promise<{
  score: number
  evidence: string[]
  phase2Pending: false
}> {
  // SQL query returns total_events, recent_events, unique_counterparties
  // Current scoring: +5 any, +5 repeat (total > unique), +5 recent
  // Returns { score, evidence, phase2Pending: false }
}
```

From src/lib/server/repository.ts lines 735–743 (call site):
```typescript
const trackRecord = await computeTradingTrackRecord(entity.id)
entity.scoreBreakdown.tradingTrackRecord = {
  score:         trackRecord.score,
  maxScore:      25,
  phase2Pending: false,   // ← remove this field
  evidence:      trackRecord.evidence,
}
entity.authenticityScore = Math.min(100, entity.authenticityScore + trackRecord.score)
```

From src/lib/constants.ts line 17–22 (SCORE_DIMENSIONS):
```typescript
tradingTrackRecord: {
  label: 'Trading Track Record',
  labelZh: '交易记录',
  max: 25,
  phase2Pending: true,  // ← change to false
},
```

From src/lib/types.ts line 17–22 (ScoreDimension):
```typescript
export interface ScoreDimension {
  score: number
  maxScore: number
  phase2Pending?: boolean  // ← remove this field entirely
  evidence?: string[]
}
```

All 16 phase2Pending sites (from 04-RESEARCH.md verified grep):
- src/lib/server/scoring.ts           line 199  — LISTED_BREAKDOWN constant
- src/lib/server/scoring.ts           line 226  — computeScore() return
- src/lib/server/repository.ts        line 109  — normalizeBreakdown() fallback
- src/lib/server/repository.ts        line 212  — computeTradingTrackRecord() JSDoc
- src/lib/server/repository.ts        line 217  — return type annotation
- src/lib/server/repository.ts        line 258  — return value (in try block)
- src/lib/server/repository.ts        line 260  — return value (in catch block)
- src/lib/server/repository.ts        line 740  — inline assignment after call
- src/lib/server/gleif.ts             line 198  — buildGleifCompany()
- src/lib/server/sync/acra.ts         line 140  — computeACRAScore()
- src/lib/server/sync/companies-house.ts line 139 — buildCHCompany()
- src/lib/server/sync/opencorporates.ts  line 187 — buildOCCompany()
- src/lib/server/sync/zefix.ts        line 124  — buildZefixCompany()
- src/lib/constants.ts                line 21   — SCORE_DIMENSIONS
- src/components/entity/ScoreGauge.tsx line 157  — isPending render logic
- src/components/entity/ScoreGauge.tsx line 164  — title= tooltip text
- src/components/entity/ScoreGauge.tsx line 247  — "Phase 1 data only" paragraph
- src/lib/types.ts                    line 20   — ScoreDimension interface
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend computeTradingTrackRecord() with volume-tier bonuses</name>
  <files>src/lib/server/repository.ts</files>
  <read_first>
    - src/lib/server/repository.ts (lines 210–265 — the full computeTradingTrackRecord function)
  </read_first>
  <action>
In `src/lib/server/repository.ts`, modify `computeTradingTrackRecord()` as follows:

1. **Add volume-tier logic** after the three existing `if` blocks (any trade events, repeat counterparty, recent activity), before the `return` statement. Use mutually exclusive `if/else if` — never stack:

```typescript
// Volume tier — award the higher tier only, never stack
if (total >= 10) {
  score += 7
  evidence.push('High-volume: 10+ verified trade events on record')
} else if (total >= 3) {
  score += 5
  evidence.push('Established volume: 3–9 verified trade events on record')
}
```

2. **Update the return type annotation** on the function from:
```typescript
export async function computeTradingTrackRecord(entityId: string): Promise<{
  score: number
  evidence: string[]
  phase2Pending: false
}>
```
To:
```typescript
export async function computeTradingTrackRecord(entityId: string): Promise<{
  score: number
  evidence: string[]
}>
```

3. **Remove `phase2Pending: false` from both return statements** inside the function:
   - Line ~258: `return { score, evidence, phase2Pending: false }` → `return { score, evidence }`
   - Line ~260 (catch): `return { score: 0, evidence: ['Trading history analysis unavailable'], phase2Pending: false }` → `return { score: 0, evidence: ['Trading history analysis unavailable'] }`

4. **Remove `phase2Pending: false` from the JSDoc comment** at line ~212 if it mentions it in the comment text.

5. **Update the call site** at line ~737–742:
   - Remove `phase2Pending: false,` from the `entity.scoreBreakdown.tradingTrackRecord` assignment object.

6. **Remove `phase2Pending: true` from `normalizeBreakdown()`** at line ~109. The fallback object there currently has `tradingTrackRecord: { score: 0, maxScore: 25, phase2Pending: true }`. Remove the `phase2Pending: true` field from that object.

The final scoring formula after this change:
- +5 if any trade events (total > 0)
- +5 if repeat counterparty (total > unique)
- +5 if recent activity (recent > 0)
- +7 if total >= 10 (high-volume tier)
- +5 if total >= 3 and total < 10 (established volume tier)
Maximum achievable: 22 pts. Do NOT add any logic to reach 25 — leave it as 22 max.
  </action>
  <verify>
    <automated>cd /d/Github/Energy_trade_inspection && npm run type-check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/server/repository.ts contains `total >= 10` (volume tier high branch)
    - src/lib/server/repository.ts contains `total >= 3` (volume tier low branch)
    - src/lib/server/repository.ts does NOT contain `phase2Pending: false` in the computeTradingTrackRecord function body or return type
    - src/lib/server/repository.ts does NOT contain `phase2Pending: true` in normalizeBreakdown
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>computeTradingTrackRecord() returns up to 22 pts with volume-tier logic. Function signature has no phase2Pending field. normalizeBreakdown() fallback has no phase2Pending field.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove all phase2Pending references from 8 remaining files</name>
  <files>
    src/lib/server/scoring.ts,
    src/lib/server/gleif.ts,
    src/lib/server/sync/acra.ts,
    src/lib/server/sync/companies-house.ts,
    src/lib/server/sync/opencorporates.ts,
    src/lib/server/sync/zefix.ts,
    src/lib/constants.ts,
    src/lib/types.ts,
    src/components/entity/ScoreGauge.tsx
  </files>
  <read_first>
    - src/lib/server/scoring.ts (full file — LISTED_BREAKDOWN at line 196, computeScore() return at line 222)
    - src/lib/constants.ts (full file — SCORE_DIMENSIONS at line 4)
    - src/lib/types.ts (full file — ScoreDimension at line 17)
    - src/components/entity/ScoreGauge.tsx (full file — isPending at line 157, tooltip at line 164, footer at line 247)
    - src/lib/server/gleif.ts (lines 190–210)
    - src/lib/server/sync/acra.ts (lines 132–150)
    - src/lib/server/sync/companies-house.ts (lines 131–148)
    - src/lib/server/sync/opencorporates.ts (lines 179–196)
    - src/lib/server/sync/zefix.ts (lines 116–133)
  </read_first>
  <action>
Make the following changes in each file. Read the current file content first to confirm line numbers before editing.

**src/lib/server/scoring.ts:**
- In `LISTED_BREAKDOWN` constant (~line 199): remove `phase2Pending: true` from the `tradingTrackRecord` entry. Result: `tradingTrackRecord: { score: 0, maxScore: 25 }`
- In `computeScore()` return (~line 226): remove `phase2Pending: true` from the `tradingTrackRecord` entry. Result: `tradingTrackRecord: { score: 0, maxScore: 25 }`

**src/lib/constants.ts:**
- Change `tradingTrackRecord.phase2Pending: true` to `phase2Pending: false` at line ~21.
- Also remove or rename `PHASE1_MAX_SCORE = 75` — delete this constant entirely (grep confirms it is unused in rendering). If deleting would cause a TypeScript error, rename to a comment `// PHASE1_MAX_SCORE: 75 (deprecated — trading track record now live)`.

**src/lib/types.ts:**
- In `ScoreDimension` interface (~line 17): remove the line `phase2Pending?: boolean // Trading Track Record is Phase 2`.
- Update the comment on `ScoreBreakdown.tradingTrackRecord` from `// max 25 — Phase 2 pending` to `// max 25`.

**src/components/entity/ScoreGauge.tsx:**
Three changes:
1. Line ~157: `const isPending = entry.phase2Pending` — change to `const isPending = false`. (The `isPending` variable is used in 4 places in the dimension map render — keep the variable but hardcode it to false so all bars render normally. This is cleaner than removing all usages in one pass.)
2. Line ~164: `title={isPending ? 'Phase 2 data — coming soon' : undefined}` — remove the `title` prop entirely from the outer dimension `<div>`.
3. Lines ~239–248: Remove the entire `<p>` paragraph at the bottom of the component that reads `"Phase 1 data only (max 75). Trading Track Record unlocks in Phase 2."` Do NOT replace it with anything in this task — the upgrade CTA is added in Plan 03.

**src/lib/server/gleif.ts (~line 198):**
Find the `tradingTrackRecord` object in `buildGleifCompany()`. Remove the `phase2Pending: true` field. Result: `tradingTrackRecord: { score: 0, maxScore: 25 }`.

**src/lib/server/sync/acra.ts (~line 140):**
Same pattern — remove `phase2Pending: true` from the `tradingTrackRecord` object in `computeACRAScore()`.

**src/lib/server/sync/companies-house.ts (~line 139):**
Same pattern — remove `phase2Pending: true` from the `tradingTrackRecord` object in `buildCHCompany()`.

**src/lib/server/sync/opencorporates.ts (~line 187):**
Same pattern — remove `phase2Pending: true` from the `tradingTrackRecord` object in `buildOCCompany()`.

**src/lib/server/sync/zefix.ts (~line 124):**
Same pattern — remove `phase2Pending: true` from the `tradingTrackRecord` object in `buildZefixCompany()`.

After all changes, run a grep to confirm zero remaining occurrences:
`grep -r "phase2Pending" src/` should return no output.
  </action>
  <verify>
    <automated>cd /d/Github/Energy_trade_inspection && grep -r "phase2Pending" src/ && echo "FAIL: phase2Pending still present" || echo "PASS: no phase2Pending found" && npm run type-check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "phase2Pending" src/` returns no lines (zero matches)
    - src/lib/constants.ts does NOT contain `PHASE1_MAX_SCORE = 75` (deleted) or contains it only as a comment
    - src/lib/types.ts ScoreDimension interface does NOT contain `phase2Pending`
    - src/components/entity/ScoreGauge.tsx does NOT contain `'Phase 1 data only'`
    - src/components/entity/ScoreGauge.tsx does NOT contain `'Phase 2 data'`
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>All 16+ phase2Pending references removed from src/. TypeScript strict check passes. ScoreGauge no longer renders the "Phase 1 data only" footer text.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → repository.ts | trade_events rows are trusted (internal data); SQL injection not possible via parameterized queries |
| scoring.ts → computeScore() | ScoringInputs constructed internally; no external user-supplied fields in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Tampering | computeTradingTrackRecord() arithmetic | mitigate | Volume tier uses exclusive `if/else if` — verify in acceptance criteria that `total >= 10` and `total >= 3` branches are mutually exclusive; no stacking possible |
| T-4-02 | Information Disclosure | ScoreGauge cleanup (phase2Pending removal) | accept | Removing the "Phase 1 data only" text is cosmetic; no paid data is exposed in this plan — breakdown still renders for all users (paywall added in Plan 03) |
| T-4-03 | Spoofing | normalizeBreakdown() fallback | accept | The fallback returns proportional scores based on stored authenticity_score — removal of phase2Pending field does not change the score values, only removes the UI indicator |
| T-4-04 | Denial of Service | computeTradingTrackRecord() SQL query | accept | Single parameterized query against indexed entity_id; no unbounded table scan; catch block returns safe fallback |
</threat_model>

<verification>
After both tasks complete:

1. `grep -r "phase2Pending" src/` — must return no output
2. `npm run type-check` — must exit 0 (no TypeScript errors)
3. `npm run build` — must complete without errors
4. Manual: Load a company page with known trade events (e.g., any seeded company). Verify the Trading Track Record bar shows a real score fraction (e.g., `5/25`) instead of `—`.
5. Manual: Load a company page with no trade events. Verify it shows `0/25` with no phantom points.
</verification>

<success_criteria>
1. `computeTradingTrackRecord()` awards up to 22 pts: +5 (any events) + 5 (repeat) + 5 (recent) + 7 (10+ events) or +5 (3–9 events)
2. Zero `phase2Pending` strings remain in `src/` (grep confirms)
3. `npm run type-check` exits 0
4. `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/04-scoring-engine-completion/04-01-SUMMARY.md` with:
- What was changed (function signature, volume tier logic added, all cleanup sites)
- Exact line numbers of key changes made
- Confirmation that grep returns zero phase2Pending hits
- Build/type-check status
</output>
