---
context: default
phase: abundant-seeking-tiger
task: complete
total_tasks: 6
status: paused
last_updated: 2026-04-19T09:36:13Z
---

# 评分体系重构 — 制裁风险与真实性评分分离

_无阻塞约束。所有工作已完成，待 commit。_

<current_state>
重构完整完成。已通过 3 轮代码审查 + type-check。所有改动未提交。

**核心变更：**
- `authenticityScore`：纯粹衡量实体可验证程度，不受制裁状态影响
- `riskLevel`：综合函数（listed→critical，unknown→cap at medium，not_listed→纯分数阈值）
- 统一了 8 个调用方，修复了 4 处扣分后未重算 riskLevel 的 bug（WR-05）
</current_state>

<completed_work>
- Task 1: scoring.ts — 删除 LISTED_BREAKDOWN 硬地板、unknown×0.7 乘数；riskLevel() 重写并导出
- Task 2: gleif.ts — 引入 riskLevel import，删除 listed 硬地板块
- Task 3: zefix.ts、opencorporates.ts — 删除硬地板，build/search result 均调用 riskLevel()
- Task 4: repository.ts — ACRA/GLEIF 路径 + 4处扣分后补算 riskLevel（WR-05 真实 bug）
- Task 5: companies-house.ts、acra.ts — 补漏调用方
- Task 6: CLAUDE.md — 更新已废弃的 unknown×0.7 文档
</completed_work>

<remaining_work>
无未完成任务。

待做：
- git commit 所有改动
- 触发 rescore（重启服务 或 `curl -X POST /api/admin/sync`）让制裁实体的真实分数写入 DB
</remaining_work>

<decisions_made>
- authenticity score 与 sanction risk 完全解耦 — 制裁实体可能是真实存在的，两者是正交维度
- riskLevel() 综合：listed→critical，unknown→cap at medium，not_listed→纯分数阈值 (85/60/35)
- intelligence sanctions_hits 扣分保留在 communityReputation — 情报命中与正式列名是不同概念
- ScoreGauge 颜色用 getRiskLevel(score) 纯真实性着色；RiskBadge 用 entity.riskLevel 综合风险 — UI 已天然分离，无需改动
</decisions_made>

<blockers>
无阻塞项。
</blockers>

## Required Reading (in order)
1. `src/lib/server/scoring.ts` — 新的 riskLevel() 综合函数，是整个重构的核心
2. `src/lib/server/repository.ts` — WR-05 修复位置（4处 deduction 块）

## Infrastructure State
- 开发服务器：未运行
- 数据库：Docker PostgreSQL（docker-compose up -d）
- 未提交改动：8 个文件

## Files Modified (all uncommitted)
- `CLAUDE.md`
- `src/lib/server/scoring.ts`
- `src/lib/server/gleif.ts`
- `src/lib/server/repository.ts`
- `src/lib/server/sync/acra.ts`
- `src/lib/server/sync/companies-house.ts`
- `src/lib/server/sync/opencorporates.ts`
- `src/lib/server/sync/zefix.ts`

<context>
计划文件：~/.claude/plans/peppy-moseying-cake.md
任务名：abundant-seeking-tiger
全部工作在单次会话内完成，type-check 通过，3轮代码审查，无残留硬编码 riskLevel 字面量。
</context>

<next_action>
Start with: git commit 所有改动，commit message 示例：
  feat(scoring): separate sanction risk from authenticity score

  Remove LISTED_BREAKDOWN hard-floor and unknown×0.7 multiplier.
  riskLevel() is now a composite function: listed→critical,
  unknown→capped at medium, not_listed→pure score thresholds.
  All 8 callers updated; 4 post-deduction riskLevel recomputes added.
</next_action>
