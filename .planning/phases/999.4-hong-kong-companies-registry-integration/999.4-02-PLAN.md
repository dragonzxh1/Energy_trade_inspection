---
phase: 999.4
plan: 02
type: execute
wave: 2
depends_on: [999.4-01]
files_modified: [src/lib/server/sync/hkcr.ts]
autonomous: true
requirements: [D-01, D-02, D-04]
user_setup: []

must_haves:
  truths:
    - "syncHKCRFull() 可从 data.gov.hk CKAN API 下载 CSV 文件并解析"
    - "searchHKCRCache() 可从 hkcr_cache 表查询香港公司"
    - "mightBeHKNumber() 可识别 7 位纯数字注册号"
    - "hkcrToSearchResult() 可转换缓存结果为 SearchResult 格式"
  artifacts:
    - path: "src/lib/server/sync/hkcr.ts"
      provides: "HKCR 批量同步 + 本地缓存查询"
      exports: ["syncHKCRFull", "searchHKCRCache", "mightBeHKNumber", "hkcrToSearchResult"]
      min_lines: 150
  key_links:
    - from: "src/lib/server/sync/hkcr.ts"
      to: "data.gov.hk CKAN API"
      via: "fetch package_show"
      pattern: "package_show.*hk-cr"
    - from: "src/lib/server/sync/hkcr.ts"
      to: "hkcr_cache 表"
      via: "db.query INSERT/SELECT"
      pattern: "INSERT INTO hkcr_cache|SELECT.*FROM hkcr_cache"
---

<objective>
创建 HKCR 同步模块，实现 CSV 批量下载 + 本地缓存导入模式（类比 gleif-golden-copy.ts），为 Tier 2 搜索提供香港公司数据。

Purpose: data.gov.hk 无搜索 API，需批量下载 CSV 并导入本地缓存
Output: hkcr.ts 模块（sync + search + helper 函数）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@.planning/phases/999.4-hong-kong-companies-registry-integration/999.4-01-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.4-hong-kong-companies-registry-integration/999.4-CONTEXT.md
@.planning/phases/999.4-hong-kong-companies-registry-integration/999.4-RESEARCH.md
@.planning/phases/999.4-hong-kong-companies-registry-integration/999.4-PATTERNS.md
</context>

<interfaces>
<!-- gleif-golden-copy.ts 批量导入模式（主要参照） -->
```typescript
// Source: src/lib/server/sync/gleif-golden-copy.ts
import { Readable } from 'node:stream'
import { db } from '@/lib/server/db'

const BATCH_SIZE = 1000

async function streamGleifRecords(
  apiUrl: string,
  arrayKey: string,
  onRecord: (record: unknown) => Promise<void>,
): Promise<number> { ... }

async function writeSyncLog(
  source: string,
  status: 'success' | 'error',
  count: number,
  durationMs: number,
  errorMsg?: string,
): Promise<void> { ... }
```

<!-- companies-house.ts 注册号检测模式 -->
```typescript
// Source: src/lib/server/sync/companies-house.ts line 55-59
const UK_NUMBER_REGEX = /^(SC|NI|OC|SO|NC|R|IP|SP|IC|SI|NP|NV|RC|SR|NR|NO)?\d{6,8}$/i

export function mightBeUKNumber(s: string): boolean {
  return UK_NUMBER_REGEX.test(s.trim().toUpperCase())
}

export function chToSearchResult(company: CHCompany) {
  const { authenticityScore } = computeCHScore(company)
  return {
    id: `ch:${company.company_number}`,
    name: company.title ?? company.company_name ?? '',
    type: 'company' as const,
    country: 'United Kingdom',
    jurisdictionFlag: '🇬🇧',
    sanctionStatus: 'unknown' as const,
    authenticityScore,
    riskLevel: riskLevel(authenticityScore, 'unknown'),
    registrationNumber: company.company_number,
    slug: company.company_number.toLowerCase(),
  }
}
```

<!-- D-01: CSV 批量下载策略 -->
```typescript
// CKAN package_show API
const CKAN_PACKAGE_URL = 'https://data.gov.hk/en-data/api/3/action/package_show?id=hk-cr-crdata-list-newly-registered-companies-2526'

// 返回 268 个 CSV/XLSX 资源 URL
// CSV 字段：Company Number, Company Name, Company Name (Chinese), Company Type, Company Status, Date of Incorporation
```

<!-- D-04: 注册号格式检测 -->
```typescript
// 香港公司注册号：7位纯数字（无前缀字母）
const HK_NUMBER_REGEX = /^\d{7}$/

export function mightBeHKNumber(s: string): boolean {
  return HK_NUMBER_REGEX.test(s.trim())
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建 hkcr.ts 文件结构 + 类型定义</name>
  <files>src/lib/server/sync/hkcr.ts</files>
  <read_first>
    src/lib/server/sync/gleif-golden-copy.ts
    src/lib/server/sync/companies-house.ts
    .planning/phases/999.4-hong-kong-companies-registry-integration/999.4-CONTEXT.md
    .planning/phases/999.4-hong-kong-companies-registry-integration/999.4-PATTERNS.md
  </read_first>
  <action>
创建 hkcr.ts 文件，定义类型和常量（per D-01, D-02, D-04）。

**文件头部：**
```typescript
/**
 * Hong Kong Companies Registry sync module.
 * 
 * Strategy: CSV batch download + local cache import (gleif-golden-copy.ts pattern)
 * Reason: data.gov.hk CKAN API does NOT provide searchable company database.
 * 
 * syncHKCRFull()    — batch download CSV files + import to hkcr_cache
 * searchHKCRCache() — query local cache table (Tier 2 integration)
 * mightBeHKNumber() — detect 7-digit HK registration number
 * hkcrToSearchResult() — convert cache row to SearchResult format
 */

import csvParse from 'csv-parse'
import { db } from '@/lib/server/db'
import { riskLevel } from '@/lib/server/scoring'

// ── Types ──────────────────────────────────────────────────────────────────────

/** HK company cache row (per D-02) */
export interface HKCRCacheRow {
  company_number: string
  company_name: string
  company_name_chinese?: string
  company_type?: string
  company_status?: string
  date_of_incorporation?: Date
  nature_of_business?: string
}

/** CSV row format from data.gov.hk */
interface HKCRCSVRow {
  'Company Number'?: string
  'Company Name'?: string
  'Company Name (Chinese)'?: string
  'Company Type'?: string
  'Company Status'?: string
  'Date of Incorporation'?: string
  // CSV column names may vary — normalize in parse step
}

// ── Constants ──────────────────────────────────────────────────────────────────

const CKAN_PACKAGE_URL = 'https://data.gov.hk/en-data/api/3/action/package_show?id=hk-cr-crdata-list-newly-registered-companies-2526'
const HK_NUMBER_REGEX = /^\d{7}$/
const BATCH_SIZE = 1000
```
  </action>
  <verify>
    <automated>grep -E "export interface HKCRCacheRow|const CKAN_PACKAGE_URL|const HK_NUMBER_REGEX" src/lib/server/sync/hkcr.ts | wc -l | grep -q "3"</automated>
  </verify>
  <done>
hkcr.ts 文件已创建，包含类型定义和常量
  </done>
</task>

<task type="auto">
  <name>Task 2: 实现 mightBeHKNumber() + hkcrToSearchResult()</name>
  <files>src/lib/server/sync/hkcr.ts</files>
  <read_first>
    src/lib/server/sync/companies-house.ts
    src/lib/server/scoring.ts
  </read_first>
  <action>
实现 helper 函数（per D-04，类比 companies-house.ts）：

```typescript
// ── Registration number detection (D-04) ───────────────────────────────────────

/** Detect 7-digit HK company registration number (no prefix letters). */
export function mightBeHKNumber(s: string): boolean {
  return HK_NUMBER_REGEX.test(s.trim())
}

// ── Score computation (类比 computeCHScore) ────────────────────────────────────

/**
 * Compute authenticity score for HK company from cache.
 * entityExistence: Live status in HK official registry (max 20/25)
 * documentConsistency: date_of_incorporation + company_type (max 8/10)
 */
export function computeHKCRScore(company: HKCRCacheRow): number {
  const isLive = company.company_status === 'Live'
  const entityExistence = isLive ? 20 : 0
  const documentConsistency = 
    (company.date_of_incorporation ? 4 : 0) +
    (company.company_type ? 4 : 0)
  return entityExistence + documentConsistency
}

// ── Result conversion (类比 chToSearchResult) ───────────────────────────────────

/** Convert HK cache row to SearchResult format. */
export function hkcrToSearchResult(company: HKCRCacheRow) {
  const authenticityScore = computeHKCRScore(company)
  return {
    id: `hkcr:${company.company_number}`,
    name: company.company_name,
    type: 'company' as const,
    country: 'Hong Kong',
    jurisdictionFlag: '🇭🇰',
    sanctionStatus: 'unknown' as const,
    authenticityScore,
    riskLevel: riskLevel(authenticityScore, 'unknown'),
    registrationNumber: company.company_number,
    slug: `hk-${company.company_number}`,
  }
}
```
  </action>
  <verify>
    <automated>grep -E "export function mightBeHKNumber|export function hkcrToSearchResult|export function computeHKCRScore" src/lib/server/sync/hkcr.ts | wc -l | grep -q "3"</automated>
  </verify>
  <done>
helper 函数已实现：mightBeHKNumber() + computeHKCRScore() + hkcrToSearchResult()
  </done>
</task>

<task type="auto">
  <name>Task 3: 实现 searchHKCRCache() 本地缓存查询</name>
  <files>src/lib/server/sync/hkcr.ts</files>
  <read_first>
    src/lib/server/sync/gleif-golden-copy.ts
    src/lib/server/repository.ts
    .planning/phases/999.4-hong-kong-companies-registry-integration/999.4-PATTERNS.md
  </read_first>
  <action>
实现本地缓存查询函数（per D-01，类比 gleif-golden-copy.ts 的 lei_cache 查询模式）：

```typescript
// ── Local cache query (per D-01) ───────────────────────────────────────────────

/**
 * Search hkcr_cache table for Hong Kong companies.
 * 
 * If query matches HK number pattern (7 digits), do exact lookup.
 * Otherwise, do pg_trgm fuzzy name search on both English and Chinese names.
 */
export async function searchHKCRCache(query: string, limit = 5): Promise<HKCRCacheRow[]> {
  if (!query || query.trim().length < 2) return []

  const normalizedQuery = query.trim()

  // Check if query is a HK number (D-04)
  if (mightBeHKNumber(normalizedQuery)) {
    const { rows } = await db.query<HKCRCacheRow>(
      `SELECT company_number, company_name, company_name_chinese, 
              company_type, company_status, date_of_incorporation, nature_of_business
       FROM hkcr_cache
       WHERE company_number = $1
       LIMIT $2`,
      [normalizedQuery, limit]
    )
    return rows
  }

  // Name search with pg_trgm (GIN index)
  const { rows } = await db.query<HKCRCacheRow>(
    `SELECT company_number, company_name, company_name_chinese,
            company_type, company_status, date_of_incorporation, nature_of_business,
           (company_name <-> $2) AS name_dist
     FROM hkcr_cache
     WHERE company_name ILIKE $1 
        OR company_name_chinese ILIKE $1
     ORDER BY name_dist
     LIMIT $3`,
    [`%${normalizedQuery}%`, normalizedQuery, limit]
  )

  return rows
}
```

**注意事项：**
- 注册号查询使用精确匹配（B-tree 索引）
- 名称查询使用 ILIKE + pg_trgm 距离排序（GIN 索引）
- 支持英文和中文名搜索
  </action>
  <verify>
    <automated>grep -E "export async function searchHKCRCache|SELECT.*FROM hkcr_cache|mightBeHKNumber\(normalizedQuery\)" src/lib/server/sync/hkcr.ts | wc -l | grep -q "3"</automated>
  </verify>
  <done>
searchHKCRCache() 已实现，支持注册号精确查询 + 名称模糊搜索
  </done>
</task>

<task type="auto">
  <name>Task 4: 实现 syncHKCRFull() CSV 批量同步</name>
  <files>src/lib/server/sync/hkcr.ts</files>
  <read_first>
    src/lib/server/sync/gleif-golden-copy.ts
    .planning/phases/999.4-hong-kong-companies-registry-integration/999.4-RESEARCH.md
    .planning/phases/999.4-hong-kong-companies-registry-integration/999.4-PATTERNS.md
  </read_first>
  <action>
实现 CSV 批量下载 + 导入函数（per D-01，类比 gleif-golden-copy.ts）：

```typescript
// ── CSV batch sync (per D-01, gleif-golden-copy.ts pattern) ────────────────────

/**
 * Fetch CSV resource URLs from CKAN package_show API.
 * Returns list of CSV/XLSX file URLs for weekly newly registered companies.
 */
async function fetchCSVResourceUrls(): Promise<string[]> {
  const response = await fetch(CKAN_PACKAGE_URL, {
    headers: { 'User-Agent': 'ETI-Bot/1.0' },
    signal: AbortSignal.timeout(30000),
  })
  
  if (!response.ok) {
    throw new Error(`CKAN package fetch failed: HTTP ${response.status}`)
  }
  
  const data = await response.json()
  if (!data.success) {
    throw new Error('CKAN package fetch failed: success=false')
  }
  
  // Extract CSV/XLSX resource URLs
  const resources = data.result?.resources ?? []
  return resources
    .filter((r: { format?: string }) => 
      r.format?.toUpperCase() === 'CSV' || r.format?.toUpperCase() === 'XLSX'
    )
    .map((r: { url?: string }) => r.url)
    .filter((url: string | undefined): url is string => Boolean(url))
}

/**
 * Download and parse a single CSV file.
 * Returns array of normalized HKCRCacheRow objects.
 */
async function parseCSVFile(url: string): Promise<HKCRCacheRow[]> {
  try {
    const response = await fetch(url, {
      signal: AbortSignal.timeout(60000),
    })
    
    if (!response.ok) {
      console.error(`[hkcr] CSV download failed: ${url}`)
      return []
    }
    
    const text = await response.text()
    const parser = csvParse(text, { columns: true, skip_empty_lines: true })
    
    const records: HKCRCacheRow[] = []
    for await (const record of parser) {
      // Normalize CSV column names (may vary across files)
      const normalized: HKCRCacheRow = {
        company_number: record['Company Number'] ?? record['company_number'] ?? '',
        company_name: record['Company Name'] ?? record['company_name'] ?? '',
        company_name_chinese: record['Company Name (Chinese)'] ?? record['company_name_chinese'] ?? undefined,
        company_type: record['Company Type'] ?? record['company_type'] ?? undefined,
        company_status: record['Company Status'] ?? record['company_status'] ?? undefined,
        date_of_incorporation: record['Date of Incorporation'] 
          ? new Date(record['Date of Incorporation']) 
          : undefined,
        nature_of_business: record['Nature of Business'] ?? undefined,
      }
      
      // Skip rows without required fields
      if (!normalized.company_number || !normalized.company_name) continue
      
      records.push(normalized)
    }
    
    return records
  } catch (err) {
    console.error(`[hkcr] CSV parse error for ${url}:`, err)
    return []
  }
}

/**
 * Batch insert HK company records into hkcr_cache.
 * Uses ON CONFLICT UPSERT to handle duplicates.
 */
async function batchInsertHKCR(records: HKCRCacheRow[]): Promise<number> {
  if (records.length === 0) return 0
  
  const client = await db.connect()
  try {
    await client.query('BEGIN')
    
    let inserted = 0
    const batch: HKCRCacheRow[] = []
    
    const flushBatch = async () => {
      if (batch.length === 0) return
      
      const cols = 7
      const placeholders = batch
        .map((_, i) => {
          const base = i * cols
          return `($${base+1},$${base+2},$${base+3},$${base+4},$${base+5},$${base+6},$${base+7})`
        })
        .join(',')
      
      const values = batch.flatMap((r) => [
        r.company_number,
        r.company_name,
        r.company_name_chinese ?? null,
        r.company_type ?? null,
        r.company_status ?? null,
        r.date_of_incorporation ?? null,
        r.nature_of_business ?? null,
      ])
      
      await client.query(
        `INSERT INTO hkcr_cache 
           (company_number, company_name, company_name_chinese, 
            company_type, company_status, date_of_incorporation, nature_of_business)
         VALUES ${placeholders}
         ON CONFLICT (company_number) DO UPDATE SET
           company_name = EXCLUDED.company_name,
           company_status = EXCLUDED.company_status,
           company_type = EXCLUDED.company_type,
           date_of_incorporation = EXCLUDED.date_of_incorporation,
           last_synced_at = NOW()`,
        values
      )
      
      inserted += batch.length
      batch.length = 0
      
      if (inserted % 10000 === 0) {
        console.log(`[hkcr] inserted ${inserted.toLocaleString()} records...`)
      }
    }
    
    for (const record of records) {
      batch.push(record)
      if (batch.length >= BATCH_SIZE) await flushBatch()
    }
    
    await flushBatch()
    await client.query('COMMIT')
    
    return inserted
  } catch (err) {
    await client.query('ROLLBACK').catch(() => {})
    throw err
  } finally {
    client.release()
  }
}

/**
 * Full sync: download all CSV files from data.gov.hk and import to hkcr_cache.
 * Per D-01: batch download + local cache import pattern.
 */
export async function syncHKCRFull(): Promise<{ inserted: number; errors: number }> {
  const startMs = Date.now()
  let totalInserted = 0
  let totalErrors = 0
  
  console.log('[hkcr] Starting full sync...')
  
  try {
    const csvUrls = await fetchCSVResourceUrls()
    console.log(`[hkcr] Found ${csvUrls.length} CSV resources`)
    
    for (const url of csvUrls) {
      try {
        console.log(`[hkcr] Processing ${url}`)
        const records = await parseCSVFile(url)
        
        if (records.length > 0) {
          const inserted = await batchInsertHKCR(records)
          totalInserted += inserted
          console.log(`[hkcr] Inserted ${inserted} records from ${url}`)
        }
      } catch (err) {
        totalErrors++
        console.error(`[hkcr] Failed to process ${url}:`, err)
      }
    }
    
    // Log sync result
    await writeSyncLog('hkcr:full', 'success', totalInserted, Date.now() - startMs)
    console.log(`[hkcr] Full sync complete: ${totalInserted.toLocaleString()} records in ${Date.now() - startMs}ms`)
    
    return { inserted: totalInserted, errors: totalErrors }
  } catch (err) {
    await writeSyncLog('hkcr:full', 'error', 0, Date.now() - startMs, String(err))
    throw err
  }
}

async function writeSyncLog(
  source: string,
  status: 'success' | 'error',
  count: number,
  durationMs: number,
  errorMsg?: string,
): Promise<void> {
  try {
    await db.query(
      `INSERT INTO sanctions_sync_log (source, status, record_count, duration_ms, error_message)
       VALUES ($1, $2, $3, $4, $5)`,
      [source, status, count, durationMs, errorMsg ?? null]
    )
  } catch (err) {
    console.error('[hkcr] sync log write failed:', err)
  }
}
```

**关键实现要点（per RESEARCH.md）：**
- CKAN package_show API 返回 268 个资源 URL
- 每个资源是每周新增公司名单 CSV 文件
- 使用 `csv-parse` 库解析 CSV（无需额外安装，gleif 已用）
- 批量 INSERT + ON CONFLICT UPSERT（类比 gleif-golden-copy.ts）
- 写入 `sanctions_sync_log` 记录同步状态
- 每文件失败单独记录，不中断整体流程
  </action>
  <verify>
    <automated>grep -E "export async function syncHKCRFull|async function fetchCSVResourceUrls|INSERT INTO hkcr_cache|ON CONFLICT" src/lib/server/sync/hkcr.ts | wc -l | grep -q "4"</automated>
  </verify>
  <done>
syncHKCRFull() 已实现，支持 CKAN CSV 批量下载 + 导入到 hkcr_cache
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hkcr.ts → data.gov.hk CKAN API | 外部 API 调用，需处理超时和错误 |
| hkcr.ts → hkcr_cache 表 | 本地数据库写入，需 UPSERT 避免重复 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.4-02-01 | Denial of Service | CKAN API fetch | mitigate | AbortSignal.timeout(30000) per fetch |
| T-999.4-02-02 | Tampering | CSV input validation | mitigate | Skip rows without company_number/company_name |
| T-999.4-02-03 | Information Disclosure | sync log error | accept | Error message logged to trusted DB table |

## ASVS V5 Input Validation

- mightBeHKNumber(): regex `^\d{7}$` 验证注册号格式
- CSV 解析时过滤空字段：`if (!normalized.company_number || !normalized.company_name) continue`
- pg_trgm 查询使用参数化 SQL：`$1, $2, $3` 防止 SQL injection
</threat_model>

<verification>
**验证步骤：**
1. hkcr.ts 文件已创建，导出 4 个函数
2. mightBeHKNumber() 正确识别 7 位数字
3. searchHKCRCache() 可查询 hkcr_cache 表
4. syncHKCRFull() 包含 CKAN API + CSV 解析 + batch INSERT

**自动化验证：**
```bash
grep "export async function syncHKCRFull" src/lib/server/sync/hkcr.ts
grep "export async function searchHKCRCache" src/lib/server/sync/hkcr.ts
grep "export function mightBeHKNumber" src/lib/server/sync/hkcr.ts
grep "export function hkcrToSearchResult" src/lib/server/sync/hkcr.ts
```
</verification>

<success_criteria>
- hkcr.ts 文件存在，≥ 150 行
- 导出 syncHKCRFull、searchHKCRCache、mightBeHKNumber、hkcrToSearchResult
- CKAN API 获取 CSV URL 列表
- CSV 解析 + batch INSERT + ON CONFLICT UPSERT
- 本地缓存查询支持注册号精确 + 名称模糊
</success_criteria>

<output>
After completion, create `.planning/phases/999.4-hong-kong-companies-registry-integration/999.4-02-SUMMARY.md`
</output>