---
phase: 999.4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [db/migrations/038_hkcr_cache.sql]
autonomous: true
requirements: [D-05]
user_setup: []

must_haves:
  truths:
    - "hkcr_cache 表存在于数据库中"
    - "表包含 company_number, company_name, company_name_chinese 等字段"
    - "pg_trgm 索引支持名称模糊搜索"
  artifacts:
    - path: "db/migrations/038_hkcr_cache.sql"
      provides: "hkcr_cache 缓存表结构"
      contains: "CREATE TABLE hkcr_cache"
      min_lines: 20
  key_links:
    - from: "db/migrations/038_hkcr_cache.sql"
      to: "PostgreSQL database"
      via: "instrumentation.ts applyMigrations()"
      pattern: "CREATE TABLE.*hkcr_cache"
---

<objective>
创建香港公司注册处本地缓存表 hkcr_cache，为后续 CSV 批量导入和 Tier 2 搜索提供数据存储基础。

Purpose: 香港公司数据本地缓存，避免实时 API 调用（data.gov.hk 无搜索 API）
Output: Migration 038 创建 hkcr_cache 表 + pg_trgm 索引
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.4-hong-kong-companies-registry-integration/999.4-CONTEXT.md
@.planning/phases/999.4-hong-kong-companies-registry-integration/999.4-RESEARCH.md
@.planning/phases/999.4-hong-kong-companies-registry-integration/999.4-PATTERNS.md
</context>

<interfaces>
<!-- Migration 参照：037_lei_cache.sql -->
```sql
-- Source: db/migrations/037_lei_cache.sql
CREATE TABLE IF NOT EXISTS lei_cache (
  lei                              CHAR(20)     PRIMARY KEY,
  legal_name                       TEXT         NOT NULL,
  jurisdiction                     CHAR(2),
  country                          CHAR(2),
  ...
  last_synced_at                   TIMESTAMPTZ  NOT NULL DEFAULT NOW(),
  created_at                       TIMESTAMPTZ  NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS lei_cache_legal_name_trgm
  ON lei_cache USING GIN (legal_name gin_trgm_ops);
```

<!-- D-02 字段范围 -->
```typescript
// Source: 999.4-CONTEXT.md D-02
export interface HKCRCompany {
  company_number: string          // 7位注册号 (PRIMARY KEY)
  company_name: string            // 公司英文名
  company_name_chinese?: string   // 公司中文名
  company_type: string            // e.g. 'Private Company Limited by Shares'
  company_status: string          // 'Live' | 'Deregistered' | 'Dissolved'
  date_of_incorporation: string   // ISO 日期
  nature_of_business?: string     // 业务性质
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建 Migration 038_hkcr_cache.sql</name>
  <files>db/migrations/038_hkcr_cache.sql</files>
  <read_first>
    db/migrations/037_lei_cache.sql
    .planning/phases/999.4-hong-kong-companies-registry-integration/999.4-CONTEXT.md
  </read_first>
  <action>
创建 hkcr_cache 表 migration 文件，参照 037_lei_cache.sql 模式（per D-05）。

**表结构（per D-02 字段范围）：**
```sql
-- Migration 038: Hong Kong Companies Registry local cache
-- Purpose: Store HK company data from weekly CSV batch downloads

CREATE TABLE IF NOT EXISTS hkcr_cache (
  id SERIAL PRIMARY KEY,
  company_number TEXT NOT NULL UNIQUE,           -- 7位注册号（唯一键）
  company_name TEXT NOT NULL,                    -- 公司英文名
  company_name_chinese TEXT,                     -- 公司中文名（可选）
  company_type TEXT,                             -- e.g. 'Private Company Limited by Shares'
  company_status TEXT,                           -- 'Live' | 'Deregistered' | 'Dissolved'
  date_of_incorporation DATE,                    -- 注册日期
  nature_of_business TEXT,                       -- 业务性质（可选）
  fetched_at TIMESTAMPTZ DEFAULT NOW(),          -- 数据获取时间
  last_synced_at TIMESTAMPTZ DEFAULT NOW()       -- 最后同步时间
);

-- Trigram 索引：支持英文名模糊搜索
CREATE INDEX IF NOT EXISTS idx_hkcr_cache_name_trgm
  ON hkcr_cache USING GIN (company_name gin_trgm_ops);

-- Trigram 索引：支持中文名模糊搜索
CREATE INDEX IF NOT EXISTS idx_hkcr_cache_name_chinese_trgm
  ON hkcr_cache USING GIN (company_name_chinese gin_trgm_ops);

-- B-tree 索引：支持注册号精确查询
CREATE INDEX IF NOT EXISTS idx_hkcr_cache_number
  ON hkcr_cache (company_number);

-- 状态索引：支持按公司状态筛选
CREATE INDEX IF NOT EXISTS idx_hkcr_cache_status
  ON hkcr_cache (company_status);
```

**注意事项：**
- `pg_trgm` extension 可能在 migration 010/011 已创建，使用 `IF NOT EXISTS`
- `company_number` 作为 UNIQUE 键，用于 ON CONFLICT UPSERT
- `fetched_at` 记录 CSV 数据获取时间，`last_synced_at` 记录数据库写入时间
  </action>
  <verify>
    <automated>grep -E "CREATE TABLE.*hkcr_cache|CREATE INDEX.*hkcr" db/migrations/038_hkcr_cache.sql | wc -l | grep -q "6"</automated>
  </verify>
  <done>
hkcr_cache 表 migration 文件已创建，包含：
- 表定义（company_number 主键 + 8 个数据字段）
- 4 个索引（2个 GIN trgm + 2个 B-tree）
- 文件大小 ≥ 30 行
  </done>
</task>

<task type="auto">
  <name>Task 2: 验证 migration 文件符合命名规范</name>
  <files>db/migrations/038_hkcr_cache.sql</files>
  <read_first>
    db/migrations/037_lei_cache.sql
  </read_first>
  <action>
验证 migration 文件命名和编号正确：
1. 文件名以 `038_` 开头（顺延 037）
2. 文件名包含描述性后缀 `hkcr_cache`
3. 文件位于 `db/migrations/` 目录
4. 确认 instrumentation.ts 会自动加载（无需手动干预）

**验证命令：**
```bash
ls db/migrations/038_hkcr_cache.sql
head -5 db/migrations/038_hkcr_cache.sql
```
  </action>
  <verify>
    <automated>ls db/migrations/038_hkcr_cache.sql && echo "OK"</automated>
  </verify>
  <done>
Migration 文件命名符合规范（038_前缀 + hkcr_cache 描述），位于正确目录
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration → Database | Migration 文件写入 PostgreSQL，需确保 SQL 安全 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.4-01-01 | Tampering | migration SQL | accept | Migration 文件由开发者编写，trusted source |
| T-999.4-01-02 | Information Disclosure | company data | mitigate | 仅存储公开注册数据，无敏感信息 |

## ASVS V5 Input Validation

- company_number 格式验证：regex `^\d{7}$` 在应用层实现（D-04），不在 migration 层
- migration 本身无需输入验证（纯 schema 定义）
</threat_model>

<verification>
**验证步骤：**
1. Migration 文件已创建且符合命名规范
2. SQL 语法正确（可通过 `npm run build` 间接验证）
3. 表结构符合 D-02 字段范围定义

**自动化验证：**
```bash
grep "CREATE TABLE.*hkcr_cache" db/migrations/038_hkcr_cache.sql
grep "company_number TEXT NOT NULL UNIQUE" db/migrations/038_hkcr_cache.sql
```
</verification>

<success_criteria>
- Migration 文件 `db/migrations/038_hkcr_cache.sql` 存在
- 表定义包含 company_number (UNIQUE)、company_name、company_name_chinese 等字段
- 4 个索引（GIN + B-tree）已定义
- 文件符合 038_命名规范
</success_criteria>

<output>
After completion, create `.planning/phases/999.4-hong-kong-companies-registry-integration/999.4-01-SUMMARY.md`
</output>