---
context: phase
phase: 999.4-hong-kong-companies-registry-integration
task: complete
total_tasks: 4
status: completed
last_updated: 2026-04-20T02:45:00Z
---

# HKCR Production Sync Fix — 已完成

_无阻塞约束。_

<current_state>
Phase 999.4 HKCR 集成已完成并部署到生产服务器。同步功能已修复并成功执行：

**服务器状态：**
- `/var/www/eti/Energy_trade_inspection` - 生产环境
- `hkcr_cache` 表有 278,796 条香港公司记录
- PM2 进程名: `eti`
- 所有代码已推送到 GitHub (master 分支)
</current_state>

<completed_work>

Completed Tasks:
- Task 1: UI dataSource 显示 — 添加 `dataSource: ['Hong Kong Companies Registry']` (commit: 9cbb9f8)
- Task 2: SourcesPanel 链接 — 添加 HKCR 到 SOURCE_LINKS (commit: 9cbb9f8)
- Task 3: Node.js fetch 修复 — AbortController + curl headers (commit: a34017e)
- Task 4: 日期格式修复 — DD-MM-YYYY → YYYY-MM-DD (commit: 5caa7ed)
- Task 5: 生产同步执行 — 278,796 records inserted, 0 errors

**服务器上的临时文件：**
- `sync_hkcr.mjs` - 独立同步脚本（可删除）
</completed_work>

<remaining_work>
无未完成任务。

Phase 999.4 已完成。可进行的后续工作：
- 测试 UI 搜索香港公司（如输入公司名或 7-8 位注册号）
- 删除服务器上的 `sync_hkcr.mjs`
- 运行定期增量同步（如每周）
</remaining_work>

<decisions_made>
- Node.js fetch 问题：`AbortSignal.timeout` 在服务器上不稳定，改用显式 `AbortController` + `setTimeout` + `clearTimeout`
- Headers 问题：添加 `User-Agent: curl/8.0` 和 `Accept: */*` 模拟 curl 行为
- 日期格式问题：HK CSV 使用 DD-MM-YYYY，PostgreSQL 需要 YYYY-MM-DD，添加 `convertHKDate()` 函数转换
</decisions_made>

<blockers>
无阻塞项。
</blockers>

## Required Reading (in order)
1. `src/lib/server/sync/hkcr.ts` — HKCR 同步模块核心代码
2. `src/lib/server/sync/index.ts` — 同步编排器，包含 `hkcr:full` 源
3. `src/app/company/[slug]/page.tsx` — SourcesPanel 中的 HKCR 链接

## Infrastructure State
- 生产服务器：Ubuntu 24.04, Nginx, PM2 (进程名: `eti`)
- 数据库：PostgreSQL 16, `hkcr_cache` 表已填充
- GitHub：master 分支，所有改动已推送
- 未提交改动：`package-lock.json` (可忽略)

## Critical Anti-Patterns (do NOT repeat these)
- [ANTI-PATTERN]: `AbortSignal.timeout()` 在服务器 Node.js 环境可能导致 fetch 失败 → 使用显式 AbortController
- [ANTI-PATTERN]: 直接 `new Date('DD-MM-YYYY')` PostgreSQL 无法解析 → 必须先转换为 ISO 格式

<context>
Phase 999.4 从讨论、规划到执行已完成。当前会话主要是修复生产部署后的同步问题。

关键技术发现：
1. curl 在服务器上正常工作，但 Node.js fetch 失败 — 通过模拟 curl headers 解决
2. CSV 文件的日期格式是 DD-MM-YYYY，需要转换
</context>

<next_action>
Phase 999.4 完成。可选下一步：
- 在 UI 测试搜索香港公司（如 "Jingrui" 或注册号 "79954086"）
- 删除服务器上的 `sync_hkcr.mjs`: `rm sync_hkcr.mjs`
- 开始下一个 phase（参见 ROADMAP.md）
</next_action>