---
phase: 999.4
plan: 03
type: execute
wave: 3
depends_on: [999.4-02]
files_modified: [src/lib/server/repository.ts, src/lib/server/sync/index.ts, src/app/api/admin/sync/route.ts]
autonomous: true
requirements: [D-01, D-02, D-03]
user_setup: []

must_haves:
  truths:
    - "repository.ts Tier 2 Promise.all 包含 searchHKCRCache"
    - "HKCR 结果出现在搜索结果中（registrySource: 'hkcr'）"
    - "管理员可通过 POST /api/admin/sync { source: 'hkcr:full' } 触发同步"
    - "SFC 警告联动复用 getWarningHits()（per D-03）"
  artifacts:
    - path: "src/lib/server/repository.ts"
      provides: "Tier 2 HKCR 搜索集成"
      modification_points: ["Tier 2 Promise.all", "result mapping", "non_lei_cache write"]
    - path: "src/lib/server/sync/index.ts"
      provides: "SyncSource 'hkcr:full' 注册"
      modification_points: ["SyncSource type", "runSync dispatch"]
    - path: "src/app/api/admin/sync/route.ts"
      provides: "管理员同步 API 扩展"
      modification_points: ["POST handler hkcr:full branch"]
  key_links:
    - from: "src/lib/server/repository.ts"
      to: "src/lib/server/sync/hkcr.ts"
      via: "import { searchHKCRCache, hkcrToSearchResult }"
      pattern: "searchHKCRCache\(query, 5\)"
    - from: "src/lib/server/sync/index.ts"
      to: "src/lib/server/sync/hkcr.ts"
      via: "import { syncHKCRFull }"
      pattern: "syncHKCRFull\(\)"
    - from: "src/app/api/admin/sync/route.ts"
      to: "src/lib/server/sync/index.ts"
      via: "runSync('hkcr:full')"
      pattern: "source === 'hkcr:full'"
---

<objective>
将 HKCR 本地缓存集成到 repository.ts Tier 2 搜索流程，并注册 sync source 到管理员 API，实现香港公司数据的搜索和同步功能。

Purpose: 完成 HKCR 数据源从 sync → cache → search → API 的完整链路
Output: repository.ts Tier 2 集成 + sync/index.ts 注册 + admin API 扩展
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@.planning/phases/999.4-hong-kong-companies-registry-integration/999.4-02-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.4-hong-kong-companies-registry-integration/999.4-CONTEXT.md
@.planning/phases/999.4-hong-kong-companies-registry-integration/999.4-RESEARCH.md
@.planning/phases/999.4-hong-kong-companies-registry-integration/999.4-PATTERNS.md
</context>

<interfaces>
<!-- repository.ts Tier 2 集成点（line 553-639） -->
```typescript
// Source: src/lib/server/repository.ts line 560-566
const [acraEntities, chEntities, zefixEntities, ocEntities, gleifRecords] = await Promise.all([
  searchACRA(query, 5).catch(() => []),
  searchCompaniesHouse(query, 5).catch(() => []),
  searchZefix(query, 5).catch(() => []),
  searchOpenCorporates(query, 5).catch(() => []),
  searchGleifMultiple(query, 5).catch(() => []),
])

// Source: src/lib/server/repository.ts line 568-571
const acraResults  = acraEntities.filter((e) => !tier1RegNums.has(e.uen)).map(acraToSearchResult)
const chResults    = chEntities.filter((c) => !tier1RegNums.has(c.company_number)).map(chToSearchResult)
const zefixResults = zefixEntities.filter((c) => !tier1RegNums.has(c.uid)).map(zefixToSearchResult)
const ocResults    = ocEntities.filter((c) => !tier1RegNums.has(c.company_number)).map(ocToSearchResult)

// Source: src/lib/server/repository.ts line 573-579
const allRegNums = new Set([
  ...tier1RegNums,
  ...acraResults.map((r) => r.registrationNumber).filter(Boolean),
  ...chResults.map((r) => r.registrationNumber).filter(Boolean),
  ...zefixResults.map((r) => r.registrationNumber).filter(Boolean),
  ...ocResults.map((r) => r.registrationNumber).filter(Boolean),
])

// Source: src/lib/server/repository.ts line 595-627
const toCache: Parameters<typeof writeNonLeiCache>[0] = [
  ...acraResults.map((r) => ({
    id:             `acra:${r.registrationNumber ?? r.id}`,
    canonicalName:  r.name,
    registrySource: 'acra',
    jurisdiction:   'SG' as string | null,
    result:         r,
  })),
  ...chResults.map((r) => ({
    id:             `ch:${r.registrationNumber ?? r.id}`,
    canonicalName:  r.name,
    registrySource: 'ch',
    jurisdiction:   'GB' as string | null,
    result:         r,
  })),
  // ...
]

// Source: src/lib/server/repository.ts line 631-638
return [
  ...tier1Results,
  ...acraResults,
  ...chResults,
  ...zefixResults,
  ...ocResults,
  ...gleifTier2,
].slice(0, 20)
```

<!-- sync/index.ts SyncSource type + dispatch pattern -->
```typescript
// Source: src/lib/server/sync/index.ts line 13-26
export type SyncSource =
  | 'ofac'
  | 'fraud'
  | 'legitdomains'
  | 'warninglists'
  | 'gleif'
  | 'gleif:full'
  | 'gleif:delta'
  | 'gleif:level2'
  | 'gleif:level2:full'
  | 'gleif:exceptions'
  | 'gleif:exceptions:full'
  | 'sanctions-gleif-link'
  | 'all'

// Source: src/lib/server/sync/index.ts line 105-113 (gleif:delta pattern)
if (source === 'gleif:delta') {
  const start = Date.now()
  try {
    const { count } = await syncLeiDelta()
    results.push({ source: 'gleif:delta', success: true, count, durationMs: Date.now() - start })
  } catch (err) {
    results.push({ source: 'gleif:delta', success: false, error: String(err), durationMs: Date.now() - start })
  }
}
```

<!-- admin/sync/route.ts gleif:full pattern -->
```typescript
// Source: src/app/api/admin/sync/route.ts line 133-151
// GLEIF full import: spawn background process (875 MB download, hours to complete)
if (source === 'gleif:full') {
  const scriptPath = path.join(process.cwd(), 'scripts', 'sync-gleif-full.mjs')
  const child = spawn(process.execPath, [scriptPath], {
    detached: true,
    stdio: 'ignore',
    env: {
      ...process.env,
      DATABASE_URL: process.env.DATABASE_URL,
    },
  })
  child.unref()

  return NextResponse.json({
    success: true,
    source: 'gleif:full',
    pid: child.pid,
    message: 'GLEIF full Level 1 import started in background. Check GET /api/admin/sync for progress via sanctions_sync_log.',
  })
}
```

<!-- D-03: SFC 警告联动 -->
```typescript
// Source: src/lib/server/warning-lists.ts line 25-67
export async function getWarningHits(
  entityName: string,
  _entityType?: 'company' | 'vessel' | 'terminal'
): Promise<WarningHit[]> { ... }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: repository.ts Tier 2 Promise.all 集成</name>
  <files>src/lib/server/repository.ts</files>
  <read_first>
    src/lib/server/repository.ts
    src/lib/server/sync/hkcr.ts
    .planning/phases/999.4-hong-kong-companies-registry-integration/999.4-PATTERNS.md
  </read_first>
  <action>
修改 repository.ts Tier 2 区块，集成 HKCR 本地缓存查询（per D-01）。

**修改点 1：Import（文件顶部 ~line 7-31）**
```typescript
// 添加 HKCR 导入
import { searchHKCRCache, hkcrToSearchResult, mightBeHKNumber } from './sync/hkcr'
```

**修改点 2：Tier 2 Promise.all（~line 560-566）**
```typescript
// 将 searchHKCRCache 加入并行查询
const [acraEntities, chEntities, zefixEntities, ocEntities, gleifRecords, hkcrEntities] = await Promise.all([
  searchACRA(query, 5).catch(() => []),
  searchCompaniesHouse(query, 5).catch(() => []),
  searchZefix(query, 5).catch(() => []),
  searchOpenCorporates(query, 5).catch(() => []),
  searchGleifMultiple(query, 5).catch(() => []),
  searchHKCRCache(query, 5),  // HKCR: LOCAL cache query (no catch needed - returns [])
])
```

**注意事项：**
- `searchHKCRCache` 是本地缓存查询，不是外部 API，不需要 `.catch(() => [])`
- 与 gleifRecords 同级别（都是本地缓存）
  </action>
  <verify>
    <automated>grep -E "import.*searchHKCRCache|hkcrEntities|searchHKCRCache\(query, 5\)" src/lib/server/repository.ts | wc -l | grep -q "3"</automated>
  </verify>
  <done>
repository.ts Tier 2 Promise.all 已包含 searchHKCRCache
  </done>
</task>

<task type="auto">
  <name>Task 2: repository.ts 结果映射 + 去重</name>
  <files>src/lib/server/repository.ts</files>
  <read_first>
    src/lib/server/repository.ts
    src/lib/server/sync/hkcr.ts
  </read_first>
  <action>
修改 repository.ts 结果映射和去重逻辑（per D-01, D-02）。

**修改点 3：Result mapping（~line 568-571）**
```typescript
// 添加 HKCR 结果映射
const acraResults  = acraEntities.filter((e) => !tier1RegNums.has(e.uen)).map(acraToSearchResult)
const chResults    = chEntities.filter((c) => !tier1RegNums.has(c.company_number)).map(chToSearchResult)
const zefixResults = zefixEntities.filter((c) => !tier1RegNums.has(c.uid)).map(zefixToSearchResult)
const ocResults    = ocEntities.filter((c) => !tier1RegNums.has(c.company_number)).map(ocToSearchResult)
const hkcrResults  = hkcrEntities
  .filter((c) => !tier1RegNums.has(c.company_number))
  .map(hkcrToSearchResult)  // NEW: HKCR result conversion
```

**修改点 4：allRegNums update（~line 573-579）**
```typescript
// 添加 HK 注册号到去重集合
const allRegNums = new Set([
  ...tier1RegNums,
  ...acraResults.map((r) => r.registrationNumber).filter(Boolean),
  ...chResults.map((r) => r.registrationNumber).filter(Boolean),
  ...zefixResults.map((r) => r.registrationNumber).filter(Boolean),
  ...ocResults.map((r) => r.registrationNumber).filter(Boolean),
  ...hkcrResults.map((r) => r.registrationNumber).filter(Boolean),  // NEW
])
```

**注意事项：**
- hkcrResults 需在 gleifTier2 计算之前定义（因为 gleifTier2 依赖 allRegNums）
- 过滤逻辑：排除已在 Tier 1 中出现的注册号
  </action>
  <verify>
    <automated>grep -E "hkcrResults|hkcrToSearchResult|\.map\(hkcrToSearchResult\)" src/lib/server/repository.ts | wc -l | grep -q "3"</automated>
  </verify>
  <done>
repository.ts 结果映射已包含 hkcrResults，去重逻辑已添加 HK 注册号
  </done>
</task>

<task type="auto">
  <name>Task 3: repository.ts non_lei_cache 写入 + 返回</name>
  <files>src/lib/server/repository.ts</files>
  <read_first>
    src/lib/server/repository.ts
    src/lib/server/sync/hkcr.ts
  </read_first>
  <action>
修改 repository.ts 缓存写入和最终返回（per D-01）。

**修改点 5：toCache update（~line 595-627）**
```typescript
// 添加 HKCR 结果到缓存写入
const toCache: Parameters<typeof writeNonLeiCache>[0] = [
  ...acraResults.map((r) => ({
    id:             `acra:${r.registrationNumber ?? r.id}`,
    canonicalName:  r.name,
    registrySource: 'acra',
    jurisdiction:   'SG' as string | null,
    result:         r,
  })),
  ...chResults.map((r) => ({
    id:             `ch:${r.registrationNumber ?? r.id}`,
    canonicalName:  r.name,
    registrySource: 'ch',
    jurisdiction:   'GB' as string | null,
    result:         r,
  })),
  ...zefixResults.map((r) => ({
    id:             `zefix:${r.registrationNumber ?? r.id}`,
    canonicalName:  r.name,
    registrySource: 'zefix',
    jurisdiction:   'CH' as string | null,
    result:         r,
  })),
  ...ocResults.map((r) => ({
    id:             r.id,
    canonicalName:  r.name,
    registrySource: 'oc',
    jurisdiction:   null,
    result:         r,
  })),
  ...hkcrResults.map((r) => ({
    id:             `hkcr:${r.registrationNumber ?? r.id}`,  // NEW
    canonicalName:  r.name,
    registrySource: 'hkcr',
    jurisdiction:   'HK' as string | null,
    result:         r,
  })),
]
```

**修改点 6：Return array（~line 631-638）**
```typescript
// 添加 HKCR 结果到最终返回
return [
  ...tier1Results,
  ...acraResults,
  ...chResults,
  ...zefixResults,
  ...ocResults,
  ...gleifTier2,
  ...hkcrResults,  // NEW
].slice(0, 20)
```

**注意事项：**
- HKCR 结果标记 `registrySource: 'hkcr'`
- jurisdiction 固定为 `'HK'`
- 结果顺序：Tier 1 → ACRA → CH → Zefix → OC → GLEIF → HKCR
  </action>
  <verify>
    <automated>grep -E "registrySource: 'hkcr'|jurisdiction: 'HK'|\.hkcrResults" src/lib/server/repository.ts | wc -l | grep -q "3"</automated>
  </verify>
  <done>
repository.ts 缓存写入和返回已包含 HKCR 结果
  </done>
</task>

<task type="auto">
  <name>Task 4: sync/index.ts SyncSource 注册</name>
  <files>src/lib/server/sync/index.ts</files>
  <read_first>
    src/lib/server/sync/index.ts
    src/lib/server/sync/hkcr.ts
  </read_first>
  <action>
修改 sync/index.ts，注册 HKCR sync source（per D-01）。

**修改点 1：Import（文件顶部）**
```typescript
// 添加 HKCR 导入
import { syncHKCRFull } from './hkcr'
```

**修改点 2：SyncSource type（~line 13-26）**
```typescript
// 添加 'hkcr:full' 到 SyncSource union
export type SyncSource =
  | 'ofac'
  | 'fraud'
  | 'legitdomains'
  | 'warninglists'
  | 'gleif'
  | 'gleif:full'
  | 'gleif:delta'
  | 'gleif:level2'
  | 'gleif:level2:full'
  | 'gleif:exceptions'
  | 'gleif:exceptions:full'
  | 'sanctions-gleif-link'
  | 'hkcr:full'  // NEW
  | 'all'
```

**修改点 3：runSync dispatch 块（~line 105-153 附近添加）**
```typescript
// 添加 HKCR sync dispatch（类比 gleif:delta 模式）
if (source === 'hkcr:full') {
  const start = Date.now()
  try {
    const { inserted, errors } = await syncHKCRFull()
    results.push({
      source: 'hkcr:full',
      success: errors === 0,
      count: inserted,
      error: errors > 0 ? `${errors} files failed` : undefined,
      durationMs: Date.now() - start,
    })
  } catch (err) {
    results.push({
      source: 'hkcr:full',
      success: false,
      error: String(err),
      durationMs: Date.now() - start,
    })
  }
}
```
  </action>
  <verify>
    <automated>grep -E "import.*syncHKCRFull|hkcr:full|source === 'hkcr:full'" src/lib/server/sync/index.ts | wc -l | grep -q "3"</automated>
  </verify>
  <done>
sync/index.ts 已注册 'hkcr:full' SyncSource，导入 syncHKCRFull
  </done>
</task>

<task type="auto">
  <name>Task 5: admin/sync/route.ts API 扩展</name>
  <files>src/app/api/admin/sync/route.ts</files>
  <read_first>
    src/app/api/admin/sync/route.ts
    src/lib/server/sync/index.ts
  </read_first>
  <action>
修改 admin/sync/route.ts，添加 hkcr:full 同步触发分支。

**修改点：POST handler（~line 133-151 附近添加，类比 gleif:full）**
```typescript
// HKCR full sync: CSV batch download + import
if (source === 'hkcr:full') {
  try {
    const results = await runSync('hkcr:full')
    const hasError = results.some((r) => !r.success)
    return NextResponse.json(
      { results },
      { status: hasError ? 207 : 200 }
    )
  } catch (error: unknown) {
    const message = error instanceof Error ? error.message : String(error)
    return NextResponse.json({ error: message }, { status: 500 })
  }
}
```

**注意事项：**
- hkcr:full sync 在进程内执行（CSV 文件较小，不需要 child process）
- 返回结果包含 inserted count 和 errors count
- 路径添加在 gleif:delta 分支附近
  </action>
  <verify>
    <automated>grep -E "source === 'hkcr:full'|runSync\('hkcr:full'\)" src/app/api/admin/sync/route.ts | wc -l | grep -q "2"</automated>
  </verify>
  <done>
admin/sync/route.ts 已添加 hkcr:full 同步触发分支
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin/sync/route.ts → sync/index.ts | 管理员 API 调用 sync 模块，需认证保护 |
| repository.ts → hkcr.ts | Tier 2 搜索调用本地缓存，无外部 API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.4-03-01 | Spoofing | admin/sync auth | mitigate | isAuthorized() check before POST (existing) |
| T-999.4-03-02 | Tampering | hkcr query input | mitigate | pg_trgm parameterized query ($1, $2, $3) |
| T-999.4-03-03 | Information Disclosure | non_lei_cache | accept | 缓存公开注册数据，无敏感信息 |

## ASVS V5 Input Validation

- repository.ts 使用 searchHKCRCache 参数化查询
- admin/sync/route.ts 验证 source 字符串（已有 switch/if 分支）
</threat_model>

<verification>
**验证步骤：**
1. repository.ts Tier 2 包含 searchHKCRCache
2. hkcrResults 正确映射和去重
3. sync/index.ts 导入 syncHKCRFull 并注册 'hkcr:full'
4. admin/sync/route.ts 支持 POST { source: 'hkcr:full' }

**自动化验证：**
```bash
grep "searchHKCRCache" src/lib/server/repository.ts
grep "hkcr:full" src/lib/server/sync/index.ts
grep "source === 'hkcr:full'" src/app/api/admin/sync/route.ts
```
</verification>

<success_criteria>
- repository.ts Tier 2 Promise.all 包含 searchHKCRCache
- hkcrResults 正确映射（hkcrToSearchResult）+ 去重（tier1RegNums）
- non_lei_cache 写入包含 HKCR 结果（registrySource: 'hkcr'）
- sync/index.ts 导出 SyncSource 'hkcr:full' + runSync dispatch
- admin/sync/route.ts 支持 POST { source: 'hkcr:full' }
</success_criteria>

<output>
After completion, create `.planning/phases/999.4-hong-kong-companies-registry-integration/999.4-03-SUMMARY.md`
</output>