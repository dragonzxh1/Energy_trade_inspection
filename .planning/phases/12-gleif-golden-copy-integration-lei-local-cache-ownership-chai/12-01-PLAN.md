---
phase: 12-gleif-golden-copy-integration-lei-local-cache-ownership-chai
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/migrations/037_lei_cache.sql
  - src/lib/server/sync/index.ts
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - D-01
  - D-08
  - D-09

must_haves:
  truths:
    - "Migration 037_lei_cache.sql 存在且能成功执行（lei_cache 表 + 3 个索引）"
    - "npm 包 unzipper 和 stream-json 已安装，@types/unzipper 已安装到 devDependencies"
    - "SyncSource 联合类型包含 gleif、gleif:full、gleif:delta、gleif:level2、gleif:exceptions"
  artifacts:
    - path: "db/migrations/037_lei_cache.sql"
      provides: "lei_cache 表 DDL + 索引定义"
      contains: "CREATE TABLE lei_cache"
    - path: "src/lib/server/sync/index.ts"
      provides: "扩展后的 SyncSource 联合类型"
      contains: "gleif:full"
  key_links:
    - from: "src/lib/server/sync/index.ts"
      to: "SyncSource 联合类型"
      via: "gleif | gleif:full | gleif:delta | gleif:level2 | gleif:exceptions 并入 union"
      pattern: "gleif:full"
---

<objective>
为 Phase 12 构建地基：安装流式 ZIP/JSON 依赖包，创建 lei_cache 数据库迁移，并将 GLEIF 同步源加入 SyncSource 联合类型。

Purpose: 后续所有 Plan（同步模块、缓存优先查找、风险信号）都依赖 lei_cache 表存在且 npm 包已就绪。
Output: 037_lei_cache.sql（完整 DDL）、安装后的 unzipper/stream-json、更新后的 SyncSource 类型。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-gleif-golden-copy-integration-lei-local-cache-ownership-chai/12-CONTEXT.md
@.planning/phases/12-gleif-golden-copy-integration-lei-local-cache-ownership-chai/12-RESEARCH.md
@.planning/phases/12-gleif-golden-copy-integration-lei-local-cache-ownership-chai/12-PATTERNS.md

<interfaces>
<!-- 关键接口：现有 SyncSource 联合类型（将被扩展） -->
<!-- 源文件：src/lib/server/sync/index.ts 第 11 行 -->
```typescript
export type SyncSource = 'ofac' | 'fraud' | 'legitdomains' | 'warninglists' | 'all'
```

<!-- Migration 036 模式（037 需紧随其后）-->
<!-- 源文件：db/migrations/036_icij_sanctions_linkage.sql -->
```sql
ALTER TABLE icij_entities
  ADD COLUMN IF NOT EXISTS is_sanctioned BOOLEAN NOT NULL DEFAULT FALSE,
  ADD COLUMN IF NOT EXISTS sanctions_match TEXT;
CREATE INDEX IF NOT EXISTS idx_icij_sanctioned ON icij_entities (is_sanctioned) WHERE is_sanctioned = TRUE;
```

<!-- ICIJ 表索引模式（lei_cache 复用）-->
<!-- 源文件：db/migrations/011_ports_psc_icij.sql 第 73-78 行 -->
```sql
CREATE INDEX IF NOT EXISTS idx_icij_name_trgm ON icij_entities USING GIN (lower(name) gin_trgm_ops);
CREATE INDEX IF NOT EXISTS idx_icij_dataset   ON icij_entities (dataset);
CREATE INDEX IF NOT EXISTS idx_icij_linked    ON icij_entities (linked_entity_id) WHERE linked_entity_id IS NOT NULL;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 安装 npm 依赖 + 创建 037_lei_cache.sql 迁移</name>
  <read_first>
    - db/migrations/036_icij_sanctions_linkage.sql （前一个迁移的格式/风格参考）
    - db/migrations/011_ports_psc_icij.sql （icij_entities 索引模式参考）
    - package.json （确认当前依赖状态，避免重复安装）
  </read_first>
  <files>db/migrations/037_lei_cache.sql, package.json, package-lock.json</files>
  <action>
**步骤 1 — 安装 npm 包：**

```bash
npm install unzipper stream-json
npm install --save-dev @types/unzipper
```

注意：`@types/stream-json` 不存在，不要尝试安装。后续实现文件中对 stream-json 的导入使用 `// @ts-expect-error` 注释。

**步骤 2 — 创建 `db/migrations/037_lei_cache.sql`：**

文件内容必须完全按照 CONTEXT.md D-01 规范创建，使用 `IF NOT EXISTS` 守护：

```sql
-- Migration 037: GLEIF LEI local cache
-- Stores Golden Copy Level 1 (entity), Level 2 RR (ownership chain),
-- and REPEX (reporting exceptions) data for cache-first LEI lookups.

CREATE EXTENSION IF NOT EXISTS pg_trgm;   -- may already exist from migration 010/011

CREATE TABLE IF NOT EXISTS lei_cache (
  lei                              CHAR(20)     PRIMARY KEY,
  legal_name                       TEXT         NOT NULL,
  jurisdiction                     CHAR(2),
  country                          CHAR(2),
  registration_authority_id        TEXT,
  registration_authority_entity_id TEXT,
  initial_registration_date        DATE,
  entity_status                    TEXT,
  entity_category                  TEXT,
  direct_parent_lei                CHAR(20),
  ultimate_parent_lei              CHAR(20),
  reporting_exception_type         TEXT,
  reporting_exception_reason       TEXT,
  last_synced_at                   TIMESTAMPTZ  NOT NULL DEFAULT NOW(),
  created_at                       TIMESTAMPTZ  NOT NULL DEFAULT NOW()
);

-- Trigram index for fuzzy name search (matches icij_entities pattern)
CREATE INDEX IF NOT EXISTS lei_cache_legal_name_trgm
  ON lei_cache USING GIN (legal_name gin_trgm_ops);

-- B-tree index for registry number lookup (used by resolveGleifRecord routing)
CREATE INDEX IF NOT EXISTS lei_cache_registration_authority_entity_id
  ON lei_cache (registration_authority_entity_id);

-- B-tree index for jurisdiction filter
CREATE INDEX IF NOT EXISTS lei_cache_jurisdiction
  ON lei_cache (jurisdiction);
```

迁移文件遵循既有命名约定：`037_` 前缀，纯 SQL，无 DOWN 语句（与 036 保持一致）。

**验证：** 迁移文件路径 `db/migrations/037_lei_cache.sql` 存在。运行 `npm run type-check` 无报错（此时不需要运行迁移，instrumentation.ts 会在启动时自动执行）。
  </action>
  <verify>
    <automated>grep -q "CREATE TABLE IF NOT EXISTS lei_cache" db/migrations/037_lei_cache.sql && grep -q "lei_cache_legal_name_trgm" db/migrations/037_lei_cache.sql && node -e "require('unzipper')" 2>/dev/null && echo "ok" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `db/migrations/037_lei_cache.sql` 包含字符串 `CREATE TABLE IF NOT EXISTS lei_cache`
    - `db/migrations/037_lei_cache.sql` 包含字符串 `lei_cache_legal_name_trgm`
    - `db/migrations/037_lei_cache.sql` 包含字符串 `lei_cache_registration_authority_entity_id`
    - `db/migrations/037_lei_cache.sql` 包含字符串 `lei_cache_jurisdiction`
    - `db/migrations/037_lei_cache.sql` 包含列定义：`direct_parent_lei CHAR(20)`、`ultimate_parent_lei CHAR(20)`、`reporting_exception_type TEXT`
    - `node -e "require('unzipper')"` 执行无报错
    - `node -e "require('stream-json/streamers/StreamArray')"` 执行无报错
    - `package.json` 的 `dependencies` 中包含 `"unzipper"` 和 `"stream-json"`
    - `package.json` 的 `devDependencies` 中包含 `"@types/unzipper"`
  </acceptance_criteria>
  <done>迁移文件存在且完整，npm 包已安装可用。</done>
</task>

<task type="auto">
  <name>Task 2: 扩展 SyncSource 联合类型 (per D-09)</name>
  <read_first>
    - src/lib/server/sync/index.ts （当前 SyncSource 定义和 runSync() 实现，必须读取完整文件）
  </read_first>
  <files>src/lib/server/sync/index.ts</files>
  <action>
读取 `src/lib/server/sync/index.ts` 全文后，将第 11 行的 `SyncSource` 联合类型扩展为：

```typescript
export type SyncSource =
  | 'ofac'
  | 'fraud'
  | 'legitdomains'
  | 'warninglists'
  | 'gleif'
  | 'gleif:full'
  | 'gleif:delta'
  | 'gleif:level2'
  | 'gleif:exceptions'
  | 'all'
```

**重要约束：**
- 不要修改 `runSync()` 函数体（gleif dispatch 逻辑将在 Plan 05 添加，届时 gleif-golden-copy.ts 已存在）
- 不要添加任何 import（gleif-golden-copy.ts 尚未创建）
- 不要删除或重排任何现有 dispatch 块
- 只做这一处修改：替换 `SyncSource` 类型定义行

执行后运行 `npm run type-check` 确认 TypeScript 仍通过。
  </action>
  <verify>
    <automated>grep -q "gleif:full" src/lib/server/sync/index.ts && grep -q "gleif:delta" src/lib/server/sync/index.ts && npm run type-check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/server/sync/index.ts` 包含字符串 `'gleif:full'`
    - `src/lib/server/sync/index.ts` 包含字符串 `'gleif:delta'`
    - `src/lib/server/sync/index.ts` 包含字符串 `'gleif:level2'`
    - `src/lib/server/sync/index.ts` 包含字符串 `'gleif:exceptions'`
    - `npm run type-check` 以退出码 0 完成（无类型错误）
    - `runSync()` 函数体未发生变化（现有 ofac/fraud/legitdomains/warninglists dispatch 块完整保留）
  </acceptance_criteria>
  <done>SyncSource 联合类型扩展完毕，TypeScript 编译通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | 安装第三方包引入供应链风险 |
| SQL migration → PostgreSQL | DDL 在数据库级别执行 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | Tampering | npm install (unzipper, stream-json) | accept | 两个包均为成熟开源包，版本已在 RESEARCH.md 确认（unzipper@0.12.3, stream-json@2.1.0）；npm 锁文件固定版本 |
| T-12-01-02 | Tampering | 037_lei_cache.sql DDL | mitigate | 使用 `IF NOT EXISTS` 守护使迁移幂等；迁移由 instrumentation.ts advisory lock 防止并发执行 |
| T-12-01-03 | Denial of Service | SyncSource 类型扩展 | accept | 仅类型级变更，不影响运行时；TypeScript 编译门控确保不引入语法错误 |
</threat_model>

<verification>
Wave 1 完成标志：
1. `db/migrations/037_lei_cache.sql` 存在且包含完整 DDL（lei_cache 表 + 3 个索引）
2. `node -e "require('unzipper')"` 和 `node -e "require('stream-json/streamers/StreamArray')"` 均无报错
3. `src/lib/server/sync/index.ts` SyncSource 包含 gleif:full / gleif:delta / gleif:level2 / gleif:exceptions
4. `npm run type-check` 通过（0 个 TypeScript 错误）
</verification>

<success_criteria>
- lei_cache 迁移文件存在，DDL 与 CONTEXT.md D-01 规范完全一致（含所有列名和类型）
- unzipper + stream-json 可在 Node.js 中 require 成功
- SyncSource 联合类型已包含所有 5 个 GLEIF 子源
- TypeScript 编译无报错
</success_criteria>

<output>
After completion, create `.planning/phases/12-gleif-golden-copy-integration-lei-local-cache-ownership-chai/12-01-SUMMARY.md`
</output>
