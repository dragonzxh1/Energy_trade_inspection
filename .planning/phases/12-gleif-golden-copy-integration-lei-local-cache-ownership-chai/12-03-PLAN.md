---
phase: 12-gleif-golden-copy-integration-lei-local-cache-ownership-chai
plan: 03
type: execute
wave: 3
depends_on:
  - plan-01
  - plan-02
files_modified:
  - src/lib/server/repository.ts
  - src/lib/server/gleif.ts
autonomous: true
requirements:
  - D-05
  - D-06

must_haves:
  truths:
    - "resolveGleifRecord() 在调用 getGleifRecordByLei() 前先查 lei_cache"
    - "lei_cache 命中时直接返回，不发出 HTTP 请求到 GLEIF 实时 API"
    - "lei_cache 未命中时调用实时 API，并将结果写回 lei_cache（warm-on-miss）"
    - "searchEntities() GLEIF 路径先查 lei_cache 相似度，命中时跳过 searchGleifMultiple()"
    - "getGleifUltimateParentJurisdiction() 从 lei_cache 读取 ultimate_parent_lei，不发实时 API 调用（若已缓存）"
  artifacts:
    - path: "src/lib/server/repository.ts"
      provides: "getLeiCacheRecord() + writeLeiCacheRecord() 内部 helper + 两处调用点更新（resolveGleifRecord + searchEntities）"
      contains: "SELECT * FROM lei_cache WHERE lei = $1"
    - path: "src/lib/server/gleif.ts"
      provides: "getGleifUltimateParentJurisdiction() 缓存优先实现"
      contains: "ultimate_parent_lei"
  key_links:
    - from: "repository.ts resolveGleifRecord()"
      to: "lei_cache 表"
      via: "getLeiCacheRecord(lei) SELECT * FROM lei_cache WHERE lei = $1 LIMIT 1"
      pattern: "FROM lei_cache WHERE lei"
    - from: "repository.ts searchEntities() GLEIF path"
      to: "lei_cache 表"
      via: "SIMILARITY(legal_name, query) > 0.45 AND entity_status = 'ACTIVE'"
      pattern: "SIMILARITY.*lei_cache"
    - from: "repository.ts writeLeiCacheRecord()"
      to: "lei_cache 表"
      via: "INSERT ... ON CONFLICT (lei) DO UPDATE"
      pattern: "ON CONFLICT \\(lei\\) DO UPDATE"
    - from: "gleif.ts getGleifUltimateParentJurisdiction()"
      to: "lei_cache 表"
      via: "SELECT ultimate_parent_lei, jurisdiction FROM lei_cache WHERE lei = $1"
      pattern: "ultimate_parent_lei"
---

<objective>
在 repository.ts 的两个关键函数中插入 lei_cache 缓存优先逻辑（resolveGleifRecord 和 searchEntities），并将 gleif.ts 的 getGleifUltimateParentJurisdiction() 改写为 lei_cache 优先、实时 API 降级的实现。

Purpose: 将 GLEIF 实时 API 调用频率降至接近零（缓存命中时），减少延迟和外部 API 依赖。D-05 要求 getGleifUltimateParentJurisdiction() 若两级 LEI 均已缓存则不调用实时 API——该函数在 gleif.ts 中（trade-service.ts 通过 import 调用），必须在 gleif.ts 内修改。
Output: 更新后的 repository.ts（两个 helper + 两处调用点）和更新后的 gleif.ts（cache-first getGleifUltimateParentJurisdiction）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-gleif-golden-copy-integration-lei-local-cache-ownership-chai/12-CONTEXT.md
@.planning/phases/12-gleif-golden-copy-integration-lei-local-cache-ownership-chai/12-PATTERNS.md
@.planning/phases/12-gleif-golden-copy-integration-lei-local-cache-ownership-chai/12-01-SUMMARY.md
@.planning/phases/12-gleif-golden-copy-integration-lei-local-cache-ownership-chai/12-02-SUMMARY.md

<interfaces>
<!-- 现有 resolveGleifRecord 函数签名 -->
<!-- 源文件：src/lib/server/repository.ts 第 617 行 -->
```typescript
async function resolveGleifRecord(record: Awaited<ReturnType<typeof getGleifRecordByLei>>): Promise<Company | null>
```

<!-- 现有 searchEntities GLEIF 路径（第 463-517 行）-->
<!-- 在 shouldSearchCompanies && localResults.length === 0 块中，调用 searchGleifMultiple() -->
<!-- 需在 searchGleifMultiple 调用之前插入 lei_cache 相似度查询 -->

<!-- 现有 GleifLeiRecord 接口（gleif.ts 导出）-->
```typescript
export interface GleifLeiRecord {
  lei: string
  legalName: string
  jurisdiction: string | null
  country: string | null
  initialRegistrationDate: string | null
  registrationAuthorityId: string | null
  registrationAuthorityEntityId: string | null
}
```

<!-- LeiCacheRow 接口（由 gleif-golden-copy.ts 导出，Plan 02 产物）-->
```typescript
export interface LeiCacheRow {
  lei: string
  legal_name: string
  jurisdiction: string | null
  country: string | null
  registration_authority_id: string | null
  registration_authority_entity_id: string | null
  initial_registration_date: string | null
  entity_status: string
  entity_category: string | null
  direct_parent_lei: string | null
  ultimate_parent_lei: string | null
  reporting_exception_type: string | null
  reporting_exception_reason: string | null
  last_synced_at: string
  created_at: string
}
```

<!-- 现有 getGleifUltimateParentJurisdiction（gleif.ts 第 249-290 行）-->
<!-- 当前实现：两次 GLEIF 实时 API 调用（ultimate-parent-relationship + lei-records/{parentLei}）-->
<!-- 调用者：src/lib/server/trade-service.ts 第 302 行 -->
```typescript
export async function getGleifUltimateParentJurisdiction(lei: string): Promise<string | null>
```

<!-- cache-first 模式参考 -->
<!-- 源文件：src/lib/server/intelligence-cache.ts -->
```typescript
// 读：SELECT data_json FROM intelligence_cache WHERE entity_type = $1 AND entity_key = $2
// 写：INSERT ... ON CONFLICT DO UPDATE SET ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 repository.ts 中添加 lei_cache helper 函数和缓存优先 resolveGleifRecord；在 gleif.ts 中改写 getGleifUltimateParentJurisdiction 为缓存优先</name>
  <read_first>
    - src/lib/server/repository.ts （必须读取：第 1-50 行导入区、第 610-715 行 resolveGleifRecord 完整实现）
    - src/lib/server/gleif.ts （第 249-291 行：getGleifUltimateParentJurisdiction 完整函数 + 顶部 import 区）
    - src/lib/server/intelligence-cache.ts （第 1-50 行：UPSERT ON CONFLICT 模式）
    - src/lib/server/sync/gleif-golden-copy.ts （确认 LeiCacheRow 接口定义和导出名称）
  </read_first>
  <files>src/lib/server/repository.ts, src/lib/server/gleif.ts</files>
  <action>
**--- repository.ts 修改 ---**

读取 repository.ts 全文后，进行以下外科手术式修改（不触碰无关代码）：

**修改 1 — 在文件顶部 import 区块末尾添加 LeiCacheRow 导入：**

在现有 import 语句之后（在第一个 `const` 或 `function` 之前）添加：
```typescript
import type { LeiCacheRow } from '@/lib/server/sync/gleif-golden-copy'
```

**修改 2 — 在 `resolveGleifRecord` 函数之前添加两个 helper 函数：**

找到 `async function resolveGleifRecord(` 这一行，在其前方插入以下两个 helper：

```typescript
/** Cache-first: read a LEI record from lei_cache. Returns null on miss or DB error. */
async function getLeiCacheRecord(lei: string): Promise<LeiCacheRow | null> {
  try {
    const { rows } = await db.query<LeiCacheRow>(
      `SELECT * FROM lei_cache WHERE lei = $1 LIMIT 1`,
      [lei],
    )
    return rows[0] ?? null
  } catch {
    return null
  }
}

/** Write a GleifLeiRecord to lei_cache on cache miss (warm-on-miss). */
async function writeLeiCacheRecord(record: GleifLeiRecord): Promise<void> {
  try {
    await db.query(
      `INSERT INTO lei_cache
         (lei, legal_name, jurisdiction, country,
          registration_authority_id, registration_authority_entity_id,
          initial_registration_date, entity_status, last_synced_at)
       VALUES ($1, $2, $3, $4, $5, $6, $7, 'ACTIVE', NOW())
       ON CONFLICT (lei) DO UPDATE SET
         legal_name                       = EXCLUDED.legal_name,
         jurisdiction                     = EXCLUDED.jurisdiction,
         country                          = EXCLUDED.country,
         registration_authority_id        = EXCLUDED.registration_authority_id,
         registration_authority_entity_id = EXCLUDED.registration_authority_entity_id,
         initial_registration_date        = EXCLUDED.initial_registration_date,
         entity_status                    = EXCLUDED.entity_status,
         last_synced_at                   = NOW()`,
      [
        record.lei,
        record.legalName,
        record.jurisdiction,
        record.country,
        record.registrationAuthorityId,
        record.registrationAuthorityEntityId,
        record.initialRegistrationDate,
      ],
    )
  } catch (err) {
    console.error('[lei-cache] write failed:', err)
  }
}
```

**修改 3 — 在 getEntityByKey 中调用 getGleifRecordByLei 的位置插入缓存检查：**

定位 `getEntityByKey` 函数中调用 `getGleifRecordByLei(lei)` 的位置（处理 `gleif:` 前缀的 id）。在调用前插入缓存检查：

```typescript
// 原代码模式（定位用）：
// const gleifRecord = await getGleifRecordByLei(lei)
// const company = await resolveGleifRecord(gleifRecord)

// 修改为：
const cachedLei = await getLeiCacheRecord(lei)
if (cachedLei) {
  // Cache hit: build GleifLeiRecord from cache row
  const gleifRecordFromCache: GleifLeiRecord = {
    lei: cachedLei.lei,
    legalName: cachedLei.legal_name,
    jurisdiction: cachedLei.jurisdiction,
    country: cachedLei.country,
    initialRegistrationDate: cachedLei.initial_registration_date,
    registrationAuthorityId: cachedLei.registration_authority_id,
    registrationAuthorityEntityId: cachedLei.registration_authority_entity_id,
  }
  return resolveGleifRecord(gleifRecordFromCache)
}
// Cache miss: call live API and warm the cache
const gleifRecord = await getGleifRecordByLei(lei)
if (gleifRecord) await writeLeiCacheRecord(gleifRecord)
const company = await resolveGleifRecord(gleifRecord)
```

注意：需读取实际代码找到确切的调用位置，不要修改周围的逻辑。

**约束（repository.ts）：**
- 不删除或修改任何现有的 resolveGleifRecord 逻辑
- 不修改 getGleifRecordByLei（live API 函数保持不变）
- helper 函数使用 try-catch 静默失败（缓存错误不应影响主查询路径）
- `writeLeiCacheRecord` 中仅写 Level 1 字段（不写 direct_parent_lei 等，Level 2 由 syncLeiLevel2 负责）

---

**--- gleif.ts 修改 ---**

读取 gleif.ts 全文后，对 `getGleifUltimateParentJurisdiction` 进行以下精准改写（per D-05）：

**重要：** `getGleifUltimateParentJurisdiction` 在 `gleif.ts` 中（第 249-290 行），由 `trade-service.ts` 通过 named import 调用。必须在 `gleif.ts` 内修改，不能在 trade-service.ts 中包装。

**修改 1 — 在 gleif.ts 顶部 import 区添加 db 和 LeiCacheRow 导入：**

检查 `gleif.ts` 是否已导入 `db`（pool）。如果没有，在 import 区添加：
```typescript
import { pool as db } from '@/lib/server/db'
import type { LeiCacheRow } from '@/lib/server/sync/gleif-golden-copy'
```
注意：查看实际 `gleif.ts` 的 import 路径（可能是 `pool` 而非 `db`），匹配现有命名。

**修改 2 — 在 getGleifUltimateParentJurisdiction 函数开头插入 lei_cache 优先路径：**

找到：
```typescript
export async function getGleifUltimateParentJurisdiction(lei: string): Promise<string | null> {
  try {
    // Step 1: resolve the ultimate-parent relationship to obtain the parent LEI
    const relRes = await fetch(
```

在 `try {` 块的第一行（`const relRes = await fetch(` 之前）插入：

```typescript
    // Cache-first: check lei_cache for ultimate_parent_lei (per D-05)
    // If both the entity and its ultimate parent are in lei_cache, skip live API entirely.
    try {
      const { rows: entityRows } = await db.query<Pick<LeiCacheRow, 'ultimate_parent_lei'>>(
        `SELECT ultimate_parent_lei FROM lei_cache WHERE lei = $1 LIMIT 1`,
        [lei],
      )
      const ultimateParentLei = entityRows[0]?.ultimate_parent_lei
      if (ultimateParentLei && ultimateParentLei !== lei) {
        const { rows: parentRows } = await db.query<Pick<LeiCacheRow, 'jurisdiction'>>(
          `SELECT jurisdiction FROM lei_cache WHERE lei = $1 LIMIT 1`,
          [ultimateParentLei],
        )
        const cachedJurisdiction = parentRows[0]?.jurisdiction
        if (cachedJurisdiction) {
          return cachedJurisdiction  // Cache hit — no live API call
        }
      }
    } catch {
      // Cache lookup failed — fall through to live API
    }
    // Cache miss or incomplete — fall through to live GLEIF API calls below
```

**约束（gleif.ts）：**
- 不修改 `getGleifUltimateParentJurisdiction` 的函数签名（`(lei: string): Promise<string | null>`）
- 原有的两步实时 API 调用（Step 1 + Step 2）保持完整，作为降级路径
- 仅在已有 `try {` 块之内最前面插入缓存检查——不改变错误处理结构
- 如果 lei_cache 查询失败（catch），静默降级到实时 API（不抛出，不返回 null）
  </action>
  <verify>
    <automated>grep -n "getLeiCacheRecord\|writeLeiCacheRecord" /home/hippo/projects/Energy_trade_inspection/src/lib/server/repository.ts | head -20 && grep -n "ultimate_parent_lei\|lei_cache" /home/hippo/projects/Energy_trade_inspection/src/lib/server/gleif.ts | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/server/repository.ts` 包含函数定义 `async function getLeiCacheRecord(`
    - `src/lib/server/repository.ts` 包含函数定义 `async function writeLeiCacheRecord(`
    - `src/lib/server/repository.ts` 包含字符串 `SELECT * FROM lei_cache WHERE lei = $1`
    - `src/lib/server/repository.ts` 包含字符串 `ON CONFLICT (lei) DO UPDATE`（来自 writeLeiCacheRecord）
    - `src/lib/server/repository.ts` 包含字符串 `LeiCacheRow`（import 或使用）
    - `src/lib/server/gleif.ts` 包含字符串 `ultimate_parent_lei`（lei_cache 缓存优先查询）
    - `src/lib/server/gleif.ts` 包含字符串 `Cache-first`（注释，确认插入点正确）
    - `grep -n "ultimate_parent_lei" src/lib/server/gleif.ts` 至少命中 1 行（cache-first 路径已插入）
    - `npm run type-check` 以退出码 0 完成（无 TypeScript 错误）
  </acceptance_criteria>
  <done>repository.ts 的 getLeiCacheRecord、writeLeiCacheRecord helper 以及缓存优先调用点均已插入；gleif.ts 的 getGleifUltimateParentJurisdiction 已添加 lei_cache 优先查找；TypeScript 编译通过。</done>
</task>

<task type="auto">
  <name>Task 2: 在 searchEntities() GLEIF 路径中插入 lei_cache 相似度查询</name>
  <read_first>
    - src/lib/server/repository.ts （第 460-520 行：searchEntities GLEIF 路径完整代码，包含 searchGleifMultiple 调用和结果合并逻辑）
  </read_first>
  <files>src/lib/server/repository.ts</files>
  <action>
在 `searchEntities()` 函数中找到 GLEIF 查询块（`searchGleifMultiple(query, 5)` 调用处），在其前方插入 lei_cache 相似度查询作为快速路径：

**定位标志（在这段代码之前插入）：**
```typescript
// 现有代码（约第 464-470 行）：
const [acraEntities, chEntities, zefixEntities, ocEntities, gleifRecords] = await Promise.all([
  searchACRA(query, 5).catch(() => []),
  searchCompaniesHouse(query, 5).catch(() => []),
  searchZefix(query, 5).catch(() => []),
  searchOpenCorporates(query, 5).catch(() => []),
  searchGleifMultiple(query, 5).catch(() => []),
])
```

**修改方式：**

在 `Promise.all([...])` 块之前插入缓存命中检查。如果 lei_cache 中有结果，将 `gleifRecords` 参数替换为缓存结果，跳过 `searchGleifMultiple` 调用：

```typescript
// Cache-first: try lei_cache similarity search before calling live GLEIF API
let cachedGleifRows: LeiCacheRow[] = []
try {
  const { rows } = await db.query<LeiCacheRow>(
    `SELECT * FROM lei_cache
     WHERE SIMILARITY(legal_name, $1) > 0.45
       AND entity_status = 'ACTIVE'
     ORDER BY SIMILARITY(legal_name, $1) DESC
     LIMIT 5`,
    [query],
  )
  cachedGleifRows = rows
} catch {
  // Ignore cache errors — fall through to live API
}

// Use cache hits if found; otherwise call live GLEIF API
const [acraEntities, chEntities, zefixEntities, ocEntities, gleifRecords] = await Promise.all([
  searchACRA(query, 5).catch(() => []),
  searchCompaniesHouse(query, 5).catch(() => []),
  searchZefix(query, 5).catch(() => []),
  searchOpenCorporates(query, 5).catch(() => []),
  cachedGleifRows.length > 0 ? Promise.resolve([]) : searchGleifMultiple(query, 5).catch(() => []),
])

// Merge cache hits as GleifLeiRecord-compatible objects
const gleifFromCache: GleifLeiRecord[] = cachedGleifRows.map((row) => ({
  lei: row.lei,
  legalName: row.legal_name,
  jurisdiction: row.jurisdiction,
  country: row.country,
  initialRegistrationDate: row.initial_registration_date,
  registrationAuthorityId: row.registration_authority_id,
  registrationAuthorityEntityId: row.registration_authority_entity_id,
}))
const allGleifRecords = [...gleifFromCache, ...gleifRecords]
```

然后将后续代码中使用 `gleifRecords` 的地方替换为 `allGleifRecords`：

- 原：`const gleifResults = gleifRecords.filter(...)` → 改为 `const gleifResults = allGleifRecords.filter(...)`

**约束：**
- 不要修改 ACRA、CompaniesHouse、Zefix、OpenCorporates 的任何查询逻辑
- 不要修改 gleifResults 的 map() 逻辑（结果格式不变）
- 缓存查询失败时静默降级到实时 API（catch 块为空）
- `allGleifRecords` 变量中 cache 结果排在前面（保留排序优先级）
  </action>
  <verify>
    <automated>grep -n "SIMILARITY.*lei_cache\|lei_cache.*SIMILARITY\|cachedGleifRows\|allGleifRecords" /home/hippo/projects/Energy_trade_inspection/src/lib/server/repository.ts | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/server/repository.ts` 包含字符串 `SIMILARITY(legal_name, $1) > 0.45`
    - `src/lib/server/repository.ts` 包含字符串 `cachedGleifRows`（缓存命中变量）
    - `src/lib/server/repository.ts` 包含字符串 `cachedGleifRows.length > 0`（跳过实时 API 的条件）
    - `src/lib/server/repository.ts` 包含字符串 `allGleifRecords`（合并结果变量）
    - `npm run type-check` 以退出码 0 完成
    - `npm run lint` 以退出码 0 完成
  </acceptance_criteria>
  <done>searchEntities() GLEIF 路径已插入 lei_cache 相似度查询作为快速路径，TypeScript 和 lint 均通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户搜索查询 → lei_cache SIMILARITY 查询 | 用户输入经 pg_trgm 相似度函数处理 |
| lei_cache 行 → GleifLeiRecord 对象 | 数据库行转换为业务对象（无额外校验） |
| trade-service.ts → getGleifUltimateParentJurisdiction | 内部服务调用，lei 来自已验证的 GLEIF 记录 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | Tampering | SQL 注入（query 参数 → SIMILARITY 查询） | mitigate | 使用参数化查询 `$1`，不拼接 SQL；pg_trgm SIMILARITY 函数本身不会执行 SQL |
| T-12-03-02 | Denial of Service | SIMILARITY 全表扫描 lei_cache（无缓存预热时） | accept | `lei_cache_legal_name_trgm` GIN 索引（迁移 037 创建）使 SIMILARITY 扫描高效；表为空时查询立即返回 0 行 |
| T-12-03-03 | Information Disclosure | cache miss 写回时 entity_status 硬编码为 'ACTIVE' | accept | 来自 GLEIF live API 的记录在 live API 返回时已是 ACTIVE（否则 GLEIF 不返回）；轻微不精确但不影响安全 |
| T-12-03-04 | Elevation of Privilege | getLeiCacheRecord 返回含 reporting_exception_type 的行 | accept | 该字段仅用于 Plan 04 风险信号注入；数据来自 GLEIF 官方数据，非用户输入 |
| T-12-03-05 | Tampering | gleif.ts db import 引入新的 DB 依赖 | accept | gleif.ts 是服务端模块；db pool 已是全局单例（global in dev）；不存在并发安全问题 |
</threat_model>

<verification>
Wave 3 Plan 03 完成标志：
1. repository.ts 包含 getLeiCacheRecord() 和 writeLeiCacheRecord() helper
2. repository.ts 的 searchEntities GLEIF 路径包含 lei_cache 相似度查询
3. gleif.ts 的 getGleifUltimateParentJurisdiction 在 try 块开头添加 lei_cache 优先路径（查询 ultimate_parent_lei，再查 parent 的 jurisdiction）
4. `grep -n "ultimate_parent_lei" src/lib/server/gleif.ts` 命中至少 1 行
5. `npm run type-check` 通过（0 个 TypeScript 错误）
6. `npm run lint` 通过
</verification>

<success_criteria>
- repository.ts 的 GLEIF 查询路径优先读 lei_cache，命中则不调用 GLEIF 实时 API
- 缓存未命中时调用实时 API 并写回 lei_cache（warm-on-miss）
- searchEntities() 的 lei_cache SIMILARITY 查询使用阈值 0.45
- getGleifUltimateParentJurisdiction() 若 lei_cache 同时有实体行（含 ultimate_parent_lei）和父级行（含 jurisdiction），则不发出任何 HTTP 请求（per D-05）
- TypeScript 编译和 lint 均无错误
</success_criteria>

<output>
After completion, create `.planning/phases/12-gleif-golden-copy-integration-lei-local-cache-ownership-chai/12-03-SUMMARY.md`
</output>
