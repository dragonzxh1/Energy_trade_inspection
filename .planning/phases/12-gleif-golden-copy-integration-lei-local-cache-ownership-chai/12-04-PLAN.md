---
phase: 12-gleif-golden-copy-integration-lei-local-cache-ownership-chai
plan: 04
type: execute
wave: 3
depends_on:
  - plan-03
files_modified:
  - src/lib/server/scoring.ts
  - src/lib/server/repository.ts
  - src/lib/server/gleif.ts
autonomous: true
requirements:
  - D-07

must_haves:
  truths:
    - "ScoringInputs 接口包含可选字段 reportingExceptionFlag: boolean"
    - "scoreCompany() 在 reportingExceptionFlag 为 true 时从 communityReputation 扣除 3 分（最小值 0）"
    - "repository.ts 在 getLeiCacheRecord 命中且 reporting_exception_type 为 NON_CONSOLIDATING / NON_PUBLIC / NO_LEI 时，向 company.riskFlags 添加一个 RiskFlag 对象"
    - "NATURAL_PERSONS 例外类型不产生 riskFlag，不触发评分扣分"
    - "buildGleifCompany() 在 opacity exception 时直接对返回对象的 communityReputation 分数减 3（不依赖 computeScore 路径）"
  artifacts:
    - path: "src/lib/server/scoring.ts"
      provides: "扩展后的 ScoringInputs 接口 + communityReputation 扣分逻辑"
      contains: "reportingExceptionFlag"
    - path: "src/lib/server/repository.ts"
      provides: "reporting_exception 风险标志注入（in-memory RiskFlag，不持久化到 risk_flags 表）"
      contains: "reporting_exception"
    - path: "src/lib/server/gleif.ts"
      provides: "buildGleifCompany() 直接对 communityReputation 和 authenticityScore 应用 3 分扣减（opacity exception 路径）"
      contains: "OPACITY_EXCEPTION_TYPES"
  key_links:
    - from: "repository.ts getLeiCacheRecord"
      to: "scoring.ts ScoringInputs.reportingExceptionFlag"
      via: "reporting_exception_type 读自 lei_cache 行，opaque 类型时 → computeScore({ reportingExceptionFlag: true })"
      pattern: "reportingExceptionFlag"
    - from: "scoring.ts scoreCompany()"
      to: "communityReputation 维度"
      via: "Math.max(0, C - 3) when reportingExceptionFlag is true"
      pattern: "Math.max.*C - 3"
    - from: "gleif.ts buildGleifCompany()"
      to: "返回对象 communityReputation.score + authenticityScore"
      via: "直接减 3，不经过 computeScore()（因 buildGleifCompany 使用硬编码内联评分）"
      pattern: "OPACITY_EXCEPTION_TYPES"
---

<objective>
将 GLEIF 报告豁免（Reporting Exception）数据转化为可操作的风险信号：在 scoring.ts 中添加 3 分扣除；在 repository.ts 中注入 RiskFlag 对象到公司实体的 riskFlags 数组；在 gleif.ts 的 buildGleifCompany() 中直接对返回对象执行 3 分扣减（因该函数绕过 computeScore 使用硬编码内联评分）。

Purpose: 隐瞒所有权结构的实体（opacity 类型豁免）应在 ETI 的风险评分中被降级，并在实体页面以风险标志徽章形式展示。buildGleifCompany() 在 gleif.ts 中使用硬编码内联分数（communityReputation 直接赋值），不经过 computeScore()——因此 scoring.ts 的扣分逻辑永远不会被 GLEIF 实体触发，必须在 buildGleifCompany() 内直接应用扣减。
Output: 更新的 scoring.ts（ScoringInputs 扩展 + scoreCompany 逻辑）、更新的 repository.ts（风险标志注入）、更新的 gleif.ts（buildGleifCompany 直接扣分）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-gleif-golden-copy-integration-lei-local-cache-ownership-chai/12-CONTEXT.md
@.planning/phases/12-gleif-golden-copy-integration-lei-local-cache-ownership-chai/12-PATTERNS.md
@.planning/phases/12-gleif-golden-copy-integration-lei-local-cache-ownership-chai/12-03-SUMMARY.md

<interfaces>
<!-- 当前 ScoringInputs 接口（scoring.ts 第 31-47 行）-->
```typescript
export interface ScoringInputs {
  entityType:         'company' | 'vessel' | 'terminal'
  sanctionStatus:     'not_listed' | 'listed' | 'unknown'
  fraudAlertCount?:   number
  whitelisted?:       boolean
  country:            string
  registrationNumber: string | null
  imo?:               string | null
  aisData?:           VesselAisData | null
  intelligence?:      IntelligenceSnapshot | null
  domainAgeDays?: number | null
  hasWebPresence?: boolean | null
}
```

<!-- 当前 scoreCompany communityReputation 块（scoring.ts 第 162-174 行）-->
```typescript
// Community Reputation (max 10)
const sanctionHits = intelligence?.sanctions_hits?.length ?? 0
const fraudHits    = fraudAlertCount ?? 0
if (fraudHits > 0) {
  C = 0
} else {
  C += sanctionHits === 0 ? 6 : 0
  C += riskHits === 0 ? 4 : 2
  if (whitelisted) C = Math.min(10, C + 2)
}
// 此后需要插入 reportingExceptionFlag 扣分
```

<!-- RiskFlag 接口（src/lib/types.ts）-->
```typescript
export interface RiskFlag {
  id: string
  category: string
  severity: RiskLevel    // 'low' | 'medium' | 'high' | 'critical'
  submittedAt: string    // ISO 8601
  status: 'pending' | 'verified'
}
```

<!-- buildGleifCompany() 当前实现（gleif.ts 第 195-247 行）-->
<!-- 关键点：该函数使用硬编码内联分数，不调用 computeScore() -->
<!-- communityReputation 直接赋值：sanctionStatus === 'not_listed' ? 8 : 0 -->
<!-- authenticityScore = entityExistence + documentConsistency + communityReputation -->
<!-- 因此 scoring.ts 的 reportingExceptionFlag 扣分永远不会触达 GLEIF 实体 -->
<!-- 必须在 buildGleifCompany() 内直接对返回对象应用扣减 -->
```typescript
export function buildGleifCompany(record: GleifLeiRecord, sanctionStatus: SanctionStatus) {
  // ...
  const communityReputation = sanctionStatus === 'not_listed' ? 8 : 0
  const authenticityScore   = entityExistence + documentConsistency + communityReputation
  return {
    ...base,
    authenticityScore,
    scoreBreakdown: {
      // ...
      communityReputation: { score: communityReputation, maxScore: 10 },
    },
    riskLevel: 'medium' as const,
  }
}
```

<!-- PATTERNS.md 确认的风险标志构建方式 -->
```typescript
const OPACITY_EXCEPTION_TYPES = new Set(['NON_CONSOLIDATING', 'NON_PUBLIC', 'NO_LEI'])

const exceptionFlag: RiskFlag = {
  id: `gleif-exception-${exceptionType.toLowerCase()}`,
  category: 'reporting_exception',
  severity: 'medium',
  status: 'verified',
  submittedAt: new Date().toISOString(),
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 scoring.ts ScoringInputs + communityReputation 扣分（per D-07）</name>
  <read_first>
    - src/lib/server/scoring.ts （必须读取全文：第 31-47 行 ScoringInputs 接口、第 122-183 行 scoreCompany 函数、第 162-174 行 communityReputation 块）
  </read_first>
  <files>src/lib/server/scoring.ts</files>
  <action>
读取 scoring.ts 全文后，进行以下两处精准修改：

**修改 1 — 在 ScoringInputs 接口末尾添加 reportingExceptionFlag 字段：**

在 `hasWebPresence?: boolean | null` 行之后，在接口闭合括号 `}` 之前，添加：
```typescript
  /**
   * True when the entity has an opacity-indicating GLEIF Reporting Exception
   * (NON_CONSOLIDATING, NON_PUBLIC, or NO_LEI). Deducts 3 pts from communityReputation.
   * Not set for NATURAL_PERSONS exception (informational only).
   */
  reportingExceptionFlag?: boolean
```

**修改 2 — 在 scoreCompany 的 communityReputation 块末尾添加扣分逻辑：**

找到 `if (whitelisted) C = Math.min(10, C + 2)` 这一行（在 `else` 块内）。在整个 if/else 块之后，`return {` 之前，插入：

```typescript
  // GLEIF Reporting Exception: opacity-indicating types reduce trust signal (per D-07)
  if (inputs.reportingExceptionFlag) {
    C = Math.max(0, C - 3)
  }
```

**具体插入位置规则：**
1. 必须在 `if (fraudHits > 0) { C = 0 } else { ... }` 块之后
2. 必须在 `return { E: clamp(E, 25), ... }` 之前
3. 使用 `Math.max(0, C - 3)` 防止负值

**约束：**
- 不修改 scoreVessel、scoreTerminal 函数（只有公司实体有 reporting exception）
- 不修改 computeScore 公共函数的签名
- `reportingExceptionFlag` 的扣分在 if/else 块之外，安全处理 fraudHits > 0 导致 C=0 的边界情况（`Math.max(0, 0-3) = 0`，不产生负数）
  </action>
  <verify>
    <automated>grep -n "reportingExceptionFlag\|Math.max.*C - 3" /home/hippo/projects/Energy_trade_inspection/src/lib/server/scoring.ts | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/server/scoring.ts` 包含字符串 `reportingExceptionFlag?: boolean`（ScoringInputs 接口中）
    - `src/lib/server/scoring.ts` 包含字符串 `reportingExceptionFlag`（在 scoreCompany 函数体中引用）
    - `src/lib/server/scoring.ts` 包含字符串 `Math.max(0, C - 3)` 或 `C = Math.max(0, C - 3)`
    - `npm run type-check` 以退出码 0 完成
  </acceptance_criteria>
  <done>ScoringInputs 添加了 reportingExceptionFlag，scoreCompany 在该标志为 true 时扣除 3 分。</done>
</task>

<task type="auto">
  <name>Task 2: 在 gleif.ts buildGleifCompany() 中直接应用扣分；在 repository.ts 中注入 reporting_exception RiskFlag（per D-07）</name>
  <read_first>
    - src/lib/server/gleif.ts （第 195-247 行：buildGleifCompany 完整实现——注意该函数使用硬编码内联评分，不调用 computeScore，scoring.ts 的扣分无法触达此路径）
    - src/lib/server/repository.ts （第 696-713 行：resolveGleifRecord 的 fallback buildGleifCompany 路径；Plan 03 新增的缓存命中块；riskFlags 字段位置）
    - src/lib/types.ts （确认 RiskFlag 接口的确切字段：id、category、severity、submittedAt、status）
  </read_first>
  <files>src/lib/server/gleif.ts, src/lib/server/repository.ts</files>
  <action>
**--- 修改 gleif.ts：buildGleifCompany() 直接扣分 ---**

读取 gleif.ts 后，对 `buildGleifCompany` 函数进行以下修改：

**背景：** `buildGleifCompany` 使用硬编码内联分数（`communityReputation = sanctionStatus === 'not_listed' ? 8 : 0`），不调用 `computeScore()`。因此 scoring.ts 的 `reportingExceptionFlag` 路径对 GLEIF 实体无效。必须在此函数内直接对返回对象应用扣减。

**修改 1 — 添加函数签名的第三个可选参数：**

将：
```typescript
export function buildGleifCompany(record: GleifLeiRecord, sanctionStatus: SanctionStatus) {
```

改为：
```typescript
const OPACITY_EXCEPTION_TYPES = new Set(['NON_CONSOLIDATING', 'NON_PUBLIC', 'NO_LEI'])

export function buildGleifCompany(
  record: GleifLeiRecord,
  sanctionStatus: SanctionStatus,
  reportingExceptionType?: string | null,
) {
```

注意：将 `OPACITY_EXCEPTION_TYPES` 常量定义在函数之前（模块级），方便后续在 repository.ts 中也引用同名常量。

**修改 2 — 在 non-listed 分支的 return 语句前插入扣分逻辑：**

找到函数末尾（非 listed 分支）的：
```typescript
  const communityReputation = sanctionStatus === 'not_listed' ? 8 : 0
  const authenticityScore   = entityExistence + documentConsistency + communityReputation

  return {
    ...base,
    authenticityScore,
    scoreBreakdown: {
      entityExistence:     { score: entityExistence,     maxScore: 25 },
      assetReality:        { score: 0,                   maxScore: 30 },
      tradingTrackRecord:  { score: 0,                   maxScore: 25 },
      documentConsistency: { score: documentConsistency, maxScore: 10 },
      communityReputation: { score: communityReputation, maxScore: 10 },
    },
    riskLevel: 'medium' as const,
  }
```

修改为：
```typescript
  const communityReputation = sanctionStatus === 'not_listed' ? 8 : 0
  // GLEIF Reporting Exception: opacity-indicating types reduce trust signal (per D-07)
  // buildGleifCompany uses inline scoring (not computeScore), so deduction is applied here directly.
  const hasOpacityException =
    reportingExceptionType != null && OPACITY_EXCEPTION_TYPES.has(reportingExceptionType)
  const communityReputationFinal = hasOpacityException
    ? Math.max(0, communityReputation - 3)
    : communityReputation
  const authenticityScore = entityExistence + documentConsistency + communityReputationFinal

  return {
    ...base,
    authenticityScore,
    scoreBreakdown: {
      entityExistence:     { score: entityExistence,          maxScore: 25 },
      assetReality:        { score: 0,                        maxScore: 30 },
      tradingTrackRecord:  { score: 0,                        maxScore: 25 },
      documentConsistency: { score: documentConsistency,      maxScore: 10 },
      communityReputation: { score: communityReputationFinal, maxScore: 10 },
    },
    riskLevel: 'medium' as const,
  }
```

**约束（gleif.ts）：**
- 不修改 `listed` 分支的硬编码分数（该分支已经返回固定值 7）
- `OPACITY_EXCEPTION_TYPES` 定义为模块级常量（不在函数内部）
- `reportingExceptionType` 是可选参数，默认值等效于 null（无豁免）——所有现有调用方不传第三参数时行为不变

---

**--- 修改 repository.ts：注入 RiskFlag ---**

读取 repository.ts 后，在以下两处注入 reporting_exception 风险标志：

**准备工作 — 模块级常量（仅在 repository.ts 中未定义时添加）：**

在 helper 函数区域（getLeiCacheRecord/writeLeiCacheRecord 附近）添加：
```typescript
const OPACITY_EXCEPTION_TYPES = new Set(['NON_CONSOLIDATING', 'NON_PUBLIC', 'NO_LEI'])
```

**注入点 A — getLeiCacheRecord 缓存命中路径（Plan 03 中新增的代码块）：**

找到 Plan 03 中新增的代码块：
```typescript
if (cachedLei) {
  const gleifRecordFromCache: GleifLeiRecord = { ... }
  return resolveGleifRecord(gleifRecordFromCache)
}
```

修改为（将 `reportingExceptionType` 传给 `buildGleifCompany`，并在 company 返回前追加 RiskFlag）：
```typescript
if (cachedLei) {
  const gleifRecordFromCache: GleifLeiRecord = {
    lei: cachedLei.lei,
    legalName: cachedLei.legal_name,
    jurisdiction: cachedLei.jurisdiction,
    country: cachedLei.country,
    initialRegistrationDate: cachedLei.initial_registration_date,
    registrationAuthorityId: cachedLei.registration_authority_id,
    registrationAuthorityEntityId: cachedLei.registration_authority_entity_id,
  }
  const company = await resolveGleifRecord(gleifRecordFromCache)
  if (company && cachedLei.reporting_exception_type &&
      OPACITY_EXCEPTION_TYPES.has(cachedLei.reporting_exception_type)) {
    company.riskFlags = [
      ...(company.riskFlags ?? []),
      {
        id: `gleif-exception-${cachedLei.reporting_exception_type.toLowerCase()}`,
        category: 'reporting_exception',
        severity: 'medium' as const,
        status: 'verified' as const,
        submittedAt: new Date().toISOString(),
      },
    ]
  }
  return company
}
```

注意：`resolveGleifRecord` 最终调用 `buildGleifCompany`。由于 gleif.ts 的 `buildGleifCompany` 现在接受第三个参数，但 `resolveGleifRecord` 的调用路径不直接传 `reportingExceptionType`，因此 **RiskFlag 注入在 repository.ts 的缓存命中块中完成**（在 company 对象上追加），而评分扣减通过 Plan 03 改造的缓存命中路径+第三参数传递实现。

具体地：在缓存命中路径中，找到 repository.ts 调用 `buildGleifCompany` 的位置（通过 `resolveGleifRecord` 间接调用），或直接在返回 company 前修改已构建对象的 `scoreBreakdown.communityReputation.score` 和 `authenticityScore`：

```typescript
  // Apply scoring deduction directly on the returned company object
  // (buildGleifCompany now handles this via reportingExceptionType param,
  //  but resolveGleifRecord doesn't pass it — so apply here as fallback)
  if (company && cachedLei.reporting_exception_type &&
      OPACITY_EXCEPTION_TYPES.has(cachedLei.reporting_exception_type)) {
    const crScore = company.scoreBreakdown?.communityReputation?.score ?? 0
    const deduction = Math.min(3, crScore)
    if (deduction > 0) {
      company.scoreBreakdown.communityReputation.score = crScore - deduction
      company.authenticityScore = Math.max(0, company.authenticityScore - deduction)
    }
  }
```

**注入点 B — warm-on-miss 路径（cache miss 后查询 lei_cache 获取 exception）：**

在 cache miss 路径的 `return company` 之前，读取刚写回的缓存行（可能已有 exception 数据来自 Level 2 sync）：
```typescript
const freshCached = await getLeiCacheRecord(gleifRecord.lei)
if (company && freshCached?.reporting_exception_type &&
    OPACITY_EXCEPTION_TYPES.has(freshCached.reporting_exception_type)) {
  company.riskFlags = [
    ...(company.riskFlags ?? []),
    {
      id: `gleif-exception-${freshCached.reporting_exception_type.toLowerCase()}`,
      category: 'reporting_exception',
      severity: 'medium' as const,
      status: 'verified' as const,
      submittedAt: new Date().toISOString(),
    },
  ]
  // Apply score deduction on warm-on-miss path as well
  const crScore = company.scoreBreakdown?.communityReputation?.score ?? 0
  const deduction = Math.min(3, crScore)
  if (deduction > 0) {
    company.scoreBreakdown.communityReputation.score = crScore - deduction
    company.authenticityScore = Math.max(0, company.authenticityScore - deduction)
  }
}
return company
```

**约束（repository.ts）：**
- `RiskFlag` 对象不持久化到 `risk_flags` 表（per PATTERNS.md：该表是用户提交的社区标志）
- `NATURAL_PERSONS` 类型不在 `OPACITY_EXCEPTION_TYPES` 集合中（per D-07）
- `severity: 'medium' as const` 和 `status: 'verified' as const` 是必需的（TypeScript 字面量类型）
- `company.riskFlags` 可能为 undefined，使用 `?? []` 防卫
  </action>
  <verify>
    <automated>grep -n "OPACITY_EXCEPTION_TYPES\|reporting_exception\|communityReputationFinal" /home/hippo/projects/Energy_trade_inspection/src/lib/server/gleif.ts | head -10 && grep -n "OPACITY_EXCEPTION_TYPES\|reporting_exception\|NON_CONSOLIDATING" /home/hippo/projects/Energy_trade_inspection/src/lib/server/repository.ts | head -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/server/gleif.ts` 包含字符串 `OPACITY_EXCEPTION_TYPES`（模块级常量定义）
    - `src/lib/server/gleif.ts` 包含字符串 `communityReputationFinal`（扣减后的最终分数变量）
    - `src/lib/server/gleif.ts` 包含字符串 `reportingExceptionType`（buildGleifCompany 第三参数）
    - `src/lib/server/gleif.ts` 中 `buildGleifCompany` 的 `communityReputation` 分数通过 `Math.max(0, communityReputation - 3)` 扣减
    - `src/lib/server/repository.ts` 包含字符串 `'reporting_exception'`（RiskFlag category 值）
    - `src/lib/server/repository.ts` 包含字符串 `OPACITY_EXCEPTION_TYPES`（集合常量）
    - `src/lib/server/repository.ts` 包含字符串 `NON_CONSOLIDATING`（opacity 类型之一，在集合定义中）
    - `src/lib/server/repository.ts` 包含字符串 `gleif-exception-`（RiskFlag id 前缀）
    - `src/lib/server/repository.ts` 不包含 `'NATURAL_PERSONS'` 在 OPACITY_EXCEPTION_TYPES 集合中
    - `grep "NATURAL_PERSONS" src/lib/server/gleif.ts` 不命中（NATURAL_PERSONS 不在 OPACITY_EXCEPTION_TYPES 中）
    - `npm run type-check` 以退出码 0 完成
    - `npm run lint` 以退出码 0 完成
  </acceptance_criteria>
  <done>gleif.ts buildGleifCompany() 直接对 communityReputation 和 authenticityScore 应用 3 分扣减（opacity exception 时）；repository.ts 在缓存命中和 warm-on-miss 路径均注入 reporting_exception RiskFlag 并同步评分；NATURAL_PERSONS 被排除在外。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lei_cache.reporting_exception_type → RiskFlag.category | 数据库字段值直接成为风险标志属性 |
| RiskFlag → 实体页面展示 | 风险标志影响用户看到的风险评级 |
| gleif.ts buildGleifCompany() → authenticityScore | 直接修改最终评分，绕过 computeScore 路径 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04-01 | Tampering | reporting_exception_type 值被恶意篡改到 lei_cache | accept | lei_cache 只由 GLEIF 官方数据填充（syncLeiExceptions），不接受用户输入；DB 不对外暴露直接写接口 |
| T-12-04-02 | Tampering | OPACITY_EXCEPTION_TYPES 集合定义不正确导致误判 | mitigate | 集合在代码中硬编码（不从配置读取）；NATURAL_PERSONS 明确排除在集合外；代码审查可验证 |
| T-12-04-03 | Denial of Service | riskFlags 数组无限增长 | accept | 每个实体最多一个 reporting_exception 标志（gleif-exception-{type} id 唯一）；实际只命中一次 opacity 类型 |
| T-12-04-04 | Information Disclosure | RiskFlag 暴露 GLEIF 内部 exception reason | accept | ExceptionCategory（type）是 GLEIF 公开字段；ExceptionReason 未暴露到 RiskFlag（仅存于 lei_cache 表）|
| T-12-04-05 | Tampering | buildGleifCompany 第三参数由调用方控制 | accept | 调用方仅限 repository.ts（服务端内部）；值来自 lei_cache，不来自用户输入 |
</threat_model>

<verification>
Wave 3 Plan 04 完成标志：
1. scoring.ts ScoringInputs 包含 reportingExceptionFlag?: boolean
2. scoring.ts scoreCompany 在 reportingExceptionFlag 为 true 时执行 Math.max(0, C - 3)
3. gleif.ts 模块级 OPACITY_EXCEPTION_TYPES 常量定义，包含 NON_CONSOLIDATING / NON_PUBLIC / NO_LEI
4. gleif.ts buildGleifCompany 接受第三参数 reportingExceptionType，直接对 communityReputation 和 authenticityScore 扣减
5. repository.ts 在缓存命中路径注入 reporting_exception RiskFlag
6. repository.ts OPACITY_EXCEPTION_TYPES 集合不含 NATURAL_PERSONS
7. npm run type-check 通过
8. npm run lint 通过
</verification>

<success_criteria>
- 实体 LEI 在 lei_cache 中有 reporting_exception_type = 'NON_PUBLIC' 时，返回的 company 对象的 riskFlags 含一个 category='reporting_exception' 的标志
- NATURAL_PERSONS 类型不产生任何风险标志
- 该风险标志不写入 risk_flags 表（仅在内存中构建）
- buildGleifCompany(record, 'not_listed', 'NON_PUBLIC') 返回的 communityReputation.score 为 5（8 - 3），authenticityScore 比不传第三参数时低 3 分
- computeScore({ entityType: 'company', reportingExceptionFlag: true, ... }) 的 communityReputation 分数比不传该标志时低 3 分（scoring.ts 路径）
- TypeScript 和 lint 均无错误
</success_criteria>

<output>
After completion, create `.planning/phases/12-gleif-golden-copy-integration-lei-local-cache-ownership-chai/12-04-SUMMARY.md`
</output>
