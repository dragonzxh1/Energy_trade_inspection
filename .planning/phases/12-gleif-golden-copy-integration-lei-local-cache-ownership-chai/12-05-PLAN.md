---
phase: 12-gleif-golden-copy-integration-lei-local-cache-ownership-chai
plan: 05
type: execute
wave: 4
depends_on:
  - plan-04
files_modified:
  - src/lib/server/sync/index.ts
  - src/app/api/admin/sync/route.ts
  - src/app/api/cron/gleif-delta/route.ts
autonomous: true
requirements:
  - D-09
  - D-10

must_haves:
  truths:
    - "POST /api/admin/sync { source: 'gleif:full' } 触发 sync-gleif-full.mjs 子进程并返回 { pid, message }"
    - "POST /api/admin/sync { source: 'gleif:delta' } 在进程内调用 runSync('gleif:delta') 并返回结果"
    - "GET /api/cron/gleif-delta 无 Bearer token 时返回 401"
    - "GET /api/cron/gleif-delta 携带正确 Bearer token 时调用 syncLeiDelta + syncLeiLevel2 + syncLeiExceptions"
    - "runSync() 中新增 gleif:delta / gleif:level2 / gleif:exceptions dispatch 块"
  artifacts:
    - path: "src/app/api/cron/gleif-delta/route.ts"
      provides: "每日 delta cron 路由，Bearer 认证 + 三个 sync 函数调用"
      exports: ["GET"]
    - path: "src/app/api/admin/sync/route.ts"
      provides: "gleif:full → child process，gleif:delta/level2/exceptions → runSync() in-process"
      contains: "sync-gleif-full.mjs"
    - path: "src/lib/server/sync/index.ts"
      provides: "gleif delta/level2/exceptions 的 runSync() dispatch 块"
      contains: "gleif:delta"
  key_links:
    - from: "/api/admin/sync POST"
      to: "scripts/sync-gleif-full.mjs"
      via: "child_process.spawn() with detached: true"
      pattern: "sync-gleif-full.mjs"
    - from: "/api/cron/gleif-delta GET"
      to: "syncLeiDelta + syncLeiLevel2 + syncLeiExceptions"
      via: "import from @/lib/server/sync/gleif-golden-copy"
      pattern: "syncLeiDelta\\|syncLeiLevel2\\|syncLeiExceptions"
---

<objective>
完成 Phase 12 的触发层：将 GLEIF sync 函数连接到 admin sync 路由和每日 cron 路由，使运维人员可以手动触发全量/delta 同步，调度器可以每日自动触发 delta 同步。

Purpose: 数据层（Plan 02-04）已就绪，但没有触发路径数据就不会流入。这是 Phase 12 最后一块拼图。
Output: src/app/api/cron/gleif-delta/route.ts（新建）、更新的 admin/sync/route.ts、更新的 sync/index.ts dispatch 块。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-gleif-golden-copy-integration-lei-local-cache-ownership-chai/12-CONTEXT.md
@.planning/phases/12-gleif-golden-copy-integration-lei-local-cache-ownership-chai/12-PATTERNS.md
@.planning/phases/12-gleif-golden-copy-integration-lei-local-cache-ownership-chai/12-04-SUMMARY.md

<interfaces>
<!-- cron/cleanup/route.ts 的完整 isAuthorized + GET 模式（逐字复用）-->
```typescript
// 源文件：src/app/api/cron/cleanup/route.ts

export const runtime = 'nodejs'

function isAuthorized(req: NextRequest): boolean {
  const secret = process.env.ADMIN_SECRET ?? process.env.SYNC_SECRET
  if (!secret) return false
  const auth = req.headers.get('authorization') ?? ''
  return auth === `Bearer ${secret}`
}

export async function GET(req: NextRequest) {
  if (!isAuthorized(req)) {
    return NextResponse.json({ error: 'Unauthorized.' }, { status: 401 })
  }
  // ... do work ...
  return NextResponse.json({ ok: true, ... })
}
```

<!-- admin/sync/route.ts 的 child process spawn 模式（逐字复用）-->
```typescript
// 源文件：src/app/api/admin/sync/route.ts 第 111-129 行

if (source === 'opensanctions') {
  const scriptPath = path.join(process.cwd(), 'scripts', 'sync-opensanctions.mjs')
  const child = spawn(process.execPath, [scriptPath], {
    detached: true,
    stdio: 'ignore',
    env: { ...process.env, DATABASE_URL: process.env.DATABASE_URL },
  })
  child.unref()
  return NextResponse.json({
    success: true,
    source: 'opensanctions',
    pid: child.pid,
    message: 'OpenSanctions sync started in the background. Check GET /api/admin/sync for progress.',
  })
}
```

<!-- sync/index.ts 的 runSync dispatch 块模式 -->
```typescript
// 源文件：src/lib/server/sync/index.ts 第 24-42 行

if (source === 'ofac' || source === 'all') {
  const start = Date.now()
  try {
    const { count } = await syncOFAC()
    results.push({ source: 'ofac', success: true, count, durationMs: Date.now() - start })
  } catch (err) {
    results.push({ source: 'ofac', success: false, error: String(err), durationMs: Date.now() - start })
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 sync/index.ts — 添加 gleif delta/level2/exceptions dispatch 块</name>
  <read_first>
    - src/lib/server/sync/index.ts （必须读取全文：确认当前 SyncSource 已含 gleif 子源，以及 runSync 函数末尾位置）
    - src/lib/server/sync/gleif-golden-copy.ts （确认 syncLeiDelta / syncLeiLevel2 / syncLeiExceptions 的导出名称和返回类型 Promise<{ count: number }>）
  </read_first>
  <files>src/lib/server/sync/index.ts</files>
  <action>
读取 `src/lib/server/sync/index.ts` 全文后，进行以下修改：

**修改 1 — 在文件顶部的 import 区域添加 gleif-golden-copy 导入：**

在现有 `import { syncRegulatoryWarnings } from './regulatory-warnings'` 行之后添加：
```typescript
import { syncLeiDelta, syncLeiLevel2, syncLeiExceptions } from './gleif-golden-copy'
```

注意：`syncLeiFull` 不在此处导入——它由 `admin/sync/route.ts` 直接通过 child process spawn 触发，不经过 `runSync()`。

**修改 2 — 在 `runSync()` 函数中，在 `return results` 之前添加 gleif dispatch 块：**

找到 `return results` 行（当前函数的最后一行），在其前方插入：

```typescript
  if (source === 'gleif:delta') {
    const start = Date.now()
    try {
      const { count } = await syncLeiDelta()
      results.push({ source: 'gleif:delta', success: true, count, durationMs: Date.now() - start })
    } catch (err) {
      results.push({ source: 'gleif:delta', success: false, error: String(err), durationMs: Date.now() - start })
    }
  }

  if (source === 'gleif:level2') {
    const start = Date.now()
    try {
      const { count } = await syncLeiLevel2()
      results.push({ source: 'gleif:level2', success: true, count, durationMs: Date.now() - start })
    } catch (err) {
      results.push({ source: 'gleif:level2', success: false, error: String(err), durationMs: Date.now() - start })
    }
  }

  if (source === 'gleif:exceptions') {
    const start = Date.now()
    try {
      const { count } = await syncLeiExceptions()
      results.push({ source: 'gleif:exceptions', success: true, count, durationMs: Date.now() - start })
    } catch (err) {
      results.push({ source: 'gleif:exceptions', success: false, error: String(err), durationMs: Date.now() - start })
    }
  }
```

**约束：**
- `gleif:full` **不**添加到 runSync dispatch（全量导入通过 child process 触发，不在 runSync 内）
- `gleif` (不带子源) **不**添加到 runSync dispatch（无明确的单一 gleif 动作）
- 不修改现有 ofac/fraud/legitdomains/warninglists 的任何 dispatch 块
- 不修改 `getSyncStatus()` 函数（gleif sync log 已写入 sanctions_sync_log，会自动出现在状态列表）
  </action>
  <verify>
    <automated>grep -n "syncLeiDelta\|syncLeiLevel2\|syncLeiExceptions\|gleif:delta" src/lib/server/sync/index.ts | head -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/server/sync/index.ts` 包含 `import { syncLeiDelta, syncLeiLevel2, syncLeiExceptions } from './gleif-golden-copy'`
    - `src/lib/server/sync/index.ts` 包含 `source === 'gleif:delta'`
    - `src/lib/server/sync/index.ts` 包含 `source === 'gleif:level2'`
    - `src/lib/server/sync/index.ts` 包含 `source === 'gleif:exceptions'`
    - `src/lib/server/sync/index.ts` 不包含 `syncLeiFull` 导入（全量 sync 不通过 runSync 调用）
    - `npm run type-check` 以退出码 0 完成
  </acceptance_criteria>
  <done>runSync() 新增 gleif:delta / gleif:level2 / gleif:exceptions 三个 dispatch 块。</done>
</task>

<task type="auto">
  <name>Task 2: 更新 admin/sync/route.ts — 添加 gleif:full child process + gleif:delta in-process dispatch</name>
  <read_first>
    - src/app/api/admin/sync/route.ts （必须读取全文：第 100-180 行 POST handler，了解 source 路由逻辑和现有 if-else 结构）
  </read_first>
  <files>src/app/api/admin/sync/route.ts</files>
  <action>
读取 `src/app/api/admin/sync/route.ts` 全文后，在 POST handler 中添加 GLEIF dispatch 逻辑。

**定位插入点：** 找到 `if (source === 'opensanctions') { ... }` 块结束的 `}` 之后（即返回 opensanctions 结果后的位置），在 `if (source === 'fraud') {` 行之前插入以下代码：

```typescript
  // GLEIF full import: spawn background process (875 MB download, hours to complete)
  if (source === 'gleif:full') {
    const scriptPath = path.join(process.cwd(), 'scripts', 'sync-gleif-full.mjs')
    const child = spawn(process.execPath, [scriptPath], {
      detached: true,
      stdio: 'ignore',
      env: {
        ...process.env,
        DATABASE_URL: process.env.DATABASE_URL,
      },
    })
    child.unref()

    return NextResponse.json({
      success: true,
      source: 'gleif:full',
      pid: child.pid,
      message: 'GLEIF full Level 1 import started in background. Check GET /api/admin/sync for progress via sanctions_sync_log.',
    })
  }

  // GLEIF delta syncs: run in-process (3-58 MB, completes within maxDuration)
  if (source === 'gleif:delta' || source === 'gleif:level2' || source === 'gleif:exceptions' || source === 'gleif') {
    const gleifSource = source === 'gleif' ? 'gleif:delta' : source as SyncSource
    try {
      const results = await runSync(gleifSource)
      const hasError = results.some((r) => !r.success)
      return NextResponse.json({ results }, { status: hasError ? 207 : 200 })
    } catch (error: unknown) {
      const message = error instanceof Error ? error.message : String(error)
      return NextResponse.json({ error: message }, { status: 500 })
    }
  }
```

**约束：**
- `spawn` 的参数与 `opensanctions` 块保持完全一致（`detached: true, stdio: 'ignore', env: { ...process.env, DATABASE_URL }`）
- `child.unref()` 必须在 `spawn` 之后立即调用
- `source === 'gleif'` 时作为 `'gleif:delta'` 处理（最常用的触发动作）
- 不修改现有的 opensanctions / fraud / warninglists 处理块
- `SyncSource` 类型转换使用 `source as SyncSource`（类型系统已更新）
  </action>
  <verify>
    <automated>grep -n "gleif:full\|sync-gleif-full\|gleif:delta\|gleif:level2\|gleif:exceptions" src/app/api/admin/sync/route.ts | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/admin/sync/route.ts` 包含字符串 `sync-gleif-full.mjs`
    - `src/app/api/admin/sync/route.ts` 包含字符串 `source === 'gleif:full'`
    - `src/app/api/admin/sync/route.ts` 包含字符串 `gleif:delta`（delta in-process dispatch）
    - `src/app/api/admin/sync/route.ts` 包含字符串 `child.unref()` （紧随 gleif:full spawn 之后）
    - `src/app/api/admin/sync/route.ts` 包含字符串 `GLEIF full Level 1 import started in background`（响应消息）
    - `npm run type-check` 以退出码 0 完成
  </acceptance_criteria>
  <done>admin/sync/route.ts 添加了 gleif:full child process spawn 和 gleif:delta in-process dispatch。</done>
</task>

<task type="auto">
  <name>Task 3: 创建 src/app/api/cron/gleif-delta/route.ts（每日 delta cron 路由）</name>
  <read_first>
    - src/app/api/cron/cleanup/route.ts （必须读取全文：isAuthorized 函数、runtime 声明、GET handler 结构——完全复用）
    - src/lib/server/sync/gleif-golden-copy.ts （确认三个 sync 函数的返回类型：Promise<{ count: number }>）
  </read_first>
  <files>src/app/api/cron/gleif-delta/route.ts</files>
  <action>
创建新目录 `src/app/api/cron/gleif-delta/` 和文件 `route.ts`。

文件内容基于 `cleanup/route.ts` 的结构，完全复用 `isAuthorized()` 实现（逐字复制，不修改）：

```typescript
/**
 * GET /api/cron/gleif-delta
 *
 * Daily GLEIF Golden Copy delta sync:
 *   1. syncLeiDelta()      — Level 1 LEI2 delta (~3 MB)
 *   2. syncLeiLevel2()     — Level 2 RR delta (ownership chain, ~32 MB)
 *   3. syncLeiExceptions() — REPEX delta (reporting exceptions, ~58 MB)
 *
 * Auth: Authorization: Bearer <ADMIN_SECRET>
 *
 * Schedule: daily at 02:00 UTC (GLEIF Golden Copy updates ~01:00 UTC)
 *
 * Vercel cron.json example:
 *   { "crons": [{ "path": "/api/cron/gleif-delta", "schedule": "0 2 * * *" }] }
 *
 * PM2/crontab example:
 *   0 2 * * * curl -H "Authorization: Bearer $ADMIN_SECRET" https://yourapp.com/api/cron/gleif-delta
 */

import { NextRequest, NextResponse } from 'next/server'
import { syncLeiDelta, syncLeiLevel2, syncLeiExceptions } from '@/lib/server/sync/gleif-golden-copy'

export const runtime = 'nodejs'

function isAuthorized(req: NextRequest): boolean {
  const secret = process.env.ADMIN_SECRET ?? process.env.SYNC_SECRET
  if (!secret) return false
  const auth = req.headers.get('authorization') ?? ''
  return auth === `Bearer ${secret}`
}

export async function GET(req: NextRequest) {
  if (!isAuthorized(req)) {
    return NextResponse.json({ error: 'Unauthorized.' }, { status: 401 })
  }

  const startMs = Date.now()

  try {
    // Run all three delta syncs sequentially (RR and REPEX depend on lei_cache rows from delta)
    const [deltaResult, level2Result, exceptionsResult] = await Promise.allSettled([
      syncLeiDelta(),
      syncLeiLevel2(),
      syncLeiExceptions(),
    ])

    const counts = {
      delta: deltaResult.status === 'fulfilled' ? deltaResult.value.count : 0,
      level2: level2Result.status === 'fulfilled' ? level2Result.value.count : 0,
      exceptions: exceptionsResult.status === 'fulfilled' ? exceptionsResult.value.count : 0,
    }

    const errors = [
      deltaResult.status === 'rejected' ? `delta: ${String(deltaResult.reason)}` : null,
      level2Result.status === 'rejected' ? `level2: ${String(level2Result.reason)}` : null,
      exceptionsResult.status === 'rejected' ? `exceptions: ${String(exceptionsResult.reason)}` : null,
    ].filter(Boolean)

    const durationMs = Date.now() - startMs
    console.log(`[cron/gleif-delta] complete: counts=${JSON.stringify(counts)}, duration=${durationMs}ms, errors=${errors.length}`)

    return NextResponse.json({
      ok: errors.length === 0,
      counts,
      durationMs,
      errors: errors.length > 0 ? errors : undefined,
    }, {
      status: errors.length > 0 ? 207 : 200,
    })
  } catch (error: unknown) {
    const message = error instanceof Error ? error.message : String(error)
    console.error('[cron/gleif-delta] fatal error:', message)
    return NextResponse.json({ error: message }, { status: 500 })
  }
}
```

**设计决策说明：**
- 使用 `Promise.allSettled()` 而不是 `Promise.all()`：level2 和 exceptions 依赖 lei_cache 中已有的 LEI 记录，不严格依赖 delta 成功；单一失败不应中止其他 sync
- 三个 sync 函数各自已写入 `sanctions_sync_log`，cron 路由不重复写日志
- 不导入 `db`（三个 sync 函数自管理数据库连接）
- `isAuthorized()` 与 cleanup/route.ts 完全一致（逐字复制，包括检查 `ADMIN_SECRET ?? SYNC_SECRET` 和 `!secret return false`）

**约束：**
- `runtime = 'nodejs'` 必须声明（streaming 操作不兼容 Edge Runtime）
- 无 Bearer token 时返回 HTTP 401（不是 403）
- 不添加 `maxDuration` —— delta sync 应在 300 秒内完成；若需要可在 Next.js 路由中设置但不超过 300
  </action>
  <verify>
    <automated>npm run type-check 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/cron/gleif-delta/route.ts` 存在
    - 文件包含 `export const runtime = 'nodejs'`
    - 文件包含 `function isAuthorized(req: NextRequest): boolean`
    - 文件包含字符串 `Bearer ${secret}` （Bearer token 认证）
    - 文件包含字符串 `status: 401`（未认证时返回 401）
    - 文件包含 `import { syncLeiDelta, syncLeiLevel2, syncLeiExceptions } from '@/lib/server/sync/gleif-golden-copy'`
    - 文件包含 `Promise.allSettled(`（容错并行执行）
    - `npm run type-check` 以退出码 0 完成
    - `npm run lint` 以退出码 0 完成
    - `npm run build` 以退出码 0 完成（构建验证所有路由均可正确编译）
  </acceptance_criteria>
  <done>cron/gleif-delta/route.ts 已创建，Bearer 认证通过，三个 delta sync 函数调用正确，构建通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 外部调度器 → /api/cron/gleif-delta | HTTP 请求需携带 Bearer token 才能触发 sync |
| 管理员 → /api/admin/sync POST | 需要 Bearer 或 admin email session 认证 |
| sync-gleif-full.mjs 子进程 → PostgreSQL | 子进程通过 DATABASE_URL 独立访问数据库 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05-01 | Elevation of Privilege | 未认证触发 /api/cron/gleif-delta | mitigate | `isAuthorized()` 检查 Bearer ADMIN_SECRET；无 token 返回 401；`if (!secret) return false` 在生产环境必须设置 secret |
| T-12-05-02 | Elevation of Privilege | 未认证触发 /api/admin/sync gleif:full | mitigate | 复用 admin/sync/route.ts 现有双重认证（Bearer token OR admin email session）；与 opensanctions 同等保护级别 |
| T-12-05-03 | Denial of Service | gleif:full 重复触发（多个并发 sync 进程） | accept | child process spawn 是幂等的（多个进程会竞争写同一 lei_cache 行，但 UPSERT ON CONFLICT 是原子的）；生产场景应通过 PM2 crontab 防止重复触发 |
| T-12-05-04 | Tampering | sync-gleif-full.mjs 脚本被替换（供应链攻击） | accept | 脚本在 Git 版本控制中；生产部署通过 CI/CD 管道，不允许运行时修改脚本文件 |
| T-12-05-05 | Information Disclosure | cron 路由泄露 sync 进度信息（record counts） | accept | 响应仅包含 counts 和 durations；不含 lei 数据；路由已有 Bearer 认证保护 |
| T-12-05-06 | Denial of Service | 无效 Bearer token 暴力破解 cron 端点 | accept | ADMIN_SECRET 由运营方设置；Next.js 无内置速率限制，但 Nginx 反向代理可配置；低优先级（非数据端点）|
</threat_model>

<verification>
Wave 4 完成标志（Phase 12 完整验证）：

**自动化验证：**
1. `npm run type-check` 通过（0 个错误）
2. `npm run lint` 通过
3. `npm run build` 通过

**手动验证（开发环境）：**
4. `curl -X POST http://localhost:3000/api/admin/sync -H "Content-Type: application/json" -d '{"source":"gleif:full"}' -H "Authorization: Bearer test"` → 返回 `{ success: true, pid: ..., message: "GLEIF full Level 1 import started..." }`
5. `curl http://localhost:3000/api/cron/gleif-delta` → 返回 HTTP 401
6. `curl http://localhost:3000/api/cron/gleif-delta -H "Authorization: Bearer $ADMIN_SECRET"` → 返回 `{ ok: true, counts: { delta: N, level2: N, exceptions: N } }`（需要 GLEIF 网络可达）
7. 在 `lei_cache` 表中查询记录：`psql -c "SELECT COUNT(*) FROM lei_cache"` → 在 delta sync 后应有行数
</verification>

<success_criteria>
- POST /api/admin/sync { source: 'gleif:full' } 触发后台子进程，立即返回 pid
- POST /api/admin/sync { source: 'gleif:delta' } 在进程内调用 syncLeiDelta()，返回 count
- GET /api/cron/gleif-delta 无 token → 401
- GET /api/cron/gleif-delta 有正确 token → 调用三个 delta sync，返回 counts
- npm run build 通过（完整构建验证）
</success_criteria>

<output>
After completion, create `.planning/phases/12-gleif-golden-copy-integration-lei-local-cache-ownership-chai/12-05-SUMMARY.md`
</output>
