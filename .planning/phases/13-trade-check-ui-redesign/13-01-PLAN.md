---
phase: 13-trade-check-ui-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/trade/page.tsx
  - src/app/trade/TradeClient.tsx
autonomous: true
requirements:
  - TRADE-UI-01
  - TRADE-UI-03
  - TRADE-UI-04

must_haves:
  truths:
    - "/trade 页面的 <main> 容器不再约束最大宽度，Split Panel 可填满视口宽度"
    - "TradeClient.tsx 渲染 380px 固定左列 + 弹性右列的 CSS Grid 布局"
    - "FormValues state 提升到 TradeClient 父组件，Recent Checks 可写入"
    - "表单字段排列为单列（无 2-column grid）"
    - "右面板有 empty / loading / result 三个状态分支"
    - "Recent Checks 从 localStorage（key: eti_recent_trade_checks）读取并显示在左面板表单下方"
    - "成功拿到结果后，将本次 check 写入 localStorage，最多保留 5 条"
  artifacts:
    - path: "src/app/trade/page.tsx"
      provides: "去掉 maxWidth 的 <main> 容器"
      contains: "no maxWidth"
    - path: "src/app/trade/TradeClient.tsx"
      provides: "Split Panel 布局 + TOKEN + 三态右面板 + Recent Checks"
      exports: ["TradeClient"]
      min_lines: 400
  key_links:
    - from: "TradeClient.tsx RightPanelState"
      to: "右面板 JSX 条件渲染"
      via: "panelState === 'empty' | 'loading' | 'result'"
      pattern: "panelState.*empty|panelState.*loading|panelState.*result"
    - from: "pushRecent()"
      to: "localStorage eti_recent_trade_checks"
      via: "submit() 成功回调后调用"
      pattern: "eti_recent_trade_checks"
    - from: "Recent Checks 列表项 onClick"
      to: "setValues()"
      via: "将历史 check 的字段预填到表单"
      pattern: "setValues.*seller.*vessel"
---

<objective>
将 /trade 页面从单列布局重构为 Split Panel 骨架，并实现 Recent Checks localStorage 功能。

Purpose: 建立 Phase 13 的布局基础——380px 固定左列（表单 + Recent Checks）+ 弹性右列（三态面板）。Plan 02 将在此骨架上升级控件样式，本 Plan 确保结构正确。

Output: 
- src/app/trade/page.tsx — <main> 无 maxWidth 约束
- src/app/trade/TradeClient.tsx — Split Panel 骨架 + TOKEN 常量 + FormValues 提升 + 单列表单 + 三态右面板 + Recent Checks
</objective>

<execution_context>
@/home/hippo/.claude/get-shit-done/workflows/execute-plan.md
@/home/hippo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/hippo/projects/Energy_trade_inspection/.planning/ROADMAP.md
@/home/hippo/projects/Energy_trade_inspection/.planning/phases/13-trade-check-ui-redesign/13-CONTEXT.md
@/home/hippo/projects/Energy_trade_inspection/.planning/phases/13-trade-check-ui-redesign/13-UI-SPEC.md
@/home/hippo/projects/Energy_trade_inspection/.planning/phases/13-trade-check-ui-redesign/13-PATTERNS.md
@/home/hippo/projects/Energy_trade_inspection/.claude/skills/sketch-findings-Energy_trade_inspection/references/trade-check-layout.md

<interfaces>
<!-- 类型合同（只读，不修改） -->
<!-- From src/app/api/trade/route.ts: -->
<!-- TradeCheckResult, TradePartyResult, TradeVesselResult, TradePortResult -->
<!-- From src/lib/server/trade-rules.ts: -->
<!-- TradeFlag, TradeVerdict, FLAG_EXPLANATIONS -->
<!-- From src/lib/types: RiskLevel = 'critical' | 'high' | 'medium' | 'low' -->

<!-- 现有 TradeClient.tsx 关键类型（保留）: -->
interface FormValues {
  seller: string
  vessel: string
  imo: string
  date: string
  loadingPort: string
  commodity: string
  sellerDomain: string
}

<!-- 新增 RecentCheck 类型（Pattern H）: -->
interface RecentCheck {
  seller: string
  vessel?: string
  commodity?: string
  loadingPort?: string
  overallRisk: RiskLevel
  checkedAt: string  // ISO string
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: page.tsx — 移除 main 容器的 maxWidth 约束</name>
  <files>src/app/trade/page.tsx</files>

  <read_first>
    - src/app/trade/page.tsx — 读懂当前 main style（第 47-52 行），定位 maxWidth 和 margin: '0 auto'
    - .planning/phases/13-trade-check-ui-redesign/13-CONTEXT.md — § 布局 "全宽布局" 决策
    - .planning/phases/13-trade-check-ui-redesign/13-PATTERNS.md — "src/app/trade/page.tsx (route, minor edit)" 节
  </read_first>

  <action>
仅修改 page.tsx 第 47-52 行的 `<main>` style prop。

**当前代码（第 47-52 行）：**
```tsx
<main
  style={{
    maxWidth: 'var(--max-width)',
    margin: '0 auto',
    padding: 'var(--space-8) var(--space-4)',
  }}
>
```

**改为（移除 maxWidth、margin、padding — split panel 自己管理内边距）：**
```tsx
<main>
```

即：将 `<main style={{...}}>` 替换为无 style prop 的 `<main>`。

其他所有内容（Header、UpgradePrompt、Suspense 边界、auth 重定向逻辑）保持原样，一字不改。

不触碰 UpgradePrompt 函数定义（第 66-116 行）。
  </action>

  <verify>
    <automated>grep -n "maxWidth" /home/hippo/projects/Energy_trade_inspection/src/app/trade/page.tsx | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - page.tsx 中不再包含 `maxWidth` 字符串（grep 返回 0）
    - page.tsx 中不再包含 `margin: '0 auto'`（在 main 元素上）
    - page.tsx 中仍然包含 `UpgradePrompt`、`Header`、`Suspense`、`TradeClient`
    - page.tsx 的 `<main>` 标签无 style prop（直接 `<main>`）
  </acceptance_criteria>

  <done>page.tsx 的 main 容器无宽度约束，TradeClient 渲染时可填满视口宽度</done>
</task>

<task type="auto">
  <name>Task 2: TradeClient.tsx — Split Panel 骨架 + TOKEN + 三态面板 + Recent Checks</name>
  <files>src/app/trade/TradeClient.tsx</files>

  <read_first>
    - src/app/trade/TradeClient.tsx — 读完整个文件（~896 行），理解所有现有子组件：VerdictLabel、RiskBadge、SanctionBadge、FlagCard、PartyCard、VesselCard、PortCard、ResultBanner、SaveTradeWatchButton、ResultsView、LoadingView（当前用 GlowLoader）、TradeForm、TradeClient 主组件
    - .planning/phases/13-trade-check-ui-redesign/13-CONTEXT.md — 所有 §（布局、CSS变量映射、Primary/Secondary Button、Inputs、右面板三态、Recent Checks、Form布局调整）
    - .planning/phases/13-trade-check-ui-redesign/13-UI-SPEC.md — §1 Layout、§3 Right Panel、§4 Recent Checks、§5 Form Field Order
    - .planning/phases/13-trade-check-ui-redesign/13-PATTERNS.md — Pattern A（TOKEN）、Pattern B（Split Panel）、Pattern G（三态右面板）、Pattern H（Recent Checks）、Pattern I（FormValues 提升）
    - .claude/skills/sketch-findings-Energy_trade_inspection/references/trade-check-layout.md — HTML Structure 节（Split Panel Shell、Loading Bar Animation）
  </read_first>

  <action>
对 TradeClient.tsx 进行**全量重写**。保留所有现有子组件的逻辑，但重构布局结构、状态管理和样式。

**严格约束（不得违反）：**
- 不修改任何从 `src/app/api/trade/route.ts` 导入的类型
- 不修改 `FlagCard`、`PartyCard`、`VesselCard`、`PortCard`、`ResultBanner`、`SaveTradeWatchButton` 的业务逻辑
- 不删除 GlowLoader 的 import（但 LoadingView 内部改为内联进度条）
- 保留 `FLAG_LABEL`、`TARGET_LABEL`、`VERDICT_RISK_MAP`、`VERDICT_DISPLAY`、`RISK_COLOR`、`RISK_BG`、`RISK_BORDER` 常量
- 保留 `FLAG_EXPLANATIONS` 引用和 FlagCard 中的解释文本区块

**Step 1: 文件顶部结构**

```tsx
'use client'

import { useState, useCallback, useEffect, useRef } from 'react'
import { useSearchParams } from 'next/navigation'
import Link from 'next/link'
import type { TradeCheckResult, TradePartyResult, TradeVesselResult, TradePortResult } from '@/app/api/trade/route'
import type { TradeFlag, TradeVerdict } from '@/lib/server/trade-rules'
import { FLAG_EXPLANATIONS } from '@/lib/server/trade-rules'
import type { RiskLevel } from '@/lib/types'
// GlowLoader import 保留（不删除），但 LoadingView 改为内联进度条

// ── TOKEN（所有硬编码值集中管理）─────────────────────────────────────────────
const TOKEN = {
  surface:      '#111113',
  elevated:     '#1e1e24',
  elevated2:    '#26262e',
  border:       'rgba(255,255,255,0.07)',
  borderHover:  'rgba(255,255,255,0.14)',
  primary:      '#6366f1',
  text:         '#f1f1f3',
  textMuted:    '#8b8b9a',
  textSubtle:   '#55556a',
} as const
```

**Step 2: 保留现有 RISK_COLOR / RISK_BG / RISK_BORDER / FLAG_LABEL 等常量（原样复制）**

RISK_COLOR 的 `low` 改为 `'#4ade80'`（对齐 Recent Checks spec，原为 `'#22c55e'`）：
```typescript
const RISK_COLOR: Record<RiskLevel, string> = {
  critical: '#ef4444',
  high:     '#f97316',
  medium:   '#fbbf24',
  low:      '#4ade80',
}
```

**Step 3: RecentCheck 类型和 localStorage 工具函数**

```typescript
const LS_KEY = 'eti_recent_trade_checks'
const MAX_RECENT = 5

interface RecentCheck {
  seller: string
  vessel?: string
  commodity?: string
  loadingPort?: string
  overallRisk: RiskLevel
  checkedAt: string
}

function getRecent(): RecentCheck[] {
  try {
    return JSON.parse(localStorage.getItem(LS_KEY) ?? '[]') as RecentCheck[]
  } catch { return [] }
}

function pushRecent(entry: RecentCheck) {
  const list = [entry, ...getRecent().filter(
    r => r.seller !== entry.seller || r.vessel !== entry.vessel
  )]
  localStorage.setItem(LS_KEY, JSON.stringify(list.slice(0, MAX_RECENT)))
}
```

**Step 4: FormValues 接口和 inputStyleNew / labelStyleNew（本 Plan 先用占位样式，Plan 02 升级为 sketch CSS）**

FormValues 接口保持不变。

新增 `focused` state 和 `inputStyleNew()` 函数——本 Plan 先实现结构，Plan 02 会替换为精确 sketch 值：
```typescript
function inputStyleNew(key: string, hasError?: boolean): React.CSSProperties {
  // Plan 02 会升级此函数为 inset-shadow 版本
  // 本 Plan 先提供功能性占位
  return {
    width: '100%',
    boxSizing: 'border-box' as const,
    background: 'rgba(0,0,0,0.28)',
    border: `1px solid ${hasError ? 'rgba(239,68,68,0.5)' : TOKEN.border}`,
    borderRadius: '7px',
    color: TOKEN.text,
    fontSize: '13px',
    fontFamily: 'inherit',
    padding: '8px 12px',
    outline: 'none',
  }
}
```

**Step 5: TradeForm 重构（接受 values + setValues 作为 props，单列布局）**

```tsx
interface TradeFormProps {
  values: FormValues
  setValues: React.Dispatch<React.SetStateAction<FormValues>>
  onSubmit: (v: FormValues) => void
}

function TradeForm({ values, setValues, onSubmit }: TradeFormProps) {
  const [touched, setTouched] = useState({ seller: false })
  const [focused, setFocused] = useState<string | null>(null)

  const set = (k: keyof FormValues) => (e: React.ChangeEvent<HTMLInputElement>) =>
    setValues(v => ({ ...v, [k]: e.target.value }))

  const sellerErr = touched.seller && values.seller.trim().length < 2

  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault()
    setTouched({ seller: true })
    if (values.seller.trim().length < 2) return
    onSubmit(values)
  }

  // 字段渲染辅助（单列，无 grid）
  const field = (label: string, key: keyof FormValues, placeholder: string, hint?: string, required?: boolean, type = 'text') => {
    const hasError = key === 'seller' && touched.seller && values[key].trim().length < 2
    return (
      <div style={{ display: 'flex', flexDirection: 'column', gap: '5px' }}>
        <label style={{
          fontSize: '11px', fontWeight: 500, color: TOKEN.textMuted,
          textTransform: 'uppercase', letterSpacing: '0.07em',
        }}>
          {label}{required && <span style={{ color: '#ef4444', marginLeft: '3px' }}>*</span>}
        </label>
        <input
          type={type}
          value={values[key]}
          onChange={set(key)}
          onBlur={key === 'seller' ? () => setTouched(t => ({ ...t, seller: true })) : undefined}
          onFocus={() => setFocused(key)}
          onBlur={() => { if (key === 'seller') setTouched(t => ({ ...t, seller: true })); setFocused(null) }}
          placeholder={placeholder}
          style={inputStyleNew(key, hasError)}
        />
        {hint && <span style={{ fontSize: '11px', color: TOKEN.textSubtle }}>{hint}</span>}
        {hasError && <span style={{ fontSize: '11px', color: '#ef4444' }}>Required</span>}
      </div>
    )
  }

  return (
    <form onSubmit={handleSubmit}>
      {/* 单列布局，字段顺序：CONTEXT.md § Form 布局调整 */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: '16px' }}>
        {field('Seller / Counterparty', 'seller', 'e.g. ZHENFU ENERGY (HAINAN) CO., LTD', undefined, true)}
        {field('Vessel Name', 'vessel', 'e.g. MV START', 'Optional — leave blank to screen seller only')}
        {field('IMO Number', 'imo', '7-digit number', 'Optional — improves vessel lookup accuracy')}
        {field('Trade Date', 'date', '', 'Used to correlate AIS dark periods', false, 'date')}
        {field('Loading Port (LOCODE)', 'loadingPort', 'e.g. CNHAK, SGSIN, AEDXB', 'UN/LOCODE — enables draft risk and geo checks')}
        {field('Commodity', 'commodity', 'e.g. Fuel Oil, Crude Oil, LNG', 'Optional context')}
        {field('Seller Domain (optional)', 'sellerDomain', 'e.g. seller.com', 'Used for domain fraud detection')}
      </div>

      <div style={{ marginTop: '20px' }}>
        {/* Primary 按钮 — Plan 02 会升级为 micro-gradient；本 Plan 先用功能性样式 */}
        <button
          type="submit"
          style={{
            width: '100%',
            padding: '11px 0',
            background: 'linear-gradient(180deg, #7578f2 0%, #5558e8 100%)',
            color: '#fff',
            border: '1px solid rgba(99,102,241,0.45)',
            borderRadius: '7px',
            fontSize: '13px',
            fontWeight: 500,
            fontFamily: 'inherit',
            cursor: 'pointer',
            letterSpacing: '0.01em',
          }}
        >
          Run Trade Check →
        </button>
      </div>
    </form>
  )
}
```

**Step 6: LoadingView — 内联进度条（替代 GlowLoader）**

```tsx
function LoadingView({ seller, vessel }: { seller: string; vessel: string }) {
  const barRef = useRef<HTMLDivElement>(null)

  useEffect(() => {
    const timer = setTimeout(() => {
      if (barRef.current) barRef.current.style.width = '100%'
    }, 50)
    return () => clearTimeout(timer)
  }, [])

  return (
    <div style={{
      display: 'flex', flexDirection: 'column',
      alignItems: 'center', justifyContent: 'center',
      minHeight: '400px', gap: '16px',
    }}>
      <p style={{ fontSize: '14px', color: TOKEN.textMuted, margin: 0 }}>Screening trade...</p>
      <div style={{
        width: '200px', height: '3px',
        background: 'rgba(0,0,0,0.35)',
        borderRadius: '2px', overflow: 'hidden',
      }}>
        <div
          ref={barRef}
          style={{
            height: '100%', background: TOKEN.primary,
            width: '0%', transition: 'width 1.4s ease',
          }}
        />
      </div>
      <p style={{ fontSize: '12px', color: TOKEN.textSubtle, margin: 0 }}>
        {seller}{vessel ? ` · ${vessel}` : ''}
      </p>
    </div>
  )
}
```

**Step 7: 保留所有卡片子组件**

`VerdictLabel`、`RiskBadge`、`SanctionBadge`、`FlagCard`（含 FLAG_EXPLANATIONS 区块）、`PartyCard`、`VesselCard`、`PortCard`、`ResultBanner`、`SaveTradeWatchButton`、`ResultsView` — **原样保留**，一字不改。

`ResultsView` 中的 action 按钮（Watch trade、Export Audit PDF、New check）样式升级推迟到 Plan 02。

**Step 8: TradeClient 主组件重构**

```tsx
type RightPanelState = 'empty' | 'loading' | 'result' | 'error'

export default function TradeClient({ initialSessionId }: { initialSessionId?: string }) {
  const searchParams = useSearchParams()
  const initSeller   = searchParams.get('seller') ?? ''
  const initVessel   = searchParams.get('vessel') ?? ''

  // FormValues 提升到父组件（Recent Checks 需要写入 setValues）
  const [values, setValues] = useState<FormValues>({
    seller: initSeller, vessel: initVessel,
    imo: '', date: '', loadingPort: '', commodity: '', sellerDomain: '',
  })

  const [panelState, setPanelState] = useState<RightPanelState>('empty')
  const [result, setResult]         = useState<TradeCheckResult | null>(null)
  const [error, setError]           = useState('')
  const [lastInput, setInput]       = useState<FormValues | null>(null)

  // Recent Checks state（SSR 安全：只在 useEffect 中读 localStorage）
  const [recent, setRecent] = useState<RecentCheck[]>([])
  useEffect(() => { setRecent(getRecent()) }, [])

  // 恢复会话（从 Reports 页导航过来时）
  useEffect(() => {
    if (!initialSessionId) return
    setPanelState('loading')
    setInput({ seller: '', vessel: '', imo: '', date: '', loadingPort: '', commodity: '', sellerDomain: '' })
    fetch(`/api/trade/${encodeURIComponent(initialSessionId)}`)
      .then(r => r.ok ? r.json() : Promise.reject())
      .then(data => { setResult(data as TradeCheckResult); setPanelState('result') })
      .catch(() => { setError('Could not load report.'); setPanelState('error') })
  }, [initialSessionId])

  async function submit(v: FormValues) {
    setInput(v)
    setPanelState('loading')
    setError('')
    try {
      const res = await fetch('/api/trade', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          seller:      v.seller.trim(),
          vessel:      v.vessel.trim(),
          imo:         v.imo.trim() || undefined,
          date:        v.date || undefined,
          loadingPort: v.loadingPort.trim() || undefined,
          commodity:   v.commodity.trim() || undefined,
          sellerDomain: v.sellerDomain.trim() || undefined,
        }),
      })
      const json = await res.json()
      if (!res.ok) {
        setError((json as { error?: string }).error ?? 'Trade check failed.')
        setPanelState('error')
        return
      }
      const tradeResult = json as TradeCheckResult
      setResult(tradeResult)
      setPanelState('result')

      // 写入 Recent Checks
      const entry: RecentCheck = {
        seller: v.seller.trim(),
        vessel: v.vessel.trim() || undefined,
        commodity: v.commodity.trim() || undefined,
        loadingPort: v.loadingPort.trim() || undefined,
        overallRisk: tradeResult.overallRisk,
        checkedAt: new Date().toISOString(),
      }
      pushRecent(entry)
      setRecent(getRecent())
    } catch {
      setError('Network error. Please try again.')
      setPanelState('error')
    }
  }

  function reset() {
    setPanelState('empty')
    setResult(null)
    setError('')
  }

  return (
    // Split Panel 外壳（Pattern B — Variant B confirmed winner）
    <div style={{
      display: 'grid',
      gridTemplateColumns: '380px 1fr',
      minHeight: 'calc(100vh - 44px)',
    }}>
      {/* 左列：表单 + Recent Checks */}
      <div style={{
        borderRight: `1px solid ${TOKEN.border}`,
        padding: '32px 24px',
        overflowY: 'auto',
        background: TOKEN.surface,
      }}>
        {/* 页面标题 */}
        <div style={{ marginBottom: '24px' }}>
          <h1 style={{ fontSize: '18px', fontWeight: 700, color: TOKEN.text, margin: '0 0 6px' }}>
            Trade Check
          </h1>
          <p style={{ fontSize: '13px', color: TOKEN.textMuted, lineHeight: '1.5', margin: 0 }}>
            Screen seller, vessel, and port against sanctions, AIS, and registry data.
          </p>
        </div>

        {/* 表单 */}
        <TradeForm values={values} setValues={setValues} onSubmit={submit} />

        {/* Error 显示（仍在左列表单下方） */}
        {panelState === 'error' && (
          <div style={{
            marginTop: '16px',
            background: 'rgba(239,68,68,0.08)',
            border: '1px solid rgba(239,68,68,0.25)',
            borderRadius: '7px',
            padding: '12px 14px',
          }}>
            <p style={{ fontSize: '13px', fontWeight: 500, color: '#ef4444', margin: '0 0 4px' }}>
              Check failed
            </p>
            <p style={{ fontSize: '12px', color: TOKEN.textMuted, margin: 0 }}>{error}</p>
          </div>
        )}

        {/* Recent Checks（表单下方，仅在有历史记录时显示）*/}
        {recent.length > 0 && (
          <div style={{ marginTop: '32px' }}>
            <div style={{
              fontSize: '11px', color: TOKEN.textSubtle,
              textTransform: 'uppercase', letterSpacing: '0.07em',
              marginBottom: '12px',
            }}>
              Recent Checks
            </div>
            {recent.map((r, i) => (
              <div
                key={i}
                onClick={() => setValues({
                  seller: r.seller,
                  vessel: r.vessel ?? '',
                  commodity: r.commodity ?? '',
                  loadingPort: r.loadingPort ?? '',
                  imo: '',
                  date: '',
                  sellerDomain: '',
                })}
                style={{ padding: '8px 10px', borderRadius: '7px', cursor: 'pointer', transition: 'background 0.1s ease' }}
                onMouseEnter={e => (e.currentTarget.style.background = TOKEN.elevated)}
                onMouseLeave={e => (e.currentTarget.style.background = 'transparent')}
              >
                <div style={{ fontSize: '13px', fontWeight: 500, color: TOKEN.text }}>
                  {r.seller}
                </div>
                <div style={{ fontSize: '11px', color: TOKEN.textMuted }}>
                  {[r.commodity, r.loadingPort].filter(Boolean).join(' · ')}
                  {(r.commodity || r.loadingPort) ? ' · ' : ''}
                  <span style={{ color: RISK_COLOR[r.overallRisk] }}>
                    {r.overallRisk.toUpperCase()}
                  </span>
                </div>
              </div>
            ))}
          </div>
        )}
      </div>

      {/* 右列：三态面板 */}
      <div style={{ padding: '32px', overflowY: 'auto' }}>
        {panelState === 'empty' && (
          <div style={{
            display: 'flex', flexDirection: 'column',
            alignItems: 'center', justifyContent: 'center',
            minHeight: '400px', textAlign: 'center',
          }}>
            <div style={{ fontSize: '28px', color: TOKEN.textSubtle, marginBottom: '12px' }}>⚡</div>
            <p style={{ fontSize: '14px', color: TOKEN.textSubtle, margin: 0 }}>
              Run a trade check to see results
            </p>
          </div>
        )}

        {panelState === 'loading' && lastInput && (
          <LoadingView seller={lastInput.seller} vessel={lastInput.vessel} />
        )}

        {panelState === 'result' && result && (
          <ResultsView result={result} onReset={reset} />
        )}

        {/* error state: 右面板保持 empty（错误已在左列显示）*/}
        {panelState === 'error' && (
          <div style={{
            display: 'flex', flexDirection: 'column',
            alignItems: 'center', justifyContent: 'center',
            minHeight: '400px', textAlign: 'center',
          }}>
            <div style={{ fontSize: '28px', color: TOKEN.textSubtle, marginBottom: '12px' }}>⚡</div>
            <p style={{ fontSize: '14px', color: TOKEN.textSubtle, margin: 0 }}>
              Run a trade check to see results
            </p>
          </div>
        )}
      </div>
    </div>
  )
}
```

**禁止触碰的文件（无论如何不得修改）：**
- src/app/api/ 下的任何文件
- src/lib/server/ 下的任何文件
- src/components/ui/GlowLoader.tsx
  </action>

  <verify>
    <automated>cd /home/hippo/projects/Energy_trade_inspection && npm run type-check 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - TradeClient.tsx 包含 `gridTemplateColumns: '380px 1fr'`（Split Panel，TRADE-UI-01）
    - TradeClient.tsx 包含 `minHeight: 'calc(100vh - 44px)'`
    - TradeClient.tsx 包含 `eti_recent_trade_checks`（Recent Checks localStorage key，TRADE-UI-04）
    - TradeClient.tsx 包含 `pushRecent`（写入历史记录的函数）
    - TradeClient.tsx 包含 `type RightPanelState = 'empty' | 'loading' | 'result' | 'error'`（三态面板，TRADE-UI-03）
    - TradeClient.tsx 包含 `Run a trade check to see results`（Empty state 文案）
    - TradeClient.tsx 包含 `Screening trade...`（Loading state 文案）
    - TradeClient.tsx 包含 `transition: 'width 1.4s ease'`（进度条动画）
    - TradeClient.tsx 中 `<div style={{ display: 'flex', flexDirection: 'column', gap: '16px' }}>` 用于表单单列布局（无 2-column grid）
    - TradeClient.tsx 不包含 `gridTemplateColumns: '1fr 1fr'`（原 2-column form grid 已移除）
    - TradeClient.tsx 包含 `const TOKEN = {`（TOKEN 常量块）
    - TradeClient.tsx 包含 `setValues.*seller.*vessel`（Recent Checks 点击预填表单）
    - `npm run type-check` 无 TypeScript 错误
  </acceptance_criteria>

  <done>TradeClient.tsx 完成全量重写：Split Panel 布局正确渲染，表单单列，三态右面板可切换，Recent Checks 读写 localStorage 正常工作</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→localStorage | 客户端读写 localStorage，无跨源风险 |
| client→/api/trade | 不可信的用户输入通过 fetch 发送到 API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | localStorage eti_recent_trade_checks | accept | 本地存储只影响 UI 历史显示，无服务端信任；try/catch JSON.parse 防止 corrupt 数据 |
| T-13-02 | Information Disclosure | TradeClient.tsx → /api/trade | accept | API 层已有 auth 检查（page.tsx server 组件），纯 UI 层无额外暴露 |
</threat_model>

<verification>
手动验证步骤（Plan 01 完成后）：

1. `npm run dev` 启动开发服务器
2. 访问 `/trade`（已登录 starter+ 账户）
3. 验证：页面呈现左列（380px 固定宽度）+ 右列（弹性）的 split panel 布局
4. 验证：右列初始状态显示 "Run a trade check to see results" 文案和 ⚡ 图标
5. 输入 seller 信息，点击 "Run Trade Check →"
6. 验证：右列显示进度条动画（width 从 0% 到 100%，1.4s 过渡）
7. 等待结果，验证：右列切换为 ResultsView（ResultBanner + FlagCards）
8. 验证：左列表单下方出现 "RECENT CHECKS" 区块，包含刚完成的检查记录
9. 点击历史记录，验证：表单字段被预填
</verification>

<success_criteria>
1. page.tsx 的 `<main>` 无 maxWidth — `grep 'maxWidth' src/app/trade/page.tsx` 返回 0 行
2. TradeClient.tsx 包含 `gridTemplateColumns: '380px 1fr'` — Split Panel 结构（TRADE-UI-01）
3. TradeClient.tsx 包含 `eti_recent_trade_checks` — localStorage key 正确（TRADE-UI-04）
4. TradeClient.tsx 包含 `type RightPanelState` — 三态面板状态机（TRADE-UI-03）
5. `npm run type-check` 零 TypeScript 错误
</success_criteria>

<output>
完成后创建 `.planning/phases/13-trade-check-ui-redesign/13-01-SUMMARY.md`，记录：
- 完成的任务
- 最终文件行数
- TradeClient 新旧架构对比（ViewState → RightPanelState，FormValues 提升位置）
- 任何与 PLAN 偏差的实现决策
</output>
