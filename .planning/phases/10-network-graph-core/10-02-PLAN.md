---
phase: 10-network-graph-core
plan: 02
type: execute
wave: 1
depends_on:
  - "10-01"
files_modified:
  - src/lib/server/repository.ts
autonomous: true
requirements:
  - GRAPH-03
  - GRAPH-04

must_haves:
  truths:
    - "getNetworkGraph(entityId) 函数存在于 repository.ts 并可导出"
    - "函数使用三次独立 db.query() 调用：ETI directors 查询、ETI vessels 查询、ICIJ WITH RECURSIVE CTE 查询"
    - "ICIJ CTE 中包含 depth < 3 终止条件、LIMIT 100 行数上限、visited[] 防环机制"
    - "返回值类型为 NetworkGraphResult（包含 nodes、edges、truncated、totalNodeCount）"
    - "所有 SQL 查询使用参数化占位符 $1，不存在字符串插值"
    - "npm run type-check 零错误通过"
  artifacts:
    - path: "src/lib/server/repository.ts"
      provides: "getNetworkGraph() 数据库函数实现"
      exports: ["getNetworkGraph"]
      contains: "WITH RECURSIVE icij_cte"
  key_links:
    - from: "src/app/company/[slug]/page.tsx (Wave 2)"
      to: "src/lib/server/repository.ts"
      via: "await getNetworkGraph(company.id)"
      pattern: "export async function getNetworkGraph"
    - from: "icij_entities 表"
      to: "icij_relationships 表"
      via: "WITH RECURSIVE CTE JOIN"
      pattern: "WITH RECURSIVE icij_cte"
---

<objective>
在 repository.ts 中实现 getNetworkGraph(entityId) 函数，该函数通过三次查询聚合公司网络图数据：ETI 注册连接（董事/股东首层）、关联船舶（首层）、ICIJ 离岸实体（3跳递归）。

Purpose: 这是 Phase 10 的核心数据层——所有图谱节点和边数据均从此函数获取。GRAPH-03 要求通过 PostgreSQL WITH RECURSIVE CTE 实现 3 跳递归，GRAPH-04 要求服务端计算节点颜色。

Output: repository.ts 中新增的 getNetworkGraph() 函数，接受 ETI entity ID，返回 { nodes: NetworkNode[], edges: NetworkEdge[], truncated: boolean, totalNodeCount: number }。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-network-graph-core/10-CONTEXT.md
@.planning/phases/10-network-graph-core/10-RESEARCH.md
@.planning/phases/10-network-graph-core/10-PATTERNS.md
@.planning/phases/10-network-graph-core/10-01-SUMMARY.md

<interfaces>
<!-- 从 Plan 01 获得的类型合同（执行前必须通过 Plan 01） -->
在 src/lib/server/repository.ts 中（Plan 01 插入）:
```typescript
export interface NetworkNode {
  id: string
  type: 'root' | 'company' | 'vessel' | 'person' | 'icij'
  label: string        // ≤20 chars, trailing … if longer
  fullName: string
  etlKey: string | null
  nodeColor: 'root' | 'sanctioned' | 'fraud' | 'icij' | 'normal'
  subtype: string
}

export interface NetworkEdge {
  id: string
  source: string
  target: string
  edgeType: 'eti' | 'icij'
  label?: string
}

export interface NetworkGraphResult {
  nodes: NetworkNode[]
  edges: NetworkEdge[]
  truncated: boolean
  totalNodeCount: number
}
```

现有模式（getIcijMatches, lines 1014–1040）:
```typescript
export async function getIcijMatches(entityId: string): Promise<IcijMatch[]> {
  const { rows } = await db.query(
    `SELECT node_id, name, ...
     FROM icij_entities
     WHERE linked_entity_id = $1
     ORDER BY match_confidence DESC
     LIMIT 20`,
    [entityId]
  )
  return rows.map((r) => ({ nodeId: r.node_id, ... }))
}
```

数据库表结构（关键字段）:
- icij_entities: node_id, name, dataset, entity_type, is_sanctioned (boolean), sanctions_match (text), linked_entity_id
- icij_relationships: from_node_id, to_node_id, rel_type, link, dataset
- fraud_alerts: company_name (text), list_type ('blacklist'|'whitelist')
- entities: id, metadata_json (jsonb) — metadata_json.directors[], metadata_json.vessels[], metadata_json.beneficial_owners[]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 getNetworkGraph() — ETI 首层 + ICIJ 递归三查询函数</name>
  <read_first>
    - /home/hippo/projects/Energy_trade_inspection/src/lib/server/repository.ts — 必读 lines 996–1200，确认接口定义位置（Plan 01 结果）及 db.query() 调用模式、snake_case→camelCase 映射规范
    - /home/hippo/projects/Energy_trade_inspection/db/migrations/011_ports_psc_icij.sql — 确认 icij_entities 表字段：node_id、name、entity_type、is_sanctioned、linked_entity_id
    - /home/hippo/projects/Energy_trade_inspection/db/migrations/014_icij_relationships.sql — 确认 icij_relationships 表字段：from_node_id、to_node_id、rel_type、link
    - /home/hippo/projects/Energy_trade_inspection/db/migrations/028_fraud_alerts.sql — 确认 fraud_alerts 表字段（用于橙色节点检测）
    - /home/hippo/projects/Energy_trade_inspection/.planning/phases/10-network-graph-core/10-RESEARCH.md — 参考 Pattern 1（WITH RECURSIVE CTE 结构）和 Anti-Patterns（防环、去重、100节点上限作用域）
  </read_first>
  <files>src/lib/server/repository.ts</files>
  <behavior>
    - 行为 1：公司有 3 个 directors → nodes 包含 root node + 3 个 person nodes；edges 包含 3 条从 root 到各 director 的边
    - 行为 2：公司有 1 艘关联船舶，该船舶 sanctionStatus='listed' → vessel node 的 nodeColor='sanctioned'
    - 行为 3：公司无 directors、无 vessels、无 ICIJ matches → 返回 { nodes: [rootNode], edges: [], truncated: false, totalNodeCount: 0 }
    - 行为 4：ICIJ CTE 结果超过 100 个节点 → truncated=true，totalNodeCount 为未截断前的总数
    - 行为 5：同一 ICIJ 节点通过两条不同路径可达 → 在返回的 nodes 中只出现一次（按 node_id 去重）
    - 行为 6：director 名称与 fraud_alerts 表中某条记录的 company_name 完全匹配 → 该 director node 的 nodeColor='fraud'
  </behavior>
  <action>
在 NetworkGraphResult 接口定义之后（约 line 1150 附近），插入以下 getNetworkGraph() 函数实现：

```typescript
/**
 * Build the full network graph for a company entity.
 * Returns three categories of nodes:
 *   1. ETI registry connections (directors + beneficial owners) — first-layer, non-recursive
 *   2. ETI vessels — first-layer, non-recursive
 *   3. ICIJ offshore entities — recursive up to 3 hops, capped at 100 nodes
 *
 * Color priority: sanctioned > fraud > icij > normal
 * The 100-node cap applies ONLY to ICIJ recursive nodes. ETI directors/vessels are additional.
 */
export async function getNetworkGraph(entityId: string): Promise<NetworkGraphResult> {
  const nodes: NetworkNode[] = []
  const edges: NetworkEdge[] = []

  // ── 1. Fetch root entity ─────────────────────────────────────────────────
  const { rows: rootRows } = await db.query(
    `SELECT id, name, slug, metadata_json,
            sanction_status
     FROM entities
     WHERE id = $1
     LIMIT 1`,
    [entityId]
  )
  if (rootRows.length === 0) {
    return { nodes: [], edges: [], truncated: false, totalNodeCount: 0 }
  }
  const rootRow = rootRows[0]
  const rootName: string = rootRow.name ?? 'Unknown'

  // Root node always uses 'root' color regardless of sanction status
  nodes.push({
    id: `eti-root-${entityId}`,
    type: 'root',
    label: rootName.length > 20 ? rootName.slice(0, 19) + '\u2026' : rootName,
    fullName: rootName,
    etlKey: rootRow.slug ?? null,
    nodeColor: 'root',
    subtype: 'Company',
  })

  const rootNodeId = `eti-root-${entityId}`
  const meta = rootRow.metadata_json ?? {}

  // ── 2. ETI Directors + Beneficial Owners (first-layer, non-recursive) ───
  // Collect all director/beneficial owner names for fraud alert lookup
  const directorNames: string[] = []
  const directors: Array<{ id: string; name: string; role?: string }> =
    Array.isArray(meta.directors) ? meta.directors : []
  const beneficialOwners: Array<{ name: string }> =
    Array.isArray(meta.beneficial_owners) ? meta.beneficial_owners : []

  for (const d of directors) {
    if (!d.name) continue
    directorNames.push(d.name)
  }
  for (const bo of beneficialOwners) {
    if (!bo.name) continue
    // Avoid duplicates (some directors are also beneficial owners)
    if (!directorNames.includes(bo.name)) directorNames.push(bo.name)
  }

  // Fraud alert lookup for all director/BO names (batch query)
  const fraudAlertsMap = new Set<string>() // lowercased names with fraud alerts
  if (directorNames.length > 0) {
    // Use unnest for efficient batch lookup
    const { rows: fraudRows } = await db.query(
      `SELECT DISTINCT lower(company_name) AS lname
       FROM fraud_alerts
       WHERE lower(company_name) = ANY($1::text[])
         AND list_type = 'blacklist'`,
      [directorNames.map((n) => n.toLowerCase())]
    )
    for (const fr of fraudRows) {
      fraudAlertsMap.add(fr.lname)
    }
  }

  // Add director nodes
  const addedPersonIds = new Set<string>()
  for (const d of directors) {
    if (!d.name) continue
    const personId = `eti-dir-${d.id ?? d.name.replace(/\s+/g, '-').toLowerCase()}`
    if (addedPersonIds.has(personId)) continue
    addedPersonIds.add(personId)

    const hasFraud = fraudAlertsMap.has(d.name.toLowerCase())
    const fullName = d.name
    nodes.push({
      id: personId,
      type: 'person',
      label: fullName.length > 20 ? fullName.slice(0, 19) + '\u2026' : fullName,
      fullName,
      etlKey: null, // directors have no individual ETI page
      nodeColor: hasFraud ? 'fraud' : 'normal',
      subtype: d.role ? d.role.slice(0, 20) : 'Director',
    })
    edges.push({
      id: `edge-dir-${personId}`,
      source: rootNodeId,
      target: personId,
      edgeType: 'eti',
      label: 'director of',
    })
  }

  // Add beneficial owner nodes (skip if same name already added as director)
  for (const bo of beneficialOwners) {
    if (!bo.name) continue
    const personId = `eti-bo-${bo.name.replace(/\s+/g, '-').toLowerCase()}`
    if (addedPersonIds.has(personId)) continue
    // Also skip if same person added as director (different id prefix but same name)
    const alreadyAdded = [...addedPersonIds].some((pid) =>
      pid.startsWith('eti-dir-') &&
      nodes.find((n) => n.id === pid)?.fullName.toLowerCase() === bo.name.toLowerCase()
    )
    if (alreadyAdded) continue
    addedPersonIds.add(personId)

    const hasFraud = fraudAlertsMap.has(bo.name.toLowerCase())
    const fullName = bo.name
    nodes.push({
      id: personId,
      type: 'person',
      label: fullName.length > 20 ? fullName.slice(0, 19) + '\u2026' : fullName,
      fullName,
      etlKey: null,
      nodeColor: hasFraud ? 'fraud' : 'normal',
      subtype: 'Beneficial Owner',
    })
    edges.push({
      id: `edge-bo-${personId}`,
      source: rootNodeId,
      target: personId,
      edgeType: 'eti',
      label: 'beneficial owner of',
    })
  }

  // ── 3. ETI Vessels (first-layer, non-recursive) ──────────────────────────
  const vessels: Array<{ imo: string; name: string; flag?: string }> =
    Array.isArray(meta.vessels) ? meta.vessels : []

  for (const v of vessels) {
    if (!v.imo) continue
    const vesselNodeId = `eti-vessel-${v.imo}`

    // Look up vessel sanction and fraud status
    const { rows: vesselRows } = await db.query(
      `SELECT e.sanction_status,
              EXISTS(
                SELECT 1 FROM fraud_alerts fa
                WHERE lower(fa.company_name) = lower(e.name)
                  AND fa.list_type = 'blacklist'
              ) AS has_fraud
       FROM entities e
       WHERE e.type = 'vessel'
         AND e.metadata_json->>'imo' = $1
       LIMIT 1`,
      [v.imo]
    )

    let vesselColor: NetworkNode['nodeColor'] = 'normal'
    if (vesselRows.length > 0) {
      if (vesselRows[0].sanction_status === 'listed') vesselColor = 'sanctioned'
      else if (vesselRows[0].has_fraud) vesselColor = 'fraud'
    }

    const fullName = v.name ?? `Vessel ${v.imo}`
    nodes.push({
      id: vesselNodeId,
      type: 'vessel',
      label: fullName.length > 20 ? fullName.slice(0, 19) + '\u2026' : fullName,
      fullName,
      etlKey: v.imo, // vessel IMO = navigation key → /vessel/{imo}
      nodeColor: vesselColor,
      subtype: 'Vessel',
    })
    edges.push({
      id: `edge-vessel-${v.imo}`,
      source: rootNodeId,
      target: vesselNodeId,
      edgeType: 'eti',
      label: 'operated by',
    })
  }

  // ── 4. ICIJ Offshore Entities (WITH RECURSIVE CTE, depth ≤3, limit 100) ─
  // Step 4a: Get total count (without LIMIT) to detect truncation
  const { rows: countRows } = await db.query(
    `WITH RECURSIVE icij_cte AS (
       -- Base case: ICIJ entities directly linked to this ETI company
       SELECT
         ie.node_id,
         ie.name,
         ie.dataset,
         ie.entity_type,
         ie.is_sanctioned,
         ie.sanctions_match,
         0 AS depth,
         ARRAY[ie.node_id] AS visited,
         NULL::TEXT AS parent_node_id,
         NULL::TEXT AS rel_link
       FROM icij_entities ie
       WHERE ie.linked_entity_id = $1

       UNION ALL

       -- Recursive case: traverse icij_relationships up to depth 3
       SELECT
         next_e.node_id,
         next_e.name,
         next_e.dataset,
         next_e.entity_type,
         next_e.is_sanctioned,
         next_e.sanctions_match,
         cte.depth + 1,
         cte.visited || next_e.node_id,
         cte.node_id AS parent_node_id,
         rel.link    AS rel_link
       FROM icij_cte cte
       JOIN icij_relationships rel
         ON rel.from_node_id = cte.node_id OR rel.to_node_id = cte.node_id
       JOIN icij_entities next_e
         ON next_e.node_id = CASE
              WHEN rel.from_node_id = cte.node_id THEN rel.to_node_id
              ELSE rel.from_node_id
            END
       WHERE cte.depth < 3
         AND NOT (next_e.node_id = ANY(cte.visited))
     )
     SELECT COUNT(DISTINCT node_id)::INT AS total
     FROM icij_cte`,
    [entityId]
  )
  const totalNodeCount: number = countRows[0]?.total ?? 0
  const truncated = totalNodeCount > 100

  // Step 4b: Fetch actual nodes (with LIMIT 100, deduplicated by shallowest depth)
  const { rows: icijRows } = await db.query(
    `WITH RECURSIVE icij_cte AS (
       SELECT
         ie.node_id,
         ie.name,
         ie.dataset,
         ie.entity_type,
         ie.is_sanctioned,
         ie.sanctions_match,
         0 AS depth,
         ARRAY[ie.node_id] AS visited,
         NULL::TEXT AS parent_node_id,
         NULL::TEXT AS rel_link
       FROM icij_entities ie
       WHERE ie.linked_entity_id = $1

       UNION ALL

       SELECT
         next_e.node_id,
         next_e.name,
         next_e.dataset,
         next_e.entity_type,
         next_e.is_sanctioned,
         next_e.sanctions_match,
         cte.depth + 1,
         cte.visited || next_e.node_id,
         cte.node_id AS parent_node_id,
         rel.link    AS rel_link
       FROM icij_cte cte
       JOIN icij_relationships rel
         ON rel.from_node_id = cte.node_id OR rel.to_node_id = cte.node_id
       JOIN icij_entities next_e
         ON next_e.node_id = CASE
              WHEN rel.from_node_id = cte.node_id THEN rel.to_node_id
              ELSE rel.from_node_id
            END
       WHERE cte.depth < 3
         AND NOT (next_e.node_id = ANY(cte.visited))
     )
     -- Deduplicate: keep shallowest path to each node
     SELECT DISTINCT ON (node_id)
       node_id, name, dataset, entity_type,
       is_sanctioned, sanctions_match,
       depth, parent_node_id, rel_link
     FROM icij_cte
     ORDER BY node_id, depth
     LIMIT 100`,
    [entityId]
  )

  // Build a Set of ICIJ node IDs added so far (for edge deduplication)
  const addedIcijIds = new Set<string>()

  for (const r of icijRows) {
    const nodeId = r.node_id as string
    addedIcijIds.add(nodeId)

    const isSanctioned = r.is_sanctioned === true
    const fullName = (r.name as string) ?? 'Unknown Entity'

    nodes.push({
      id: nodeId,
      type: 'icij',
      label: fullName.length > 20 ? fullName.slice(0, 19) + '\u2026' : fullName,
      fullName,
      etlKey: null, // ICIJ nodes have no ETI detail page
      // Color priority: sanctioned > icij (ICIJ nodes are never 'fraud' or 'normal')
      nodeColor: isSanctioned ? 'sanctioned' : 'icij',
      subtype: 'Offshore Entity',
    })

    // Add edge from parent to this node
    const parentId: string | null = r.parent_node_id
    if (parentId) {
      // Parent is either another ICIJ node or (if depth=1) the root
      // depth=0 nodes have parent_node_id=NULL → edge from root
      edges.push({
        id: `edge-icij-${parentId}-${nodeId}`,
        source: parentId,
        target: nodeId,
        edgeType: 'icij',
        label: r.rel_link ?? undefined,
      })
    } else {
      // depth=0: direct link from root ETI company
      edges.push({
        id: `edge-icij-root-${nodeId}`,
        source: rootNodeId,
        target: nodeId,
        edgeType: 'icij',
        label: r.rel_link ?? undefined,
      })
    }
  }

  return { nodes, edges, truncated, totalNodeCount }
}
```

**关键实现规则（必须遵守）：**
1. 所有 db.query() 调用必须使用参数化占位符 `$1`——绝不使用字符串模板插值。
2. 100 节点上限（LIMIT 100）仅出现在 ICIJ CTE 查询中，不影响 ETI directors 和 vessels 的首层查询。
3. WITH RECURSIVE CTE 必须包含三重终止机制：`depth < 3`（跳数限制）、`NOT (next_e.node_id = ANY(cte.visited))`（防环）、`LIMIT 100`（行数限制）。
4. 节点颜色优先级（服务端计算）：sanctioned > fraud > icij > normal。根节点始终为 'root'。
5. DISTINCT ON (node_id) ORDER BY node_id, depth 确保同一节点通过多条路径时只保留最浅路径。
6. 第一次查询（COUNT 查询，不加 LIMIT）用于 truncation 检测；第二次查询（LIMIT 100）获取实际节点数据——两次使用相同的 CTE 结构，避免逻辑不一致。
  </action>
  <verify>
    <automated>cd /home/hippo/projects/Energy_trade_inspection && npm run type-check 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export async function getNetworkGraph" src/lib/server/repository.ts` 有输出
    - `grep -c "WITH RECURSIVE icij_cte" src/lib/server/repository.ts` 输出为 2（COUNT 和 LIMIT 两个查询）
    - `grep "depth < 3" src/lib/server/repository.ts` 至少有 2 行匹配
    - `grep "LIMIT 100" src/lib/server/repository.ts` 至少 1 行匹配（仅在 ICIJ CTE 中）
    - `grep "NOT (next_e.node_id = ANY(cte.visited))" src/lib/server/repository.ts` 至少 2 行匹配
    - `grep "Promise<NetworkGraphResult>" src/lib/server/repository.ts` 有输出（函数签名正确）
    - `grep -v "template\|interpolat" src/lib/server/repository.ts | grep "\$1"` 确认参数化查询存在
    - `npm run type-check` 退出码为 0，无 TypeScript 错误
  </acceptance_criteria>
  <done>getNetworkGraph() 函数实现完成，三类节点（ETI directors、ETI vessels、ICIJ 递归）聚合，类型检查零错误</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page.tsx (Server Component) → getNetworkGraph() | entityId 由 URL slug 解析后经 getEntityByKey() 验证——非用户直接输入 |
| getNetworkGraph() → PostgreSQL | SQL 参数化查询边界——所有用户可控输入以 $1 传递 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-SQL-01 | Tampering (SQL Injection) | getNetworkGraph() — 所有 db.query() 调用 | mitigate | 使用 pg 参数化查询（$1 占位符），entityId 通过 getEntityByKey() 验证后传入——与 getIcijMatches()、getIcijOfficerNetwork() 保持相同安全模式；禁止字符串模板插值 |
| T-10-DOS-01 | Denial of Service (CTE 无限递归) | WITH RECURSIVE icij_cte | mitigate | 三重终止：depth < 3 跳数限制 + NOT (node_id = ANY(visited)) 防环 + LIMIT 100 行数限制；PostgreSQL 16 statement_timeout 已在 db pool 配置中设置（现有 db.ts 继承） |
| T-10-INFODISCLOSURE-01 | Information Disclosure | NetworkGraphResult 序列化传递给客户端 | accept | nodes 仅包含公开可见的实体名称、slug、IMO；不包含 entity.id（内部 UUID）、score breakdown、私有字段；etlKey 仅为 slug/IMO（已在搜索结果等公开 API 中暴露） |
| T-10-F3-SKIP-01 | Elevation of Privilege (F3 绕过) | getNetworkGraph() 被无条件调用 | mitigate | 该函数本身是纯数据层，不含权限判断；F3 检查在调用方 page.tsx 中进行：`f3Unlocked ? await getNetworkGraph(company.id) : { nodes: [], ... }`——双重保护（数据不查询 + ContentLock 渲染锁）。Plan 03 实现调用方时必须遵守此模式 |
</threat_model>

<verification>
1. `grep -n "export async function getNetworkGraph" /home/hippo/projects/Energy_trade_inspection/src/lib/server/repository.ts` — 确认函数存在
2. `grep -c "WITH RECURSIVE" /home/hippo/projects/Energy_trade_inspection/src/lib/server/repository.ts` — 输出 ≥2（两次 CTE）
3. `cd /home/hippo/projects/Energy_trade_inspection && npm run type-check` — 零错误
4. 手动测试（可选）：若本地数据库运行，执行 `node -e "const {getNetworkGraph} = require('./src/lib/server/repository'); getNetworkGraph('test-id').then(r => console.log(r.nodes.length, r.edges.length, r.truncated))"` 确认函数可调用
</verification>

<success_criteria>
- getNetworkGraph(entityId) 函数实现完成并导出
- WITH RECURSIVE CTE 包含 depth<3 + visited[] + LIMIT 100 三重安全终止
- 返回值匹配 NetworkGraphResult 接口（nodes、edges、truncated、totalNodeCount）
- 所有 SQL 使用参数化查询（$1 占位符）
- npm run type-check 零错误通过
- Plan 03（NetworkGraph 组件 + page.tsx 集成）可以开始实现
</success_criteria>

<output>
完成后，创建 `.planning/phases/10-network-graph-core/10-02-SUMMARY.md`，记录：
- getNetworkGraph() 函数在 repository.ts 中的行号范围
- 三次 db.query() 调用的 SQL 摘要（用于后续调试参考）
- type-check 通过状态
- 任何实现中与 RESEARCH.md 不同之处（如 CTE 结构调整）
</output>
