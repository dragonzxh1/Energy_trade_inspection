---
phase: 10-network-graph-core
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/server/repository.ts
autonomous: true
requirements:
  - GRAPH-01
  - GRAPH-02
  - GRAPH-03
  - GRAPH-04

must_haves:
  truths:
    - "@xyflow/react 和 @dagrejs/dagre 已安装到 node_modules"
    - "repository.ts 中导出 NetworkNode、NetworkEdge、NetworkGraphResult 三个接口"
    - "npm run type-check 零错误通过"
  artifacts:
    - path: "package.json"
      provides: "@xyflow/react 和 @dagrejs/dagre 依赖项"
      contains: "@xyflow/react"
    - path: "src/lib/server/repository.ts"
      provides: "NetworkNode、NetworkEdge、NetworkGraphResult 类型接口"
      exports: ["NetworkNode", "NetworkEdge", "NetworkGraphResult"]
  key_links:
    - from: "src/components/entity/NetworkGraph.tsx (Wave 2)"
      to: "src/lib/server/repository.ts"
      via: "import type { NetworkNode, NetworkEdge } from '@/lib/server/repository'"
      pattern: "export interface NetworkNode"
    - from: "src/app/company/[slug]/page.tsx (Wave 2)"
      to: "src/lib/server/repository.ts"
      via: "import type { NetworkGraphResult } from '@/lib/server/repository'"
      pattern: "export interface NetworkGraphResult"
---

<objective>
安装 React Flow 可视化依赖，并在 repository.ts 中定义 Phase 10 的核心类型接口。

Purpose: Wave 0 任务，建立后续波次的类型合同——NetworkGraph 客户端组件（Wave 2）和 getNetworkGraph() 函数（Wave 1）均依赖这些接口。类型先行可防止 Wave 1/2 并行开发时产生类型不兼容。

Output: 安装完成的 @xyflow/react + @dagrejs/dagre；repository.ts 中新增三个导出接口（NetworkNode、NetworkEdge、NetworkGraphResult）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-network-graph-core/10-CONTEXT.md
@.planning/phases/10-network-graph-core/10-RESEARCH.md
@.planning/phases/10-network-graph-core/10-PATTERNS.md
@.planning/phases/10-network-graph-core/10-UI-SPEC.md

<interfaces>
<!-- 现有 repository.ts 中 ICIJ 接口模式（类型定义遵循此风格）-->
从 src/lib/server/repository.ts (lines 998–1012):
```typescript
export interface IcijMatch {
  nodeId: string
  name: string
  dataset: string
  entityType: string | null
  countries: string | null
  jurisdiction: string | null
  status: string | null
  incorporationDate: string | null
  address: string | null
  sourceUrl: string | null
  matchConfidence: number
  isSanctioned?: boolean
  sanctionsMatch?: string | null
}
```

从 src/lib/types.ts (lines 79–91):
```typescript
export interface Director {
  id: string
  name: string
  role: string
  nationality?: string
  appointedDate?: string
}

export interface VesselRef {
  imo: string
  name: string
  flag: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 安装 @xyflow/react 和 @dagrejs/dagre</name>
  <read_first>
    - /home/hippo/projects/Energy_trade_inspection/package.json — 确认当前依赖列表，验证这两个包不存在
  </read_first>
  <files>package.json, package-lock.json</files>
  <action>
在项目根目录（/home/hippo/projects/Energy_trade_inspection）执行：

```bash
cd /home/hippo/projects/Energy_trade_inspection
npm install @xyflow/react @dagrejs/dagre
```

安装后验证 package.json 中出现：
- `"@xyflow/react": "^12.10.2"` 或更高版本（在 dependencies 中）
- `"@dagrejs/dagre": "^3.0.0"` 或更高版本（在 dependencies 中）

注意：@dagrejs/dagre 自带 TypeScript 类型（./dist/types/index.d.ts），无需额外安装 @types 包。
不要在 globals.css 或 layout.tsx 中导入 @xyflow/react/dist/style.css——该导入将在 Wave 2 的 NetworkGraph.tsx 中进行（作用域限定）。
  </action>
  <verify>
    <automated>cd /home/hippo/projects/Energy_trade_inspection && node -e "require('@xyflow/react'); console.log('ok')" 2>/dev/null || grep -l "@xyflow/react" package.json</automated>
  </verify>
  <acceptance_criteria>
    - package.json 中 dependencies 包含 `"@xyflow/react"` 键
    - package.json 中 dependencies 包含 `"@dagrejs/dagre"` 键
    - node_modules/@xyflow/react 目录存在：`ls /home/hippo/projects/Energy_trade_inspection/node_modules/@xyflow/react 2>/dev/null | head -3` 有输出
    - node_modules/@dagrejs/dagre 目录存在：`ls /home/hippo/projects/Energy_trade_inspection/node_modules/@dagrejs/dagre 2>/dev/null | head -3` 有输出
  </acceptance_criteria>
  <done>两个包安装成功，package.json 已更新，node_modules 目录中存在对应包</done>
</task>

<task type="auto">
  <name>Task 2: 在 repository.ts 中定义 NetworkNode、NetworkEdge、NetworkGraphResult 接口</name>
  <read_first>
    - /home/hippo/projects/Energy_trade_inspection/src/lib/server/repository.ts — 必读全文，确认在 getIcijOfficerNetwork() 函数后（约 line 1097）找到插入位置，并理解已有接口的 JSDoc 和 camelCase 字段命名规范
    - /home/hippo/projects/Energy_trade_inspection/src/lib/types.ts — 确认 Director、VesselRef 类型字段，以避免类型冲突
    - /home/hippo/projects/Energy_trade_inspection/.planning/phases/10-network-graph-core/10-UI-SPEC.md — 核实节点颜色类型枚举值
  </read_first>
  <files>src/lib/server/repository.ts</files>
  <action>
在 `getIcijOfficerNetwork()` 函数结束后（约 line 1097），在 `// ── ICIJ: person search & person-entity links` 注释块之前，插入以下三个接口定义：

```typescript
// ── Network Graph (Phase 10) ──────────────────────────────────────────────────

/**
 * A single node in the company network graph.
 * Covers three categories: ETI registry connections (directors/vessels, first-layer)
 * and ICIJ offshore entities (recursive, up to 3 hops).
 */
export interface NetworkNode {
  /** Unique node identifier. For ETI nodes: prefixed "eti-{id}". For ICIJ nodes: icij_entities.node_id */
  id: string
  /** Visual node type — controls shape/size in the React Flow renderer */
  type: 'root' | 'company' | 'vessel' | 'person' | 'icij'
  /** Truncated display label (≤20 chars, trailing … if longer) */
  label: string
  /** Full untruncated name (used for native title tooltip) */
  fullName: string
  /** Navigation key: company slug or vessel IMO. null = non-clickable node */
  etlKey: string | null
  /** Color category — computed server-side from sanction status and fraud alerts */
  nodeColor: 'root' | 'sanctioned' | 'fraud' | 'icij' | 'normal'
  /** One-line human-readable subtype shown below the label e.g. "Director", "Vessel", "Offshore Entity" */
  subtype: string
}

/**
 * A directed edge between two nodes in the network graph.
 */
export interface NetworkEdge {
  /** Unique edge identifier */
  id: string
  /** Source node id */
  source: string
  /** Target node id */
  target: string
  /** Visual style category — ETI registry connections vs. ICIJ relationships */
  edgeType: 'eti' | 'icij'
  /** Optional relationship label (e.g. "director of", "shareholder of") from icij_relationships.link */
  label?: string
}

/**
 * Return type of getNetworkGraph(). Serialized and passed as props to the
 * NetworkGraph client component.
 */
export interface NetworkGraphResult {
  nodes: NetworkNode[]
  edges: NetworkEdge[]
  /** True when ICIJ recursive query hit the 100-node cap */
  truncated: boolean
  /** Total ICIJ node count before cap was applied (for truncation banner copy) */
  totalNodeCount: number
}
```

规则：
- 接口定义只包含类型声明，不包含任何实现代码
- 所有字段使用 camelCase（与 repository.ts 现有接口一致）
- 保持 `// ── Network Graph (Phase 10) ──...` 的分隔线风格与文件其他区块一致（使用 ─ 字符，总长约 80 chars）
  </action>
  <verify>
    <automated>cd /home/hippo/projects/Energy_trade_inspection && npm run type-check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export interface NetworkNode" src/lib/server/repository.ts` 有输出，行号存在
    - `grep -n "export interface NetworkEdge" src/lib/server/repository.ts` 有输出
    - `grep -n "export interface NetworkGraphResult" src/lib/server/repository.ts` 有输出
    - `grep "nodeColor:" src/lib/server/repository.ts` 输出包含 `'root' | 'sanctioned' | 'fraud' | 'icij' | 'normal'`
    - `grep "etlKey:" src/lib/server/repository.ts` 输出包含 `string | null`
    - `npm run type-check` 退出码为 0（零错误）
  </acceptance_criteria>
  <done>三个接口（NetworkNode、NetworkEdge、NetworkGraphResult）在 repository.ts 中定义并导出，类型检查零错误</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | 第三方包安装——潜在的供应链风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-PKG | Tampering | npm install (@xyflow/react) | accept | @xyflow/react 是 xyflow/xyflow 官方 GitHub 维护的包，npm 版本锁定（package-lock.json），无已知 CVE；Phase 10 安装时验证版本号匹配 12.10.2 |
| T-10-01-TYPES | Information Disclosure | NetworkNode.etlKey | mitigate | etlKey 仅携带公开的 slug/IMO，不含敏感字段；接口本身在服务端序列化，不暴露数据库行 ID 等内部标识符 |

*本 Plan 无 SQL 执行，无 F3 内容锁逻辑——威胁面最小。*
</threat_model>

<verification>
1. `grep "@xyflow/react" /home/hippo/projects/Energy_trade_inspection/package.json` — 确认包在 dependencies 中
2. `grep "@dagrejs/dagre" /home/hippo/projects/Energy_trade_inspection/package.json` — 确认包在 dependencies 中
3. `grep -c "export interface Network" /home/hippo/projects/Energy_trade_inspection/src/lib/server/repository.ts` — 输出应为 3
4. `cd /home/hippo/projects/Energy_trade_inspection && npm run type-check` — 零错误
</verification>

<success_criteria>
- @xyflow/react 和 @dagrejs/dagre 安装完成，package.json 已更新
- NetworkNode、NetworkEdge、NetworkGraphResult 三个接口已在 repository.ts 中定义并导出
- npm run type-check 零错误通过
- Wave 1（Plan 02）和 Wave 2（Plan 03）可以 import 这些类型开始实现
</success_criteria>

<output>
完成后，创建 `.planning/phases/10-network-graph-core/10-01-SUMMARY.md`，记录：
- 安装的包版本（@xyflow/react 和 @dagrejs/dagre 实际安装版本号）
- NetworkNode/NetworkEdge/NetworkGraphResult 接口定义在 repository.ts 中的行号
- type-check 通过状态
</output>
