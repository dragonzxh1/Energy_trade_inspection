---
phase: 10-network-graph-core
plan: 03
type: execute
wave: 2
depends_on:
  - "10-01"
  - "10-02"
files_modified:
  - src/components/entity/NetworkGraph.tsx
  - src/app/company/[slug]/page.tsx
autonomous: true
requirements:
  - GRAPH-01
  - GRAPH-02
  - GRAPH-04

must_haves:
  truths:
    - "src/components/entity/NetworkGraph.tsx 文件存在，以 'use client' 开头"
    - "公司详情页 tabs 数组中 'network' tab 位于 index 7（offshore 之后）"
    - "NetworkGraph 组件在 page.tsx 中被 ContentLock 包裹（F3 锁定）"
    - "page.tsx 中条件调用 getNetworkGraph()：f3Unlocked ? await getNetworkGraph(company.id) : {nodes:[], edges:[], truncated:false, totalNodeCount:0}"
    - "npm run build 通过（零编译错误）"
  artifacts:
    - path: "src/components/entity/NetworkGraph.tsx"
      provides: "React Flow 交互式节点图谱客户端组件"
      contains: "'use client'"
    - path: "src/app/company/[slug]/page.tsx"
      provides: "Network tab 插入 + ContentLock + getNetworkGraph() 调用"
      contains: "{ id: 'network', label: 'Network' }"
  key_links:
    - from: "NetworkGraph.tsx"
      to: "@xyflow/react"
      via: "import { ReactFlow, Controls, Background } from '@xyflow/react'"
      pattern: "from '@xyflow/react'"
    - from: "page.tsx"
      to: "NetworkGraph component"
      via: "<NetworkGraph nodes={...} edges={...} truncated={...} totalNodeCount={...} />"
      pattern: "import NetworkGraph from"
    - from: "page.tsx"
      to: "getNetworkGraph()"
      via: "f3Unlocked ? await getNetworkGraph(company.id) : fallback"
      pattern: "getNetworkGraph"
---

<objective>
创建 NetworkGraph.tsx 客户端组件（React Flow + Dagre 布局），并将其集成到公司详情页的 "Network" tab（位于 "Offshore Leaks" 之后，F3 内容锁定）。

Purpose: GRAPH-01（交互式图谱）、GRAPH-02（点击节点导航）、GRAPH-04（颜色编码）的 UI 实现层。数据由 Plan 02 的 getNetworkGraph() 服务端获取，通过 Server Component props 传入。

Output: 两个文件——新建的 NetworkGraph.tsx 客户端组件 + 修改的 page.tsx（tab 插入 + 数据获取 + ContentLock 包裹）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-network-graph-core/10-CONTEXT.md
@.planning/phases/10-network-graph-core/10-RESEARCH.md
@.planning/phases/10-network-graph-core/10-PATTERNS.md
@.planning/phases/10-network-graph-core/10-UI-SPEC.md
@.planning/phases/10-network-graph-core/10-01-SUMMARY.md
@.planning/phases/10-network-graph-core/10-02-SUMMARY.md

<interfaces>
<!-- NetworkNode / NetworkEdge / NetworkGraphResult 来自 Plan 01 的类型定义 -->
从 src/lib/server/repository.ts (Plan 01 插入):
```typescript
export interface NetworkNode {
  id: string
  type: 'root' | 'company' | 'vessel' | 'person' | 'icij'
  label: string        // ≤20 chars, trailing … if longer
  fullName: string
  etlKey: string | null
  nodeColor: 'root' | 'sanctioned' | 'fraud' | 'icij' | 'normal'
  subtype: string
}

export interface NetworkEdge {
  id: string
  source: string
  target: string
  edgeType: 'eti' | 'icij'
  label?: string
}

export interface NetworkGraphResult {
  nodes: NetworkNode[]
  edges: NetworkEdge[]
  truncated: boolean
  totalNodeCount: number
}
```

<!-- FraudAlertsPanel card/sectionTitle 模式（必须复用，不要重新发明）-->
从 src/components/entity/FraudAlertsPanel.tsx (lines 23–37):
```typescript
const card: React.CSSProperties = {
  backgroundColor: 'var(--bg-surface)',
  borderRadius:    '10px',
  padding:         'var(--space-5)',
  border:          '1px solid var(--border-subtle)',
}

const sectionTitle: React.CSSProperties = {
  color:          'var(--text-muted)',
  fontSize:       '11px',
  fontWeight:     600,
  letterSpacing:  '0.08em',
  textTransform:  'uppercase',
  marginBottom:   'var(--space-4)',
}
```

<!-- page.tsx 中 F3 权限变量（已存在，不需要重新计算）-->
从 src/app/company/[slug]/page.tsx (lines 757–758):
```typescript
const f3Unlocked = !!session?.user && plan !== 'free'
const lockReason = !session?.user ? 'guest' : 'free'
```

<!-- page.tsx 现有 import 行（line 12-13，需要扩展）-->
```typescript
import { getEntityByKey, getIcijMatches, getIcijOfficerNetwork, getCompanyFraudAlerts } from '@/lib/server/repository'
import type { IcijMatch, IcijOfficerLink } from '@/lib/server/repository'
```

<!-- page.tsx 现有 tabs 数组（lines 791–802，需要在 'offshore' 后插入 'network'）-->
```typescript
const tabs = [
  { id: 'registration',       label: 'Registration' },
  { id: 'directors',          label: 'Directors' },
  { id: 'beneficial-owners',  label: 'Beneficial Owners' },
  { id: 'vessels',            label: 'Vessels' },
  { id: 'flags',              label: 'Risk Flags' },
  { id: 'fraud-alerts',       label: 'Fraud Alerts' },
  { id: 'offshore',           label: 'Offshore Leaks' },
  // 在此插入 network
  { id: 'intelligence',       label: 'Intelligence' },
  { id: 'domain',             label: 'Domain' },
  { id: 'sources',            label: 'Sources' },
]
```

<!-- UI-SPEC 节点颜色规格（固定值，必须精确匹配）-->
节点颜色（来自 10-UI-SPEC.md）:
- root:      fill=rgba(94,106,210,0.25)  border=#5e6ad2 2px  label=#f7f8f8
- sanctioned: fill=rgba(239,68,68,0.18)  border=#ef4444 1.5px  label=#ef4444
- fraud:      fill=rgba(249,115,22,0.15)  border=#f97316 1.5px  label=#f97316
- icij:       fill=rgba(138,143,152,0.12) border=rgba(138,143,152,0.4) 1px  label=#8a8f98
- normal:     fill=rgba(94,106,210,0.15)  border=rgba(94,106,210,0.5) 1px  label=#d0d6e0

节点尺寸（来自 10-UI-SPEC.md）:
- root:   160×48px, borderRadius=10px
- company: 140×40px, borderRadius=8px
- vessel:  140×40px, borderRadius=8px
- person:  120×36px, borderRadius=18px (pill)
- icij:    130×36px, borderRadius=6px

边颜色（来自 10-UI-SPEC.md）:
- eti:  stroke=rgba(255,255,255,0.12)  strokeWidth=1.5  solid
- icij: stroke=rgba(138,143,152,0.25)  strokeWidth=1    dashed(4 4)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 NetworkGraph.tsx 客户端组件</name>
  <read_first>
    - /home/hippo/projects/Energy_trade_inspection/src/components/entity/FraudAlertsPanel.tsx — 必读全文，复制 card、sectionTitle 样式对象，参考空状态 pattern
    - /home/hippo/projects/Energy_trade_inspection/src/components/entity/AisPanel.tsx — 读前 20 行，确认 'use client' + useRouter 导入模式
    - /home/hippo/projects/Energy_trade_inspection/.planning/phases/10-network-graph-core/10-RESEARCH.md — 参考 Pattern 2（Dagre 布局函数）、Pattern 3（自定义节点组件）、Anti-Patterns（SSR 陷阱）
    - /home/hippo/projects/Energy_trade_inspection/.planning/phases/10-network-graph-core/10-UI-SPEC.md — 节点颜色规格、交互规格、画布规格（canvas height=640px, fitView, minZoom=0.3, maxZoom=2.0）
  </read_first>
  <files>src/components/entity/NetworkGraph.tsx</files>
  <action>
创建新文件 src/components/entity/NetworkGraph.tsx，内容如下（完整实现）：

```typescript
'use client'

import { useMemo } from 'react'
import { useRouter } from 'next/navigation'
import {
  ReactFlow,
  Controls,
  Background,
  BackgroundVariant,
  useNodesState,
  useEdgesState,
  Handle,
  Position,
  type Node,
  type Edge,
  type NodeProps,
} from '@xyflow/react'
import '@xyflow/react/dist/style.css'
import dagre from '@dagrejs/dagre'
import type { NetworkNode, NetworkEdge } from '@/lib/server/repository'

// ── Props ────────────────────────────────────────────────────────────────────

interface Props {
  nodes: NetworkNode[]
  edges: NetworkEdge[]
  truncated: boolean
  totalNodeCount: number
}

// ── Node dimensions (must match Dagre layout values exactly) ─────────────────

const NODE_WIDTH: Record<string, number> = {
  root: 160, company: 140, vessel: 140, person: 120, icij: 130,
}
const NODE_HEIGHT: Record<string, number> = {
  root: 48, company: 40, vessel: 40, person: 36, icij: 36,
}
const NODE_RADIUS: Record<string, number> = {
  root: 10, company: 8, vessel: 8, person: 18, icij: 6,
}

// ── Node colors (from 10-UI-SPEC.md — fixed values, do not modify) ───────────

const NODE_STYLES: Record<string, React.CSSProperties> = {
  root: {
    backgroundColor: 'rgba(94,106,210,0.25)',
    border:          '2px solid #5e6ad2',
    color:           '#f7f8f8',
  },
  sanctioned: {
    backgroundColor: 'rgba(239,68,68,0.18)',
    border:          '1.5px solid #ef4444',
    color:           '#ef4444',
  },
  fraud: {
    backgroundColor: 'rgba(249,115,22,0.15)',
    border:          '1.5px solid #f97316',
    color:           '#f97316',
  },
  icij: {
    backgroundColor: 'rgba(138,143,152,0.12)',
    border:          '1px solid rgba(138,143,152,0.4)',
    color:           '#8a8f98',
  },
  normal: {
    backgroundColor: 'rgba(94,106,210,0.15)',
    border:          '1px solid rgba(94,106,210,0.5)',
    color:           '#d0d6e0',
  },
}

// ── Shared panel styles (copied from FraudAlertsPanel.tsx) ───────────────────

const card: React.CSSProperties = {
  backgroundColor: 'var(--bg-surface)',
  borderRadius:    '10px',
  padding:         'var(--space-5)',
  border:          '1px solid var(--border-subtle)',
}

const sectionTitle: React.CSSProperties = {
  color:          'var(--text-muted)',
  fontSize:       '11px',
  fontWeight:     600,
  letterSpacing:  '0.08em',
  textTransform:  'uppercase',
  marginBottom:   'var(--space-4)',
}

// ── Dagre layout (LR direction, node separation from 10-UI-SPEC.md) ──────────

function applyDagreLayout(rfNodes: Node[], rfEdges: Edge[]): Node[] {
  const g = new dagre.graphlib.Graph()
  g.setDefaultEdgeLabel(() => ({}))
  // LR = left-to-right; nodesep=60 (vertical gap), ranksep=80 (horizontal gap)
  g.setGraph({ rankdir: 'LR', nodesep: 60, ranksep: 80 })

  rfNodes.forEach((node) => {
    const w = NODE_WIDTH[node.type ?? 'company'] ?? 140
    const h = NODE_HEIGHT[node.type ?? 'company'] ?? 40
    g.setNode(node.id, { width: w, height: h })
  })
  rfEdges.forEach((edge) => g.setEdge(edge.source, edge.target))

  dagre.layout(g)

  return rfNodes.map((node) => {
    const pos = g.node(node.id)
    const w = NODE_WIDTH[node.type ?? 'company'] ?? 140
    const h = NODE_HEIGHT[node.type ?? 'company'] ?? 40
    return {
      ...node,
      targetPosition: Position.Left,
      sourcePosition: Position.Right,
      position: { x: pos.x - w / 2, y: pos.y - h / 2 },
    }
  })
}

// ── Custom Node Component ─────────────────────────────────────────────────────

interface ETINodeData {
  label: string
  fullName: string
  etlKey: string | null
  nodeColor: NetworkNode['nodeColor']
  subtype: string
  nodeType: NetworkNode['type']
}

function ETINode({ data, type }: NodeProps) {
  const router = useRouter()
  const nodeData = data as unknown as ETINodeData
  const clickable = !!nodeData.etlKey
  const colorStyle = NODE_STYLES[nodeData.nodeColor] ?? NODE_STYLES.normal
  const radius = NODE_RADIUS[type ?? 'company'] ?? 8
  const w = NODE_WIDTH[type ?? 'company'] ?? 140
  const h = NODE_HEIGHT[type ?? 'company'] ?? 40

  function handleClick() {
    if (!nodeData.etlKey) return
    if (type === 'vessel') {
      router.push(`/vessel/${nodeData.etlKey}`)
    } else {
      router.push(`/company/${nodeData.etlKey}`)
    }
  }

  return (
    <div
      role={clickable ? 'button' : undefined}
      aria-label={clickable ? `${nodeData.fullName} \u2014 click to view entity` : undefined}
      title={nodeData.fullName}
      onClick={handleClick}
      style={{
        width:          w,
        height:         h,
        borderRadius:   radius,
        display:        'flex',
        flexDirection:  'column',
        alignItems:     'center',
        justifyContent: 'center',
        cursor:         clickable ? 'pointer' : 'default',
        padding:        '2px 6px',
        transition:     'filter 0.1s ease, border-color 0.1s ease',
        ...colorStyle,
      }}
      onMouseEnter={(e) => {
        if (clickable) {
          ;(e.currentTarget as HTMLDivElement).style.filter = 'brightness(1.15)'
        }
      }}
      onMouseLeave={(e) => {
        ;(e.currentTarget as HTMLDivElement).style.filter = 'none'
      }}
    >
      <Handle type="target" position={Position.Left} style={{ opacity: 0 }} />
      <span
        style={{
          fontSize:     '13px',
          fontWeight:   type === 'root' ? 600 : 400,
          lineHeight:   '18px',
          color:        colorStyle.color,
          maxWidth:     '100%',
          overflow:     'hidden',
          textOverflow: 'ellipsis',
          whiteSpace:   'nowrap',
        }}
      >
        {nodeData.label}
      </span>
      <span
        style={{
          fontSize:   '11px',
          lineHeight: '14px',
          color:      'var(--text-muted)',
          marginTop:  '1px',
        }}
      >
        {nodeData.subtype}
      </span>
      <Handle type="source" position={Position.Right} style={{ opacity: 0 }} />
    </div>
  )
}

// Register all node types — each maps to the same ETINode renderer
const nodeTypes = {
  root:    ETINode,
  company: ETINode,
  vessel:  ETINode,
  person:  ETINode,
  icij:    ETINode,
}

// ── Main Component ────────────────────────────────────────────────────────────

export default function NetworkGraph({ nodes, edges, truncated, totalNodeCount }: Props) {
  // Empty state: only root node (or no nodes at all)
  if (nodes.length <= 1) {
    return (
      <div style={card}>
        <p style={sectionTitle}>Network Graph</p>
        <div
          style={{
            height:          640,
            display:         'flex',
            alignItems:      'center',
            justifyContent:  'center',
          }}
        >
          <div style={{ textAlign: 'center', padding: 'var(--space-8) 0' }}>
            <p style={{ fontSize: '14px', fontWeight: 600, color: 'var(--text-primary)' }}>
              No network connections found
            </p>
            <p
              style={{
                fontSize:   '13px',
                color:      'var(--text-muted)',
                lineHeight: '20px',
                marginTop:  '8px',
                maxWidth:   360,
              }}
            >
              This entity has no director records, vessel associations, or ICIJ offshore matches on file.
            </p>
          </div>
        </div>
      </div>
    )
  }

  // Convert NetworkNode[] + NetworkEdge[] to React Flow format and apply Dagre layout
  const { layoutedNodes, rfEdges } = useMemo(() => {
    const rfNodes: Node[] = nodes.map((n) => ({
      id:       n.id,
      type:     n.type,
      data:     {
        label:     n.label,
        fullName:  n.fullName,
        etlKey:    n.etlKey,
        nodeColor: n.nodeColor,
        subtype:   n.subtype,
        nodeType:  n.type,
      },
      position: { x: 0, y: 0 }, // overwritten by Dagre
    }))

    const rfEdgesBuilt: Edge[] = edges.map((e) => ({
      id:     e.id,
      source: e.source,
      target: e.target,
      label:  e.label,
      style:
        e.edgeType === 'icij'
          ? {
              stroke:           'rgba(138,143,152,0.25)',
              strokeDasharray:  '4 4',
              strokeWidth:      1,
              strokeLinecap:    'round' as const,
            }
          : {
              stroke:      'rgba(255,255,255,0.12)',
              strokeWidth: 1.5,
              strokeLinecap: 'round' as const,
            },
    }))

    const layoutedNodes = applyDagreLayout(rfNodes, rfEdgesBuilt)
    return { layoutedNodes, rfEdges: rfEdgesBuilt }
  }, [nodes, edges])

  const [rfNodes, , onNodesChange] = useNodesState(layoutedNodes)
  const [rfEdgesState, , onEdgesChange] = useEdgesState(rfEdges)

  // Screen-reader summary (accessibility fallback)
  const directorCount = nodes.filter((n) => n.type === 'person').length
  const vesselCount   = nodes.filter((n) => n.type === 'vessel').length
  const icijCount     = nodes.filter((n) => n.type === 'icij').length
  const rootName      = nodes.find((n) => n.type === 'root')?.fullName ?? 'this entity'

  return (
    <div style={card}>
      <p style={sectionTitle}>Network Graph</p>

      {/* Screen-reader accessible summary */}
      <p className="sr-only">
        Network graph showing {rootName}&apos;s ownership and director connections.
        {directorCount > 0 && ` ${directorCount} director${directorCount > 1 ? 's' : ''}.`}
        {vesselCount > 0 && ` ${vesselCount} vessel${vesselCount > 1 ? 's' : ''}.`}
        {icijCount > 0 && ` ${icijCount} ICIJ offshore entit${icijCount > 1 ? 'ies' : 'y'}.`}
      </p>

      <div
        role="img"
        aria-label={`Network graph showing ${rootName}'s ownership and director connections`}
        style={{
          position:     'relative',
          height:       640,
          borderRadius: 10,
          overflow:     'hidden',
          border:       '1px solid var(--border-subtle)',
        }}
      >
        {/* Truncation banner — shown when ICIJ query hit 100-node cap */}
        {truncated && (
          <div
            style={{
              position:     'absolute',
              top:          0,
              left:         0,
              right:        0,
              zIndex:       10,
              background:   'rgba(245,158,11,0.1)',
              borderBottom: '1px solid rgba(245,158,11,0.2)',
              padding:      '8px 16px',
              fontSize:     12,
              color:        '#f59e0b',
            }}
          >
            Showing 100 of {totalNodeCount} network nodes \u2014 graph truncated for performance.
          </div>
        )}

        <ReactFlow
          nodes={rfNodes}
          edges={rfEdgesState}
          onNodesChange={onNodesChange}
          onEdgesChange={onEdgesChange}
          nodeTypes={nodeTypes}
          fitView
          fitViewOptions={{ padding: 0.15 }}
          minZoom={0.3}
          maxZoom={2.0}
          proOptions={{ hideAttribution: false }}
        >
          <Background
            variant={BackgroundVariant.Dots}
            color="rgba(255,255,255,0.04)"
            gap={20}
            size={1}
          />
          <Controls position="bottom-left" />
        </ReactFlow>

        {/* Scoped CSS overrides — React Flow dark theme adaptation */}
        <style>{`
          .react-flow__background { background-color: var(--bg-surface); }
          .react-flow__controls { background: var(--bg-elevated); border: 1px solid var(--border-subtle); border-radius: 6px; box-shadow: none; }
          .react-flow__controls-button { background: transparent; border-bottom: 1px solid var(--border-subtle); color: var(--text-muted); }
          .react-flow__controls-button:hover { background: var(--bg-subtle); color: var(--text-primary); }
          .react-flow__controls-button:last-child { border-bottom: none; }
          .react-flow__edge-path { stroke-linecap: round; }
          .react-flow__node { outline: none; }
          .react-flow__node:focus-visible { outline: 2px solid var(--accent-primary); outline-offset: 2px; }
          .react-flow__attribution a { color: var(--text-muted) !important; }
        `}</style>
      </div>

      {/* Panel footnote */}
      <p
        style={{
          color:      'var(--text-muted)',
          fontSize:   '11px',
          lineHeight: '16px',
          marginTop:  'var(--space-4)',
        }}
      >
        Relationship data sourced from ICIJ Offshore Leaks Database and ETI corporate registries.
        Graph shows up to 3 hops of ownership and directorship connections.
      </p>
    </div>
  )
}
```

**关键实现规则：**
1. 文件第一行必须是 `'use client'`——React Flow 使用浏览器 DOM API，SSR 会崩溃。
2. `@xyflow/react/dist/style.css` 必须在此文件顶部导入（不在 globals.css 或 layout.tsx 中）。
3. CSS 覆盖使用 `<style>` 标签内联到组件中（不写入 globals.css）——作用域限定。
4. Dagre 布局：rankdir='LR'（左右方向），nodesep=60，ranksep=80（来自 10-UI-SPEC.md）。
5. NODE_WIDTH/NODE_HEIGHT/NODE_RADIUS 数值必须与 UI-SPEC 精确匹配（root:160×48、vessel/company:140×40、person:120×36、icij:130×36）。
6. 节点颜色值必须与 UI-SPEC 精确匹配（rgba 值固定，不使用 CSS var——避免颜色值不确定性）。
7. 点击导航（GRAPH-02）：vessel 节点→ `/vessel/{etlKey}`，其他节点→ `/company/{etlKey}`。
8. 空状态触发条件：`nodes.length <= 1`（仅有 root 节点或无节点）。
9. useMemo 依赖项：`[nodes, edges]`——确保 props 变化时重新计算布局。
  </action>
  <verify>
    <automated>cd /home/hippo/projects/Energy_trade_inspection && npm run type-check 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 src/components/entity/NetworkGraph.tsx` 输出为 `'use client'`
    - `grep "from '@xyflow/react'" src/components/entity/NetworkGraph.tsx` 有输出
    - `grep "@xyflow/react/dist/style.css" src/components/entity/NetworkGraph.tsx` 有输出
    - `grep "from '@dagrejs/dagre'" src/components/entity/NetworkGraph.tsx` 有输出
    - `grep "import type { NetworkNode, NetworkEdge } from '@/lib/server/repository'" src/components/entity/NetworkGraph.tsx` 有输出
    - `grep "rankdir: 'LR'" src/components/entity/NetworkGraph.tsx` 有输出
    - `grep "fitView" src/components/entity/NetworkGraph.tsx` 有输出
    - `grep "router.push" src/components/entity/NetworkGraph.tsx` 有输出
    - `grep "rgba(239,68,68,0.18)" src/components/entity/NetworkGraph.tsx` 有输出（制裁红色）
    - `grep "rgba(249,115,22,0.15)" src/components/entity/NetworkGraph.tsx` 有输出（欺诈橙色）
    - `npm run type-check` 退出码为 0
  </acceptance_criteria>
  <done>NetworkGraph.tsx 创建完成，包含 React Flow 渲染、Dagre 布局、节点颜色、点击导航、空状态、截断横幅，类型检查零错误</done>
</task>

<task type="auto">
  <name>Task 2: 修改 page.tsx — 插入 Network tab + ContentLock + getNetworkGraph() 调用</name>
  <read_first>
    - /home/hippo/projects/Energy_trade_inspection/src/app/company/[slug]/page.tsx — 必读全文，确认：①第 12-13 行 import 语句位置（扩展 getNetworkGraph 和 NetworkGraph）；②第 762-768 行 Promise.all 结构（getNetworkGraph 独立调用，不加入此 Promise.all）；③第 791-801 行 tabs 数组（在 offshore 后插入 network）；④第 821-826 行 offshore panel（在此之后插入 network panel）
    - /home/hippo/projects/Energy_trade_inspection/src/components/entity/ContentLock.tsx — 读前 30 行，确认 ContentLock 接受的 props 类型（unlocked, reason）
    - /home/hippo/projects/Energy_trade_inspection/.planning/phases/10-network-graph-core/10-PATTERNS.md — 确认 tab 插入模式和 ContentLock 包裹模式（self-analog section）
  </read_first>
  <files>src/app/company/[slug]/page.tsx</files>
  <action>
对 src/app/company/[slug]/page.tsx 进行以下四处精确修改：

**修改 1：扩展 import 语句（约 line 12-13）**

将现有：
```typescript
import { getEntityByKey, getIcijMatches, getIcijOfficerNetwork, getCompanyFraudAlerts } from '@/lib/server/repository'
import type { IcijMatch, IcijOfficerLink } from '@/lib/server/repository'
import FraudAlertsPanel from '@/components/entity/FraudAlertsPanel'
```

替换为：
```typescript
import { getEntityByKey, getIcijMatches, getIcijOfficerNetwork, getCompanyFraudAlerts, getNetworkGraph } from '@/lib/server/repository'
import type { IcijMatch, IcijOfficerLink } from '@/lib/server/repository'
import FraudAlertsPanel from '@/components/entity/FraudAlertsPanel'
import NetworkGraph from '@/components/entity/NetworkGraph'
```

**修改 2：在 Promise.all 之后（约 line 769），独立调用 getNetworkGraph()**

在 `isWatching = watchlistRows` 这一行之后（约 line 770），插入：

```typescript
  // Network graph data (WITH RECURSIVE CTE — called separately to avoid blocking faster queries)
  // Per T-10-F3-SKIP-01: skip expensive CTE when user lacks F3 access
  const networkGraph = f3Unlocked
    ? await getNetworkGraph(company.id)
    : { nodes: [], edges: [], truncated: false as const, totalNodeCount: 0 }
```

**修改 3：在 tabs 数组（约 line 791-802）中，在 'offshore' 之后插入 'network'**

将：
```typescript
  const tabs = [
    { id: 'registration',       label: 'Registration' },
    { id: 'directors',          label: 'Directors' },
    { id: 'beneficial-owners',  label: 'Beneficial Owners' },
    { id: 'vessels',            label: 'Vessels' },
    { id: 'flags',              label: 'Risk Flags' },
    { id: 'fraud-alerts',       label: 'Fraud Alerts' },
    { id: 'offshore',           label: 'Offshore Leaks' },
    { id: 'intelligence',       label: 'Intelligence' },
    { id: 'domain',             label: 'Domain' },
    { id: 'sources',            label: 'Sources' },
  ]
```

替换为：
```typescript
  const tabs = [
    { id: 'registration',       label: 'Registration' },
    { id: 'directors',          label: 'Directors' },
    { id: 'beneficial-owners',  label: 'Beneficial Owners' },
    { id: 'vessels',            label: 'Vessels' },
    { id: 'flags',              label: 'Risk Flags' },
    { id: 'fraud-alerts',       label: 'Fraud Alerts' },
    { id: 'offshore',           label: 'Offshore Leaks' },
    { id: 'network',            label: 'Network' },
    { id: 'intelligence',       label: 'Intelligence' },
    { id: 'domain',             label: 'Domain' },
    { id: 'sources',            label: 'Sources' },
  ]
```

**修改 4：在 panels 数组（约 line 804-834）中，在 offshore ContentLock 之后插入 network panel**

在现有的：
```typescript
    <ContentLock key="offshore" unlocked={f3Unlocked} reason={lockReason}>
      <div style={{ display: 'flex', flexDirection: 'column', gap: 'var(--space-4)' }}>
        <OffshoreLeaksPanel matches={icijMatches} />
        <IcijOfficerNetworkPanel links={icijOfficerLinks} />
      </div>
    </ContentLock>,
```

之后（逗号后），插入：
```typescript
    <ContentLock key="network" unlocked={f3Unlocked} reason={lockReason}>
      <NetworkGraph
        nodes={networkGraph.nodes}
        edges={networkGraph.edges}
        truncated={networkGraph.truncated}
        totalNodeCount={networkGraph.totalNodeCount}
      />
    </ContentLock>,
```

**修改规则（手术式修改）：**
- 只做以上四处修改，不改变其他任何代码
- 保持原有代码风格（缩进、引号、换行）
- `key="network"` 的 key prop 与 tab id 必须一致
- panels 数组中 network panel 的索引必须与 tabs 数组中 'network' 的索引对应（均为 index 7）
- 不修改 f3Unlocked 和 lockReason 的计算逻辑（已存在，直接复用）
  </action>
  <verify>
    <automated>cd /home/hippo/projects/Energy_trade_inspection && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "import NetworkGraph from '@/components/entity/NetworkGraph'" src/app/company/[slug]/page.tsx` 有输出
    - `grep "getNetworkGraph" src/app/company/[slug]/page.tsx | wc -l` 输出 ≥2（import + 调用）
    - `grep "{ id: 'network'" src/app/company/[slug]/page.tsx` 有输出
    - `grep "key=\"network\"" src/app/company/[slug]/page.tsx` 有输出（ContentLock key）
    - `grep "f3Unlocked" src/app/company/[slug]/page.tsx | grep "getNetworkGraph"` 有输出（条件调用存在）
    - `grep -A3 "id: 'offshore'" src/app/company/[slug]/page.tsx | grep "network"` 验证 network 紧跟 offshore 之后
    - `npm run build` 退出码为 0，零编译错误
  </acceptance_criteria>
  <done>page.tsx 修改完成：Network tab 在 tabs/panels 数组中位于 index 7（offshore 之后），getNetworkGraph() 条件调用，ContentLock F3 包裹，npm run build 通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NetworkGraph 客户端组件 → router.push() | etlKey 由服务端序列化传入，客户端只执行导航——无用户可控输入注入导航路径 |
| page.tsx Server Component → ContentLock | F3 权限由服务端计算（f3Unlocked），客户端渲染层通过 ContentLock 模糊锁定 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-F3-01 | Elevation of Privilege | NetworkGraph tab panel (F3 content) | mitigate | 双重保护：①page.tsx 中 `f3Unlocked ? await getNetworkGraph(...) : { nodes:[], ... }` 避免数据库查询；②`<ContentLock key="network" unlocked={f3Unlocked}>` 在渲染层模糊输出。与 Directors、Fraud Alerts 等现有 F3 tab 保持相同安全模式 |
| T-10-REDIRECT-01 | Tampering (开放重定向) | ETINode onClick → router.push(etlKey) | mitigate | etlKey 由服务端 getNetworkGraph() 序列化传入——仅包含数据库中已有的 slug（公司）或 IMO（船舶）；导航路径硬编码为 `/company/{etlKey}` 或 `/vessel/{etlKey}`，不接受任意 URL |
| T-10-XSS-01 | Cross-Site Scripting | node label/fullName 渲染 | accept | 所有字符串值通过 React JSX 渲染（React 自动转义 HTML），不使用 dangerouslySetInnerHTML；网络图数据来自服务端数据库，非用户提交内容 |
| T-10-CSS-SCOPE | Tampering (全局 CSS 污染) | React Flow CSS 覆盖 | mitigate | 覆盖 CSS 通过 `<style>` 标签限定在 NetworkGraph 组件内（`.react-flow__*` 选择器不进入 globals.css）；已在 Anti-Patterns 中明确禁止全局 CSS 注入 |
</threat_model>

<verification>
1. `head -1 /home/hippo/projects/Energy_trade_inspection/src/components/entity/NetworkGraph.tsx` — 输出 `'use client'`
2. `grep -c "{ id: 'network'" /home/hippo/projects/Energy_trade_inspection/src/app/company/\[slug\]/page.tsx` — 输出 1
3. `grep "key=\"network\"" /home/hippo/projects/Energy_trade_inspection/src/app/company/\[slug\]/page.tsx` — 有输出
4. `cd /home/hippo/projects/Energy_trade_inspection && npm run build` — 零错误（包括 React Flow SSR、Dagre 类型、NetworkGraphResult 类型兼容）
</verification>

<success_criteria>
- NetworkGraph.tsx 创建完成（'use client'，React Flow + Dagre，颜色编码，点击导航，空状态，截断横幅）
- page.tsx 修改完成（Network tab 在 index 7，getNetworkGraph() 条件调用，ContentLock F3 包裹）
- npm run build 零错误通过
- 所有 GRAPH-01（图谱渲染）、GRAPH-02（点击导航）、GRAPH-04（颜色编码）要求的代码实现完成
</success_criteria>

<output>
完成后，创建 `.planning/phases/10-network-graph-core/10-03-SUMMARY.md`，记录：
- NetworkGraph.tsx 创建情况（文件大小、主要结构）
- page.tsx 修改的四处位置（行号）
- npm run build 结果
- 任何 Dagre 布局或 React Flow API 使用中遇到的意外情况
</output>
