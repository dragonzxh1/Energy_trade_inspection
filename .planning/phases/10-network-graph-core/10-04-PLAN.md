---
phase: 10-network-graph-core
plan: 04
type: execute
wave: 3
depends_on:
  - "10-03"
files_modified: []
autonomous: false
requirements:
  - GRAPH-01
  - GRAPH-02
  - GRAPH-03
  - GRAPH-04

must_haves:
  truths:
    - "公司详情页 Network tab 可见并可点击（付费账户）"
    - "图谱渲染出节点和边（非空状态公司）"
    - "颜色编码正确：制裁红、欺诈橙、ICIJ 灰、正常蓝"
    - "点击蓝色 company/vessel 节点跳转到对应详情页"
    - "免费账户看到 ContentLock 锁定状态（模糊/升级 CTA）"
  artifacts:
    - path: "src/components/entity/NetworkGraph.tsx"
      provides: "已验证的 React Flow 交互图谱组件"
    - path: "src/app/company/[slug]/page.tsx"
      provides: "已验证的 Network tab 集成"
  key_links:
    - from: "浏览器"
      to: "NetworkGraph 组件"
      via: "点击 Network tab → 图谱渲染"
      pattern: "visual verification"
---

<objective>
对 Phase 10 实现进行手动视觉验证和功能验证，确认 GRAPH-01/02/03/04 全部要求满足。

Purpose: 无自动化测试框架（STATE.md 记录技术债务），图谱渲染、点击导航、颜色编码、F3 内容锁无法通过 type-check/build 完全验证——需要人眼确认。

Output: 用户确认图谱视觉和功能正确，或反馈具体问题供修复。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-network-graph-core/10-CONTEXT.md
@.planning/phases/10-network-graph-core/10-UI-SPEC.md
@.planning/phases/10-network-graph-core/10-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 启动开发服务器并输出验证清单</name>
  <read_first>
    - /home/hippo/projects/Energy_trade_inspection/.planning/phases/10-network-graph-core/10-03-SUMMARY.md — 确认 Wave 2 构建通过
  </read_first>
  <files></files>
  <action>
执行以下步骤，为手动验证做准备：

1. 确认开发服务器可以启动：
```bash
cd /home/hippo/projects/Energy_trade_inspection
npm run build 2>&1 | tail -5
```

2. 若 build 通过，输出验证指令给用户：
```
开发服务器地址: http://localhost:3000
启动命令: npm run dev
```

3. 输出以下验证清单，供用户操作：

**功能验证清单（按顺序执行）：**

[ ] 1. GRAPH-01 图谱渲染
   - 访问任意公司详情页（付费账户登录）
   - 点击 "Network" tab（应位于 "Offshore Leaks" 之后）
   - 确认图谱画布渲染（640px 高度，带点状背景，左下角 Controls 控件）
   - 确认根节点居中显示（深蓝色边框，较大尺寸）

[ ] 2. GRAPH-04 颜色编码
   - 正常 director 节点：蓝色填充（rgba(94,106,210,0.15)），胶囊形状
   - 关联船舶节点：蓝色填充，矩形
   - ICIJ 离岸实体节点：灰色填充（rgba(138,143,152,0.12)），轻微方角
   - 若有制裁实体：红色填充（rgba(239,68,68,0.18)）
   - 若有欺诈预警实体：橙色填充（rgba(249,115,22,0.15)）

[ ] 3. GRAPH-02 点击导航
   - 点击有 etlKey 的蓝色 company 节点 → 确认跳转到 /company/{slug}
   - 点击有 etlKey 的 vessel 节点 → 确认跳转到 /vessel/{imo}
   - 点击 director person 节点（灰/蓝胶囊）→ 确认无导航（无 etlKey）

[ ] 4. GRAPH-03 3跳遍历验证（若有 ICIJ 数据的公司）
   - 选择已知有 ICIJ 匹配的公司
   - 确认 ICIJ 灰色节点存在于图谱中
   - 若节点数超过 100，确认顶部显示截断横幅："Showing 100 of {N} network nodes — graph truncated for performance."

[ ] 5. 空状态验证
   - 访问无 directors、无 vessels、无 ICIJ 数据的公司
   - 确认 Network tab 仍然可见（D-10：始终显示）
   - 确认显示空状态文字："No network connections found"

[ ] 6. F3 内容锁验证
   - 使用免费账户或未登录访问同一公司页面
   - 点击 Network tab
   - 确认看到 ContentLock 模糊效果和升级 CTA（不是图谱内容）

[ ] 7. 交互验证
   - 滚动缩放图谱（鼠标滚轮）
   - 拖拽平移图谱
   - 点击 Controls（左下角）的 fit view 按钮

如有任何问题，请描述具体现象（哪个节点颜色错误、哪个点击无效等）。
  </action>
  <verify>
    <automated>cd /home/hippo/projects/Energy_trade_inspection && npm run build 2>&1 | grep -E "error|Error|✓|Compiled" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` 退出码为 0（前置条件）
    - 验证清单输出完整，用户可按步骤操作
  </acceptance_criteria>
  <done>开发服务器就绪，验证清单已输出给用户</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 10 完整实现：
    - @xyflow/react + @dagrejs/dagre 安装（Plan 01）
    - getNetworkGraph() WITH RECURSIVE CTE 数据层（Plan 02）
    - NetworkGraph.tsx React Flow 客户端组件（Plan 03）
    - company/[slug]/page.tsx Network tab 集成，F3 ContentLock（Plan 03）
  </what-built>
  <how-to-verify>
    在浏览器中验证以下内容（需本地开发服务器运行：npm run dev）：

    **必须通过的检查项：**

    1. **Network tab 存在**：公司详情页 tab 栏包含 "Network" tab，位于 "Offshore Leaks" 之后

    2. **图谱渲染**（付费账户）：
       - 点击 Network tab 显示图谱画布（高约 640px）
       - 根节点（公司自身）显示为深蓝色边框矩形，居中偏左
       - 左下角有 React Flow Controls 控件（缩放 +/-、fit 按钮）
       - 画布背景为深色 + 细点阵

    3. **节点颜色**：
       - Director 节点：半透明蓝色填充，胶囊形（border-radius 18px）
       - ICIJ 节点：半透明灰色填充，略方形
       - 制裁节点（若有）：半透明红色填充，红色边框和标签

    4. **点击导航**：
       - 点击有 etlKey 的节点（vessel/company）→ 页面跳转到对应实体页
       - 点击 director/ICIJ 节点 → 无跳转

    5. **F3 锁定**：
       - 免费账户/未登录 → Network tab 显示锁定/升级 CTA 而非图谱

    6. **空状态**：
       - 无网络连接的公司 → 显示 "No network connections found" 文字（非错误）
  </how-to-verify>
  <resume-signal>
    输入 "approved" 确认所有检查通过，或描述具体问题（例如："颜色错误 - vessel 节点显示灰色而非蓝色"、"点击无反应 - company 节点无导航"）以便修复。
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 验证阶段 | 无新代码，仅运行已有实现——无新威胁面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-VERIFY | — | 手动验证 checkpoint | accept | 本 Plan 无新代码实现，威胁模型继承自 Plan 01-03。验证阶段确认既有安全措施（F3 内容锁、参数化查询）在运行时行为符合预期 |
</threat_model>

<verification>
手动验证——见 checkpoint 任务中的 how-to-verify 清单。

自动化前置验证（Task 1）：
```bash
cd /home/hippo/projects/Energy_trade_inspection && npm run build
```
构建成功（退出码 0）= Wave 3 自动化门控通过。
</verification>

<success_criteria>
- npm run build 零错误（Wave 3 入场条件）
- 用户确认以下 6 项通过：
  1. Network tab 可见且位于正确位置（offshore 之后）
  2. 图谱画布渲染（根节点 + 子节点 + 边）
  3. 颜色编码正确（蓝/红/橙/灰，按 UI-SPEC）
  4. 点击导航正确（有 etlKey 的节点跳转，无 etlKey 的节点不跳转）
  5. F3 内容锁生效（免费账户看到锁定状态）
  6. 空状态正确渲染（"No network connections found"）

全部通过 → Phase 10 验证完成，可开始 Phase 11 规划。
</success_criteria>

<output>
完成后，创建 `.planning/phases/10-network-graph-core/10-04-SUMMARY.md`，记录：
- 手动验证结果（各检查项通过/失败状态）
- 用户反馈的具体问题（若有）
- Phase 10 完成状态
- 为 Phase 11（ICIJ 面板扩展 + PDF SVG 导出）准备的注意事项
</output>
