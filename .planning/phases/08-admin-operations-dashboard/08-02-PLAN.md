---
phase: 08-admin-operations-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/app/api/admin/users/route.ts
  - src/app/api/admin/users/[id]/plan/route.ts
  - src/app/api/admin/stats/route.ts
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
  - ADMIN-04

must_haves:
  truths:
    - "GET /api/admin/users returns 403 for non-admin authenticated users"
    - "GET /api/admin/users returns 401 for unauthenticated requests"
    - "PATCH /api/admin/users/[id]/plan returns 400 for invalid plan values"
    - "PATCH /api/admin/users/[id]/plan returns 400 for plan='professional'"
    - "GET /api/admin/stats returns totalUsers, planDistribution, newToday, new30Days, dailyRegistrations"
    - "All three routes export runtime = 'nodejs'"
  artifacts:
    - path: "src/app/api/admin/users/route.ts"
      provides: "GET /api/admin/users — admin-gated user list"
      exports: ["GET", "runtime"]
    - path: "src/app/api/admin/users/[id]/plan/route.ts"
      provides: "PATCH /api/admin/users/[id]/plan — plan mutation"
      exports: ["PATCH", "runtime"]
    - path: "src/app/api/admin/stats/route.ts"
      provides: "GET /api/admin/stats — platform stats aggregation"
      exports: ["GET", "runtime"]
  key_links:
    - from: "src/app/api/admin/users/route.ts GET"
      to: "src/lib/server/repository.ts getAdminUsers()"
      via: "import { getAdminUsers } from '@/lib/server/repository'"
      pattern: "getAdminUsers"
    - from: "src/app/api/admin/users/[id]/plan/route.ts PATCH"
      to: "users table"
      via: "db.query('UPDATE users SET plan = $1 WHERE id = $2')"
      pattern: "UPDATE users SET plan"
    - from: "src/app/api/admin/stats/route.ts GET"
      to: "src/lib/server/repository.ts getAdminStats()"
      via: "import { getAdminStats } from '@/lib/server/repository'"
      pattern: "getAdminStats"
---

<objective>
Create the three admin API routes that power the dashboard's data access and plan mutation. All routes use the shared isAdminAuthorized() helper from Plan 01 and call repository functions rather than inlining SQL.

Purpose: The /admin page (Wave 3) calls these routes for client-side refresh; the page itself fetches data directly server-side.
Output: Three route files under src/app/api/admin/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-admin-operations-dashboard/08-RESEARCH.md
@.planning/phases/08-admin-operations-dashboard/08-PATTERNS.md
@.planning/phases/08-admin-operations-dashboard/08-01-SUMMARY.md

<interfaces>
<!-- Contracts created in Plan 01 that these routes import. -->

From src/lib/server/admin-auth.ts (created in Plan 01):
```typescript
export interface AuthResult {
  authorized: boolean
  reason: 'no_session' | 'bearer_valid' | 'admin_email' | 'not_admin'
}
export function isAdminAuthorized(req: NextRequest, userEmail?: string | null): AuthResult
```

From src/lib/server/repository.ts (created in Plan 01):
```typescript
export interface UserAdminRow {
  id: string; email: string; plan: string
  created_at: string; last_active_at: string | null
  quota_used: number; quota_limit: number
}
export interface AdminStats {
  totalUsers: number
  planDistribution: { free: number; starter: number; enterprise: number; professional: number }
  newToday: number; new30Days: number
  dailyRegistrations: Array<{ date: string; count: number }>
}
export async function getAdminUsers(): Promise<UserAdminRow[]>
export async function getAdminStats(): Promise<AdminStats>
```

Auth gate pattern (copy for all three routes):
```typescript
const session = await auth()
const authResult = isAdminAuthorized(req, session?.user?.email)
if (!authResult.authorized) {
  const status = authResult.reason === 'no_session' ? 401 : 403
  return NextResponse.json(
    { error: authResult.reason === 'no_session' ? 'Authentication required.' : 'Forbidden.' },
    { status }
  )
}
```

Plan validation in PATCH:
```typescript
const ALLOWED_PLANS = ['free', 'starter', 'enterprise'] as const
type AllowedPlan = typeof ALLOWED_PLANS[number]
// Check: if (!ALLOWED_PLANS.includes(plan as AllowedPlan)) return 400
```

Next.js 15 dynamic route params (async Promise):
```typescript
{ params }: { params: Promise<{ id: string }> }
const { id } = await params
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GET /api/admin/users and GET /api/admin/stats routes</name>
  <files>
    src/app/api/admin/users/route.ts
    src/app/api/admin/stats/route.ts
  </files>
  <read_first>
    - src/app/api/admin/sync/route.ts (full file — auth gate pattern, error handling pattern, runtime export)
    - src/lib/server/admin-auth.ts (verify isAdminAuthorized export from Plan 01)
    - src/lib/server/repository.ts (last ~50 lines — verify getAdminUsers, getAdminStats exports from Plan 01)
  </read_first>
  <action>
**File 1: src/app/api/admin/users/route.ts** (create directory src/app/api/admin/users/ first)

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { auth } from '@/auth'
import { isAdminAuthorized } from '@/lib/server/admin-auth'
import { getAdminUsers } from '@/lib/server/repository'

export const runtime = 'nodejs'

export async function GET(req: NextRequest) {
  const session = await auth()
  const authResult = isAdminAuthorized(req, session?.user?.email)
  if (!authResult.authorized) {
    const status = authResult.reason === 'no_session' ? 401 : 403
    return NextResponse.json(
      { error: authResult.reason === 'no_session' ? 'Authentication required.' : 'Forbidden.' },
      { status }
    )
  }

  try {
    const users = await getAdminUsers()
    return NextResponse.json({ users }, {
      headers: { 'Cache-Control': 'no-store' },
    })
  } catch (error: unknown) {
    const message = error instanceof Error ? error.message : String(error)
    return NextResponse.json({ error: message }, { status: 500 })
  }
}
```

**File 2: src/app/api/admin/stats/route.ts** (create directory src/app/api/admin/stats/ first)

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { auth } from '@/auth'
import { isAdminAuthorized } from '@/lib/server/admin-auth'
import { getAdminStats } from '@/lib/server/repository'

export const runtime = 'nodejs'

export async function GET(req: NextRequest) {
  const session = await auth()
  const authResult = isAdminAuthorized(req, session?.user?.email)
  if (!authResult.authorized) {
    const status = authResult.reason === 'no_session' ? 401 : 403
    return NextResponse.json(
      { error: authResult.reason === 'no_session' ? 'Authentication required.' : 'Forbidden.' },
      { status }
    )
  }

  try {
    const stats = await getAdminStats()
    return NextResponse.json(stats, {
      headers: { 'Cache-Control': 'no-store' },
    })
  } catch (error: unknown) {
    const message = error instanceof Error ? error.message : String(error)
    return NextResponse.json({ error: message }, { status: 500 })
  }
}
```
  </action>
  <verify>
    <automated>grep -n "isAdminAuthorized" /home/hippo/projects/Energy_trade_inspection/src/app/api/admin/users/route.ts && grep -n "isAdminAuthorized" /home/hippo/projects/Energy_trade_inspection/src/app/api/admin/stats/route.ts && grep -n "runtime = 'nodejs'" /home/hippo/projects/Energy_trade_inspection/src/app/api/admin/users/route.ts && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export const runtime = 'nodejs'" src/app/api/admin/users/route.ts` returns a match
    - `grep "export const runtime = 'nodejs'" src/app/api/admin/stats/route.ts` returns a match
    - `grep "isAdminAuthorized" src/app/api/admin/users/route.ts` returns a match
    - `grep "isAdminAuthorized" src/app/api/admin/stats/route.ts` returns a match
    - `grep "getAdminUsers" src/app/api/admin/users/route.ts` returns a match
    - `grep "getAdminStats" src/app/api/admin/stats/route.ts` returns a match
    - `grep "Cache-Control.*no-store" src/app/api/admin/users/route.ts` returns a match
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>Two GET routes exist with admin auth gate, nodejs runtime, no-store cache header, and repository function calls.</done>
</task>

<task type="auto">
  <name>Task 2: PATCH /api/admin/users/[id]/plan route</name>
  <files>
    src/app/api/admin/users/[id]/plan/route.ts
  </files>
  <read_first>
    - src/app/api/admin/users/route.ts (just created — verify isAdminAuthorized import path)
    - src/app/api/stripe/webhook/route.ts (UPDATE users SET plan pattern — verify exact SQL)
    - src/lib/server/db.ts (verify db import path)
  </read_first>
  <action>
Create directory structure: src/app/api/admin/users/[id]/plan/

**File: src/app/api/admin/users/[id]/plan/route.ts**

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { auth } from '@/auth'
import { db } from '@/lib/server/db'
import { isAdminAuthorized } from '@/lib/server/admin-auth'

export const runtime = 'nodejs'

const ALLOWED_PLANS = ['free', 'starter', 'enterprise'] as const
type AllowedPlan = typeof ALLOWED_PLANS[number]

export async function PATCH(
  req: NextRequest,
  { params }: { params: Promise<{ id: string }> }
) {
  const session = await auth()
  const authResult = isAdminAuthorized(req, session?.user?.email)
  if (!authResult.authorized) {
    const status = authResult.reason === 'no_session' ? 401 : 403
    return NextResponse.json(
      { error: authResult.reason === 'no_session' ? 'Authentication required.' : 'Forbidden.' },
      { status }
    )
  }

  const { id } = await params

  let plan: string
  try {
    const body = await req.json()
    plan = String(body.plan ?? '')
  } catch {
    return NextResponse.json({ error: 'Invalid request body.' }, { status: 400 })
  }

  if (!ALLOWED_PLANS.includes(plan as AllowedPlan)) {
    return NextResponse.json({ error: 'Invalid plan value.' }, { status: 400 })
  }

  try {
    await db.query(
      'UPDATE users SET plan = $1 WHERE id = $2',
      [plan, id]
    )
    return NextResponse.json({ id, plan })
  } catch (error: unknown) {
    const message = error instanceof Error ? error.message : String(error)
    return NextResponse.json({ error: message }, { status: 500 })
  }
}
```

Key constraints:
- ALLOWED_PLANS does NOT include 'professional' — this is intentional per UI-SPEC and RESEARCH.md Pitfall 4
- params is awaited (Next.js 15 App Router requires `await params`)
- UPDATE uses parameterized query `$1, $2` — no string interpolation
  </action>
  <verify>
    <automated>grep -n "ALLOWED_PLANS\|UPDATE users SET plan\|await params\|runtime" /home/hippo/projects/Energy_trade_inspection/src/app/api/admin/users/\[id\]/plan/route.ts && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export const runtime = 'nodejs'" src/app/api/admin/users/\[id\]/plan/route.ts` returns a match
    - `grep "ALLOWED_PLANS = \['free', 'starter', 'enterprise'\]" src/app/api/admin/users/\[id\]/plan/route.ts` returns a match (or equivalent const)
    - `grep "professional" src/app/api/admin/users/\[id\]/plan/route.ts` returns no match (professional NOT in allowed list)
    - `grep "UPDATE users SET plan" src/app/api/admin/users/\[id\]/plan/route.ts` returns a match
    - `grep "await params" src/app/api/admin/users/\[id\]/plan/route.ts` returns a match
    - `grep "isAdminAuthorized" src/app/api/admin/users/\[id\]/plan/route.ts` returns a match
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>PATCH route exists with plan validation allowlist (excludes professional), parameterized SQL, async params, admin gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → /api/admin/users | Any authenticated user can reach handler — admin role check is inside handler |
| HTTP client → /api/admin/users/[id]/plan PATCH body | Untrusted plan value arrives in request body |
| PATCH id parameter → DB UPDATE | URL path parameter used as SQL param — must be parameterized |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Elevation of Privilege | GET /api/admin/users | mitigate | isAdminAuthorized() returns 403 for non-admin sessions; middleware provides 401 for missing session |
| T-08-02-02 | Tampering | PATCH plan body | mitigate | Server-side ALLOWED_PLANS allowlist validation before UPDATE; rejects 'professional' and arbitrary strings with 400 |
| T-08-02-03 | Tampering | PATCH id path param | mitigate | Parameterized query `UPDATE users SET plan = $1 WHERE id = $2` — no SQL injection possible |
| T-08-02-04 | Information Disclosure | GET /api/admin/users response (emails) | mitigate | Admin-only gate (403 non-admins); Cache-Control: no-store prevents CDN caching of PII |
| T-08-02-05 | Information Disclosure | GET /api/admin/stats response | mitigate | Admin-only gate; no-store cache header |
</threat_model>

<verification>
After both tasks complete:
- `npm run type-check` exits 0
- `grep -rn "isAdminAuthorized" src/app/api/admin/` returns 3 matches (users, stats, plan routes)
- `grep -rn "runtime = 'nodejs'" src/app/api/admin/users/route.ts src/app/api/admin/stats/route.ts src/app/api/admin/users/\[id\]/plan/route.ts` returns 3 matches
- `grep "professional" src/app/api/admin/users/\[id\]/plan/route.ts` returns no match
</verification>

<success_criteria>
- Three route files exist under src/app/api/admin/
- All three export runtime = 'nodejs'
- All three call isAdminAuthorized() and return 401/403 appropriately
- PATCH route validates plan against ['free', 'starter', 'enterprise'] only — rejects 'professional' and arbitrary strings with 400
- PATCH route awaits params (Next.js 15 App Router pattern)
- npm run type-check passes with 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-operations-dashboard/08-02-SUMMARY.md`
</output>
