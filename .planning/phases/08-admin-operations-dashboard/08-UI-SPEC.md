---
phase: 8
slug: admin-operations-dashboard
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-15
---

# Phase 8 — UI Design Contract: Admin Operations Dashboard

> Visual and interaction contract for the admin dashboard phase.
> Generated by gsd-ui-researcher. Consumed by gsd-planner, gsd-executor, gsd-ui-auditor.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — custom CSS only (project-wide decision; no shadcn, no component library) |
| Preset | not applicable |
| Component library | none — inline styles on all React components, CSS variables from `src/styles/globals.css` |
| Icon library | none — text labels and Unicode glyphs used throughout codebase (consistent with existing pattern) |
| Font | Inter via `var(--font-sans)` for UI; JetBrains Mono via `var(--font-mono)` for log lines, timestamps, quota numbers |

Source: `src/styles/globals.css` tokens + codebase-wide component inspection.

---

## Spacing Scale

Declared values — all from existing `--space-*` CSS variables in `src/styles/globals.css`:

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px (`var(--space-1)`) | Icon gaps, badge padding, tight inline separators |
| sm | 8px (`var(--space-2)`) | Compact element spacing, button internal padding |
| md | 16px (`var(--space-4)`) | Default element spacing, card content horizontal padding |
| lg | 24px (`var(--space-6)`) | Section padding, card vertical padding |
| xl | 32px (`var(--space-8)`) | Gap between dashboard sections |
| 2xl | 48px (`var(--space-12)`) | Page-level top/bottom padding |
| 3xl | 64px (`var(--space-16)`) | Not used in this phase — admin is data-dense, not marketing |

Exceptions:
- Table row height: `padding: var(--space-3) var(--space-5)` — matches watchlist page pattern exactly (12px/20px)
- Plan change dropdown: minimum touch target 36px tall (accessibility minimum for admin-only desktop tool)
- Stat card internal layout: `var(--space-5)` padding (20px) — matches account page card pattern

---

## Typography

Source: `src/styles/globals.css` declared globals — do not deviate.

| Role | Size | Weight | Line Height | Notes |
|------|------|--------|-------------|-------|
| Body / table cell | 13px | 400 | 20px (1.54) | Primary data text in rows — matches watchlist pattern |
| Label / column header | 11px | 600 | 16px (1.45) | Uppercase, `letter-spacing: 0.06em` — matches all existing table headers |
| Section heading | 20px | 600 (`590`) | 28px (1.4) | Page-level `<h1>` — matches account/watchlist pages |
| Subsection heading | 16px | 600 | 22px (1.375) | Section `<h2>` inside dashboard — matches watchlist "Watched Trades" heading |
| Stat number | 28px | 600 (`590`) | 36px (1.29) | Large numeric displays in stat cards (user count, etc.) |
| Monospace data | 13px (`var(--font-mono)`) | 400 | 20px | Log error messages, sync timestamps, sync duration ms |
| Metadata / timestamps | 12px | 400 | 16px | Secondary metadata, "last active", "registered date" |

All body text uses `font-feature-settings: "cv01", "ss03"` — inherited from `body` in globals.css; do not override.

---

## Color

Source: CSS variables in `src/styles/globals.css`. No new color values introduced in this phase.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `var(--bg-primary)` `#08090a` | Page background — full canvas |
| Secondary (30%) | `var(--bg-surface)` `#0f1011` + `var(--bg-elevated)` `#191a1b` | Cards, table container, column header rows |
| Accent (10%) | `var(--accent-primary)` `#5e6ad2` | See reserved list below |
| Success / sync OK | `var(--status-clear)` `#10b981` | Sync job "success" status badge |
| Failure / sync error | `var(--status-listed)` `#ef4444` | Sync job "failed" status badge, error message text |
| Warning / quota near limit | `var(--risk-medium)` `#f59e0b` | User quota above 80% |
| Border | `var(--border-subtle)` `rgba(255,255,255,0.05)` | All card and row separators |
| Border solid | `var(--border-solid)` `#23252a` | Table outer border |

**Accent reserved for (exhaustive list):**
- "Change Plan" confirm button (primary action)
- Active tab underline in dashboard tab nav
- Plan badge border + text in user list (matches header plan badge pattern)
- Links to external data (sync source documentation links, if any)

**No new semantic colors may be introduced.** Use existing `--status-*`, `--risk-*`, `--accent-*` tokens only.

---

## Page Structure and Layout

The admin dashboard lives at `/admin`. It uses the same `<Header>` component as all other pages.
The dashboard uses a tab-nav pattern (matching `TabNav` component) to switch between three sections.

### Route: `/admin`

Layout: `maxWidth: var(--max-width)` centered, `padding: var(--space-12) var(--space-4)`.

Three tabs (rendered with `TabNav` component):

| Tab ID | Label | Requirement |
|--------|-------|-------------|
| `sync` | Sync History | ADMIN-01 |
| `users` | Users | ADMIN-02, ADMIN-03 |
| `stats` | Platform Stats | ADMIN-04 |

Access gate: if user is not admin, render a 403 message panel in place of content — do not redirect. Message: "You do not have permission to access this page. Contact the platform administrator." No links. No visual decoration.

---

## Component Inventory

### 1. SyncJobTable (ADMIN-01)

Displays the log of all sync job runs. Columns:

| Column | Width | Content |
|--------|-------|---------|
| Source | `1fr` | Sync source name — plain text, e.g. "ofac", "warninglists" |
| Status | `80px` | Badge: "SUCCESS" in `var(--status-clear)` / "FAILED" in `var(--status-listed)` / "RUNNING" in `var(--risk-medium)` |
| Records | `90px` | Record count — mono font, right-aligned |
| Duration | `80px` | Duration in ms or seconds — mono font, right-aligned |
| Time | `140px` | UTC timestamp — mono font, 12px |
| Error | `1fr` | Error message if failed — `var(--text-muted)`, italic, 12px, truncated at 1 line with `title=` tooltip |

Container: `var(--bg-surface)` card, `border-radius: 12px`, `overflow: hidden`.
Column headers: `var(--bg-elevated)` background, `var(--text-muted)` text, 11px, uppercase, 0.06em tracking — matches watchlist table header exactly.
Row separator: `1px solid var(--border-subtle)`.
Last row: no separator.

Empty state: "No sync job history found. Trigger a sync from the API to populate this log." — centered, `var(--text-muted)`, 13px, `padding: var(--space-8)`.

### 2. UserTable (ADMIN-02)

Displays the full user list. Columns:

| Column | Width | Content |
|--------|-------|---------|
| Email | `1fr` | Email address — primary text, 13px, 500 weight |
| Plan | `100px` | Plan badge — border style matching header plan badge: `var(--accent-primary)` border + text, 10px, uppercase, 0.06em; free plan shows plain `var(--text-muted)` text "Free" |
| Registered | `110px` | Registration date — 12px, `var(--text-muted)`, "12 Apr 2026" format |
| Last Active | `110px` | Last active date — same format; "Never" if null |
| Quota Used | `90px` | "42 / 100" or "Unlimited" — mono font, 12px |
| Action | `120px` | Plan change control — see PlanSelector below |

Search: a plain `<input>` above the table. `placeholder="Search users by email"`. Width: 280px. Style: `var(--bg-elevated)` background, `var(--border-subtle)` border, `var(--text-primary)` text, `border-radius: 6px`, `padding: var(--space-2) var(--space-3)`, 13px. Client-side filter on email field. No debounce required (list is small).

Empty state (after search): "No users match your search." — centered, `var(--text-muted)`, 13px.

### 3. PlanSelector (ADMIN-03)

Inline plan change control rendered in the Action column of UserTable.

- A `<select>` element with options: "free", "starter", "enterprise"
- Current plan pre-selected
- On change: submits a POST to `/api/admin/users/[id]/plan` with the new plan value
- On success: row plan badge updates without full page reload (optimistic update via client state)
- On failure: show inline error text "Failed. Try again." in `var(--status-listed)`, 11px, same cell

Style: `var(--bg-elevated)` background, `var(--border-subtle)` border, `var(--text-secondary)` text, `border-radius: 4px`, `padding: 3px 6px`, 12px, `font-family: inherit`, `cursor: pointer`.

No modal confirmation is required — this is an admin-only tool. The plan change is immediate and logged server-side.

### 4. StatCards (ADMIN-04)

Four stat cards in a 2x2 grid on the Stats tab. Grid: `display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-5)`.

| Card | Metric | Detail |
|------|--------|--------|
| Total Users | Integer count | Subtext: "registered users" |
| Plan Distribution | Plan breakdown | Subtext counts: "N free · N starter · N enterprise" |
| New Today | Count | Subtext: "registrations in the last 24h" |
| New (30 days) | Count | Subtext: "registrations in the past 30 days" |

Card style: `var(--bg-surface)` background, `var(--border-subtle)` border, `border-radius: 12px`, `padding: var(--space-5)`.
Stat number: 28px, weight 590, `var(--text-primary)`.
Stat label: 11px, weight 600, uppercase, `var(--text-muted)`, `letter-spacing: 0.06em`.
Subtext: 12px, weight 400, `var(--text-muted)`.

### 5. DailyRegistrationChart (ADMIN-04)

A bar chart showing daily new registrations for the past 30 days.

Implementation constraint: No external charting library. Use CSS bars rendered from an array of `{ date, count }` data points.

Layout: `display: flex; align-items: flex-end; gap: 4px; height: 80px`.
Each bar: `flex: 1; background-color: var(--accent-primary); opacity: 0.7; border-radius: 2px 2px 0 0; min-height: 2px`.
Bar height: proportional to max count in the 30-day window. If max is 0, all bars at `min-height: 2px` with `opacity: 0.3`.
X-axis labels: show only first day of each week — "1 Apr", "8 Apr" — 10px, `var(--text-faint)`.
Hover: `opacity: 1` on hover + native `title="{date}: {count} registrations"` tooltip.

---

## Interaction Contracts

### Tab navigation

Uses existing `TabNav` component (`src/components/entity/TabNav.tsx`) with the three admin tabs. `defaultTab="sync"`. Tab switch is client-side only — all three panels are fetched server-side and passed as `panels` prop.

### Plan change (PlanSelector)

1. Admin selects new plan in `<select>`.
2. Client sends `PATCH /api/admin/users/[id]/plan` with `{ plan: "starter" }`.
3. Optimistic update: plan badge in the row updates immediately.
4. On server error (non-2xx): revert plan badge to previous value, show inline "Failed. Try again." error for 4 seconds then clear.
5. No loading spinner required — interaction is fast enough to not need one.

### Sync job refresh

A "Refresh" button above the SyncJobTable. Label: "Refresh". On click: re-fetches `/api/admin/sync` and re-renders the table. Button style: `border: 1px solid var(--border-subtle)`, `var(--text-muted)` text, same as "Sign out" button in Header. During fetch: button disabled + label changes to "Refreshing…". No spinner.

### 403 gate

If user is not admin: the full tab nav and all content is replaced by a centered message panel (no tabs visible). Message panel: `var(--bg-surface)` card, `border-radius: 12px`, `padding: var(--space-12)`, centered in the page. Text: "Access denied" as heading (16px, 600), subtext as described above (14px, `var(--text-muted)`).

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA — plan change | "Change plan" (select label in column header) |
| Sync tab heading | "Sync History" |
| Users tab heading | "Users" |
| Stats tab heading | "Platform Stats" |
| Empty state — sync log | "No sync job history found. Trigger a sync from the API to populate this log." |
| Empty state — user list (no search match) | "No users match your search." |
| Empty state — user list (no users at all) | "No users registered yet." |
| Error state — plan change failed | "Failed. Try again." |
| Error state — data load failed | "Failed to load data. Reload the page or check server logs." |
| 403 heading | "Access denied" |
| 403 body | "You do not have permission to access this page. Contact the platform administrator." |
| Sync status badge — success | "SUCCESS" |
| Sync status badge — failed | "FAILED" |
| Sync status badge — running | "RUNNING" |
| Plan selector option labels | "Free", "Starter", "Enterprise" (title case — not uppercase) |
| Refresh button default | "Refresh" |
| Refresh button loading | "Refreshing..." |
| Stat card — total users | "Total Users" (label), "registered users" (subtext) |
| Stat card — plan distribution | "Plan Distribution" (label) |
| Stat card — new today | "New Today" (label), "registrations in the last 24h" (subtext) |
| Stat card — 30-day new | "New (30 Days)" (label), "registrations in the past 30 days" (subtext) |
| Chart title | "Daily Registrations — Past 30 Days" |
| Search input placeholder | "Search users by email" |

No destructive actions are presented in this phase. Plan changes are reversible (can change back). No confirmation dialog is required.

---

## States Required per Component

| Component | States |
|-----------|--------|
| SyncJobTable | loading skeleton (3 shimmer rows), populated, empty |
| UserTable | loading skeleton (5 shimmer rows), populated, filtered-empty |
| PlanSelector | default, changed (optimistic), error (inline text) |
| StatCards | loading (shimmer rectangle per card), populated |
| DailyRegistrationChart | loading (flat placeholder bars), populated, all-zero |
| Refresh button | default, loading/disabled |
| 403 gate | single state — message panel only |

Loading skeleton pattern: rectangle divs with `background-color: var(--border-subtle)`, `border-radius: 4px`, `opacity: 0.6`, no animation (matches existing Skeleton in `IntelligencePanel.tsx`).

---

## Accessibility Contracts

- All `<table>` or grid-layout data rows use `role="row"` and `role="cell"` if not using semantic `<table>` element
- Column headers use `scope="col"` if using `<th>` elements; if using div grid, include `aria-label` on header cells
- `<select>` for plan change: `aria-label="Change plan for {user email}"` — required for screen reader context
- Tab navigation: reuse existing `TabNav` component — ARIA roles already implemented there
- Refresh button: `aria-busy="true"` when loading
- 403 panel: `role="alert"` on the message container
- All status badges use `title=` attributes for full descriptions: e.g. `title="Sync completed successfully"` on "SUCCESS" badge
- Focus management: tab navigation arrow keys already handled by `TabNav` — no additional work needed

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — project uses no shadcn | not applicable |
| third-party | none | not applicable |

No external component registry is used in this phase or anywhere in the project.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
