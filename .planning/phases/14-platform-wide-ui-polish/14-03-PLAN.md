---
phase: 14-platform-wide-ui-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 14-01
autonomous: false
files_modified:
  - src/app/page.tsx
  - src/app/account/page.tsx
requirements:
  - UI-POLISH-04
  - UI-POLISH-05

must_haves:
  truths:
    - "首页 CTA 按钮使用 micro-gradient（linear-gradient(180deg, #7578f2, #5558e8)）"
    - "首页 feature cards 使用 #111113 背景 + rgba(255,255,255,0.07) 边框 + border-radius 10px"
    - "首页 trust stats 数字颜色为 #6366f1"
    - "account 页的 Manage Billing / Upgrade Plan 按钮使用 micro-gradient"
    - "account 页的 quota 进度条使用 4px track + #6366f1 fill"
    - "account 页的 plan badge 显示为 pill（rgba(99,102,241,0.12) 背景 + #6366f1 边框）"
    - "/screen、/watchlist、/reports、/、/account 五个页面视觉上与 /trade 风格一致（视觉验证通过）"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "升级后的首页（CTA + feature cards + trust stats）"
      contains: "linear-gradient(180deg, #7578f2"
    - path: "src/app/account/page.tsx"
      provides: "升级后的账户页（micro-gradient 按钮 + pill + 进度条）"
      contains: "rgba(99,102,241,0.12)"
  key_links:
    - from: "page.tsx tool card CTA span"
      to: "primary micro-gradient"
      via: "inline style background: linear-gradient"
      pattern: "linear-gradient.*7578f2"
    - from: "account/page.tsx card const"
      to: "surface card 硬编码值"
      via: "backgroundColor: '#111113'"
      pattern: "backgroundColor: '#111113'"
---

<objective>
升级首页 page.tsx（CTA 按钮 + feature cards + trust stats）和 account/page.tsx（action 按钮 + quota 进度条 + plan badge），然后执行视觉验证 checkpoint 确认所有五个改造页面（/screen、/watchlist、/reports、/、/account）与 /trade 视觉语言一致。

Purpose: 完成 Phase 14 全部 5 个页面的视觉升级，并通过人工验证收尾。
Output: 升级后的 page.tsx 和 account/page.tsx，以及通过视觉 checkpoint 的确认。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-platform-wide-ui-polish/14-CONTEXT.md
@.planning/phases/14-platform-wide-ui-polish/14-PATTERNS.md
@.planning/phases/13-trade-check-ui-redesign/13-UI-SPEC.md
@.planning/phases/14-platform-wide-ui-polish/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: page.tsx — CTA 按钮 + feature cards + trust stats 升级</name>
  <read_first>
    - src/app/page.tsx (完整阅读全部 490 行，理解 Server Component 结构、TOOL_CARDS 渲染、TRUST_STATS 渲染、FEATURES 渲染)
    - src/app/trade/TradeClient.tsx (读取前 30 行的 TOKEN 常量)
    - .planning/phases/14-platform-wide-ui-polish/14-PATTERNS.md (page.tsx 部分 pattern 代码)
    - .planning/phases/14-platform-wide-ui-polish/14-CONTEXT.md (首页决策)
    - .planning/phases/13-trade-check-ui-redesign/13-UI-SPEC.md (Primary Button CSS 规范)
  </read_first>
  <files>src/app/page.tsx</files>
  <action>
    这是 Server Component（无 'use client'）。使用 `<style>` 标签注入 hover CSS，不使用 useState。

    **Step 1：全局常量替换 — SANCTION_COLOR 和 RISK_COLOR 硬编码**

    找到文件顶部的：
    ```typescript
    const SANCTION_COLOR: Record<SanctionStatus, string> = {
      listed:     'var(--status-listed)',
      not_listed: 'var(--status-clear)',
      unknown:    'var(--text-muted)',
    }
    const RISK_COLOR: Record<RiskLevel, string> = {
      critical: 'var(--status-listed)',
      ...
    }
    ```

    替换为硬编码值：
    ```typescript
    const SANCTION_COLOR: Record<SanctionStatus, string> = {
      listed:     '#ef4444',
      not_listed: '#22c55e',
      unknown:    '#55556a',
    }
    const RISK_COLOR: Record<RiskLevel, string> = {
      critical: '#ef4444',
      high:     '#f97316',
      medium:   '#fbbf24',
      low:      '#4ade80',
    }
    ```

    **Step 2：在主 Page 组件 return 第一个元素前注入 `<style>` 标签**

    ```tsx
    <style>{`
      .home-cta-btn:hover {
        background: linear-gradient(180deg, #818cf8 0%, #6366f1 100%) !important;
        box-shadow: 0 1px 0 rgba(255,255,255,0.12) inset, 0 4px 10px rgba(99,102,241,0.35) !important;
        transform: translateY(-1px);
      }
      .home-tool-card:hover {
        border-top-color: rgba(255,255,255,0.14) !important;
      }
    `}</style>
    ```

    **Step 3：TOOL_CARDS 渲染区域升级**

    找到 TOOL_CARDS.map 渲染区域（约第 333–377 行）。每张 tool card：

    a. 外层 card 容器 surface 升级（替换 `var(--bg-surface)` 等 CSS 变量）：
    ```typescript
    style={{
      backgroundColor: '#111113',
      border: '1px solid rgba(255,255,255,0.07)',
      borderTop: '1px solid rgba(255,255,255,0.09)',
      borderRadius: '10px',
      boxShadow: '0 2px 8px rgba(0,0,0,0.25), inset 0 1px 0 rgba(255,255,255,0.05)',
      padding: '24px',
      display: 'flex', flexDirection: 'column', gap: '12px',
      transition: 'border-top-color 0.15s ease',
    }}
    className="home-tool-card"
    ```

    b. 在 card 内部，找到 CTA `<Link>` 或 `<a>` 标签（href={card.href}），在 Link 内部添加一个 `<span>` 渲染 card.cta 文字，使用 micro-gradient button 样式：
    ```tsx
    <Link href={card.href} style={{ textDecoration: 'none' }}>
      <span
        className="home-cta-btn"
        style={{
          display: 'inline-block',
          padding: '7px 14px',
          background: 'linear-gradient(180deg, #7578f2 0%, #5558e8 100%)',
          color: '#fff',
          border: '1px solid rgba(99,102,241,0.45)',
          boxShadow: '0 1px 0 rgba(255,255,255,0.1) inset, 0 2px 5px rgba(99,102,241,0.25)',
          borderRadius: '7px',
          fontSize: '12px', fontWeight: 500,
          transition: 'all 0.12s ease',
          cursor: 'pointer',
        }}
      >
        {card.cta}
      </span>
    </Link>
    ```

    如果当前 card 结构是整张 card 都是 Link（`<Link href={card.href}>`），保留外层 Link 包装，在 Link 内部最下方添加 CTA span。

    **Step 4：TRUST_STATS 数字颜色**

    找到 TRUST_STATS.map 渲染区域（约第 395–400 行），将数字 `{stat.value}` 的颜色改为：
    ```typescript
    color: '#6366f1',
    fontSize: '22px', fontWeight: 700, letterSpacing: '-0.02em',
    ```

    **Step 5：FEATURES 卡片 surface 升级**

    找到 FEATURES.map 渲染区域（约第 424–460 行），将 feature card 外层容器替换为 TOKEN surface card：
    ```typescript
    style={{
      backgroundColor: '#111113',
      border: '1px solid rgba(255,255,255,0.07)',
      borderTop: '1px solid rgba(255,255,255,0.09)',
      borderRadius: '10px',
      boxShadow: '0 2px 8px rgba(0,0,0,0.25), inset 0 1px 0 rgba(255,255,255,0.05)',
      padding: '20px',
    }}
    ```

    **Step 6：FeaturedEntities 中的 entity card surface（如有）**

    如果 FeaturedEntities 函数中有卡片渲染（`<div>` 带 border），同样替换为 TOKEN surface card style。如果 entity 卡片已经没有明显的 CSS 变量引用，跳过此步。

    **不要做：**
    - 不修改 SearchBox 组件
    - 不改变 TOOL_CARDS / TRUST_STATS / FEATURES 数据数组内容
    - 不修改 Header 组件
    - 不修改 FeaturedEntities 的数据查询逻辑
    - 不添加 'use client'
  </action>
  <verify>
    <automated>cd /home/hippo/projects/Energy_trade_inspection && npx tsc --noEmit 2>&1 | grep "app/page" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx 包含 `linear-gradient(180deg, #7578f2 0%, #5558e8 100%)`（CTA 按钮 gradient）
    - page.tsx 包含 `backgroundColor: '#111113'`（feature card 背景）
    - page.tsx 包含 `color: '#6366f1'`（trust stats 数字颜色）
    - page.tsx 包含 `.home-cta-btn:hover`（hover CSS）
    - page.tsx 不包含 `var(--status-listed)` 或 `var(--status-clear)`（SANCTION_COLOR 已替换）
    - `npx tsc --noEmit` 无 app/page.tsx 相关类型错误
  </acceptance_criteria>
  <done>
    page.tsx 升级完成：TOOL_CARDS CTA 按钮使用 micro-gradient，feature cards 使用 TOKEN surface，trust stats 数字为 #6366f1，SANCTION_COLOR/RISK_COLOR 硬编码。
  </done>
</task>

<task type="auto">
  <name>Task 2: account/page.tsx — 按钮 + quota 进度条 + plan badge + card surface 升级</name>
  <read_first>
    - src/app/account/page.tsx (完整阅读全部 237 行，理解 card const、quota 进度条、Manage Billing 按钮、Upgrade Plan Link)
    - src/app/trade/TradeClient.tsx (读取 primaryBtnBase 的 gradient 值，约第 295–332 行)
    - .planning/phases/14-platform-wide-ui-polish/14-PATTERNS.md (account/page.tsx 部分 pattern 代码)
    - .planning/phases/14-platform-wide-ui-polish/14-CONTEXT.md (account 决策)
    - .planning/phases/13-trade-check-ui-redesign/13-UI-SPEC.md (Primary + Secondary Button CSS 规范)
  </read_first>
  <files>src/app/account/page.tsx</files>
  <action>
    这是 Server Component（无 'use client'）。

    **Step 1：card 常量升级**

    找到约第 59–64 行的 `card` CSS 常量：
    ```typescript
    const card: React.CSSProperties = {
      backgroundColor: 'var(--bg-surface)',
      border: '1px solid var(--border-subtle)',
      borderRadius: '12px',
      padding: 'var(--space-6)',
    }
    ```

    替换为：
    ```typescript
    const card: React.CSSProperties = {
      backgroundColor: '#111113',
      border: '1px solid rgba(255,255,255,0.07)',
      borderTop: '1px solid rgba(255,255,255,0.09)',
      borderRadius: '10px',
      padding: '24px',
      boxShadow: '0 2px 8px rgba(0,0,0,0.25), inset 0 1px 0 rgba(255,255,255,0.05)',
    }
    ```

    **Step 2：sectionLabel 常量升级**

    找到约第 66–73 行的 `sectionLabel`：
    ```typescript
    const sectionLabel: React.CSSProperties = {
      color: 'var(--text-muted)',
      fontSize: '11px',
      fontWeight: 600,
      letterSpacing: '0.08em',
      textTransform: 'uppercase',
      marginBottom: '4px',
    }
    ```

    替换为（对齐 Phase 13 section label 规范）：
    ```typescript
    const sectionLabel: React.CSSProperties = {
      color: '#55556a',
      fontSize: '11px',
      fontWeight: 600,
      letterSpacing: '0.07em',
      textTransform: 'uppercase',
      marginBottom: '4px',
    }
    ```

    **Step 3：Plan name 文字 + plan badge pill**

    找到约第 127–129 行渲染 `{PLAN_LABEL[plan] ?? plan}` 的位置，替换为 pill：
    ```tsx
    <span style={{
      fontSize: '12px', fontWeight: 600, letterSpacing: '0.04em',
      textTransform: 'uppercase' as const,
      color: '#6366f1',
      backgroundColor: 'rgba(99,102,241,0.12)',
      border: '1px solid rgba(99,102,241,0.3)',
      borderRadius: '4px',
      padding: '2px 8px',
    }}>
      {PLAN_LABEL[plan] ?? plan}
    </span>
    ```

    **Step 4：Manage Billing 按钮 → primary micro-gradient**

    找到约第 133–149 行的 `<form action={openBillingPortal}><button>Manage billing</button></form>`，升级 button style：
    ```typescript
    style={{
      padding: '8px 16px',
      background: 'linear-gradient(180deg, #7578f2 0%, #5558e8 100%)',
      color: '#fff',
      border: '1px solid rgba(99,102,241,0.45)',
      boxShadow: '0 1px 0 rgba(255,255,255,0.1) inset, 0 2px 5px rgba(99,102,241,0.25)',
      borderRadius: '7px',
      fontSize: '13px', fontWeight: 500,
      fontFamily: 'inherit', cursor: 'pointer',
      transition: 'all 0.12s ease',
    }}
    ```

    **Step 5：Upgrade Plan Link → primary micro-gradient**

    找到约第 150–165 行的 `<Link href="/pricing" style={{...}}>Upgrade plan</Link>`，升级为：
    ```typescript
    style={{
      display: 'inline-block',
      padding: '8px 16px',
      background: 'linear-gradient(180deg, #7578f2 0%, #5558e8 100%)',
      color: '#fff',
      border: '1px solid rgba(99,102,241,0.45)',
      boxShadow: '0 1px 0 rgba(255,255,255,0.1) inset, 0 2px 5px rgba(99,102,241,0.25)',
      borderRadius: '7px',
      fontSize: '13px', fontWeight: 500,
      textDecoration: 'none',
      transition: 'all 0.12s ease',
    }}
    ```

    **Step 6：quota progress bar 升级**

    找到约第 189–203 行的进度条 track 和 fill：

    Track（外层 div）替换：
    ```typescript
    style={{
      height: '4px',
      background: 'rgba(0,0,0,0.35)',
      borderRadius: '2px',
      boxShadow: 'inset 0 1px 2px rgba(0,0,0,0.4)',
      overflow: 'hidden',
      marginBottom: '8px',
    }}
    ```

    Fill（内层 div）替换：
    ```typescript
    style={{
      height: '100%',
      width: `${usedPercent}%`,
      background: usedPercent >= 100 ? '#ef4444' : '#6366f1',
      borderRadius: '2px',
      transition: 'width 0.3s ease',
    }}
    ```

    移除原有的 `backgroundColor: 'var(--bg-elevated)'`、`borderRadius: '999px'`（改为 2px）、`backgroundColor: usedPercent >= 100 ? 'var(--status-listed)' : 'var(--accent-primary)'`。

    **Step 7：avatar 背景色**

    找到 initials avatar（约第 101–108 行），将 `backgroundColor: 'var(--accent-primary)'` 替换为 `backgroundColor: '#6366f1'`。

    **Step 8：内联 `var(--text-secondary)` 等剩余 CSS 变量**

    全文搜索 `var(--text-secondary)` 和 `var(--text-primary)` 的 inline style 引用，替换为：
    - `var(--text-secondary)` → `'#8b8b9a'`
    - `var(--text-primary)` → `'#f1f1f3'`
    - `var(--border-subtle)` → `'rgba(255,255,255,0.07)'`
    - `var(--accent-primary)` → `'#6366f1'`

    **不要做：**
    - 不修改 openBillingPortal server action 逻辑
    - 不修改 quota 计算逻辑（getQuotaStatus）
    - 不改变页面结构（card 数量/顺序）
    - 不添加 'use client'
  </action>
  <verify>
    <automated>cd /home/hippo/projects/Energy_trade_inspection && npx tsc --noEmit 2>&1 | grep "account/page" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - account/page.tsx 包含 `backgroundColor: '#111113'`（card 升级）
    - account/page.tsx 包含 `rgba(99,102,241,0.12)`（plan badge pill 背景）
    - account/page.tsx 包含 `linear-gradient(180deg, #7578f2 0%, #5558e8 100%)`（primary 按钮）
    - account/page.tsx 包含 `background: 'rgba(0,0,0,0.35)'`（进度条 track）
    - account/page.tsx 包含 `background: usedPercent >= 100 ? '#ef4444' : '#6366f1'`（进度条 fill）
    - account/page.tsx 不包含 `var(--bg-surface)` 或 `var(--border-subtle)`（CSS 变量已替换）
    - `npx tsc --noEmit` 无 account/page.tsx 相关类型错误
  </acceptance_criteria>
  <done>
    account/page.tsx 升级完成：card surface 使用 TOKEN 硬编码，plan badge 显示为 pill，Manage Billing/Upgrade Plan 使用 micro-gradient，quota 进度条使用 4px track + #6366f1 fill，所有 CSS 变量引用替换为硬编码值。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 14 全部 5 个页面的视觉升级已完成：
    - Plan 14-01: /screen — Split Panel 布局 + TOKEN + inline 进度条 + micro-gradient 按钮
    - Plan 14-02: /watchlist — secondary button + sanction pill + 行 hover
    - Plan 14-02: /reports — GhostButton 替换 + 行 hover + RiskBadge pill
    - Plan 14-03 Task 1: / — CTA micro-gradient + feature cards surface + trust stats 颜色
    - Plan 14-03 Task 2: /account — micro-gradient 按钮 + plan badge pill + quota 进度条

    参照页面（已完成）：/trade（Phase 13，TOKEN 规范来源）
  </what-built>
  <how-to-verify>
    1. 启动开发服务器：`npm run dev`（等待 "Ready" 提示）

    2. 打开浏览器，登录后依次访问以下页面，与 /trade 对比视觉风格：

    **[A] /trade（参照标准）**
    - 确认：Split Panel 布局，左列深色背景，micro-gradient "Run Trade Check →" 按钮，进度条替代了 loading spinner

    **[B] /screen**
    - 确认：Split Panel 布局（左 420px 宽，含 drag zone；右弹性，含结果区）
    - 确认：drag zone 有虚线边框（hover 时应变紫色高亮）
    - 确认："Screen Document →" 按钮与 /trade 的 "Run Trade Check →" 外观一致（紫色 gradient）
    - 确认：上传一个 PDF 后，右侧显示进度条动画（而非 GlowLoader spinner）
    - 拒绝标准：看到 GlowLoader spinner、单列布局、蓝色按钮

    **[C] /watchlist**
    - 确认：Remove、Dismiss Alert 等按钮为深灰色（#1e1e24），非原来的蓝色/透明
    - 确认：sanction status 显示为彩色 pill（Sanctioned=红色，Not Listed=绿色，Unknown=灰色）
    - 确认：悬停列表行时有极淡的背景变化
    - 拒绝标准：按钮仍为蓝色、status 仅为纯色文字无 pill

    **[D] /reports**
    - 确认：View、PDF 按钮为深灰色 secondary button 样式（非蓝色 ghost button）
    - 确认：列表行 hover 时背景变为 #1e1e24（稍深）
    - 确认：风险 badge 为彩色 pill（背景透明度版本），而非纯色边框
    - 拒绝标准：按钮仍为蓝色描边 ghost style

    **[E] / （首页）**
    - 确认："Run trade check →" 和 "Screen a document →" 按钮为紫色 micro-gradient
    - 确认：feature cards（Sanction Screening、Authenticity Score 等）背景为 #111113 深色卡片
    - 确认：Trust Stats 数字（如 "3"、"190+"）颜色为紫色（#6366f1）
    - 拒绝标准：CTA 仍为蓝色纯色按钮，cards 背景为浅色

    **[F] /account**
    - 确认："Manage billing" 或 "Upgrade plan" 按钮为紫色 micro-gradient
    - 确认：Plan 标签（如 "STARTER"）显示为紫色 pill
    - 确认：quota 进度条为 4px 纤细形式（非原来 6px 圆角），填充色为 #6366f1
    - 拒绝标准：按钮为蓝色纯色，进度条仍为 6px 圆角
  </how-to-verify>
  <resume-signal>
    视觉验证通过后输入 "approved"。
    如有问题，描述具体页面和具体问题（例如 "/screen drag zone hover 未变色"），Claude 将修复后重新请求验证。
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→/api/stripe/checkout | account Manage Billing 通过 server action 重定向 Stripe portal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Elevation of Privilege | account openBillingPortal | accept | server action 内已有 auth() 验证，本 plan 仅改按钮样式 |
| T-14-03-02 | Information Disclosure | page.tsx FeaturedEntities | accept | 公开实体数据，无 PII；数据查询逻辑不修改 |
</threat_model>

<verification>
1. `grep -n "linear-gradient.*7578f2" src/app/page.tsx` — 返回 CTA 按钮样式行
2. `grep -n "rgba(99,102,241,0.12)" src/app/account/page.tsx` — 返回 plan badge 背景行
3. `grep -n "rgba(0,0,0,0.35)" src/app/account/page.tsx` — 返回 progress bar track 行
4. `grep -n "var(--bg-surface)" src/app/account/page.tsx` — 返回空（已替换）
5. `grep -n "color: '#6366f1'" src/app/page.tsx` — 返回 trust stats 颜色行
6. `npx tsc --noEmit` — 通过（无类型错误）
7. 视觉 checkpoint 通过（人工确认）
</verification>

<success_criteria>
- 首页 CTA 按钮显示紫色 micro-gradient，与 /trade 主按钮外观一致
- 首页 feature cards 为深色 #111113 surface（非浅色）
- 首页 trust stats 数字为品牌紫色 #6366f1
- account 页 action 按钮为 primary micro-gradient
- account 页 quota 进度条为 4px TOKEN 规范（track + indigo fill）
- account 页 plan badge 为 pill
- 视觉 checkpoint 人工验证通过：所有 5 个改造页面视觉语言与 /trade 一致
- TypeScript 严格模式无错误
</success_criteria>

<output>
完成后创建 `.planning/phases/14-platform-wide-ui-polish/14-03-SUMMARY.md`
</output>
