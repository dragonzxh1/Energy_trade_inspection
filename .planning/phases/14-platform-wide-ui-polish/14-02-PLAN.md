---
phase: 14-platform-wide-ui-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/watchlist/page.tsx
  - src/app/reports/ReportsClient.tsx
autonomous: true
requirements:
  - UI-POLISH-02
  - UI-POLISH-03

must_haves:
  truths:
    - "/watchlist 的 Refresh、Remove、Dismiss Alert 按钮使用 secondary button 样式（#1e1e24 背景）"
    - "/watchlist 的 sanction status 显示为统一 pill（rgba 背景 + colored border + colored text）"
    - "/watchlist 表格行 hover 时显示 rgba(255,255,255,0.02) 背景"
    - "/reports 的 GhostButton 已替换为 secondary button inline style"
    - "/reports 列表行 hover 时 background 变为 #1e1e24"
    - "/reports 的 RiskBadge 使用 pill 规范（rgba 背景 + colored border）"
    - "两个文件均不再引用 var(--bg-surface)、var(--border-subtle) 等 CSS 变量（改为硬编码 TOKEN 值）"
  artifacts:
    - path: "src/app/watchlist/page.tsx"
      provides: "按钮/pill/card 样式升级的 watchlist 页"
      contains: "background: '#1e1e24'"
    - path: "src/app/reports/ReportsClient.tsx"
      provides: "GhostButton 替换 + 行 hover 的 reports 客户端"
      contains: "secondaryBtnStyle"
  key_links:
    - from: "watchlist STATUS_COLOR"
      to: "pill span 样式"
      via: "backgroundColor: `${STATUS_COLOR[...]}18`"
      pattern: "STATUS_COLOR.*18"
    - from: "ReportsClient secondaryBtnStyle"
      to: "View/PDF 按钮和 Link 渲染"
      via: "style={secondaryBtnStyle}"
      pattern: "style=\\{secondaryBtnStyle\\}"
---

<objective>
升级 /watchlist 和 /reports 两个页面的视觉控件样式，使其与 Phase 13 确立的设计规范对齐。watchlist 的 action 按钮改为 secondary button，status 显示改为统一 pill；reports 的 GhostButton 替换为 secondaryBtnStyle，行 hover 和 RiskBadge 对齐 TOKEN 规范。

Purpose: 统一平台操作列表页面的视觉语言，消除遗留 CSS 变量引用和陈旧按钮样式。
Output: 升级后的 watchlist/page.tsx 和 ReportsClient.tsx，所有 action 控件对齐 Phase 13 规范。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-platform-wide-ui-polish/14-CONTEXT.md
@.planning/phases/14-platform-wide-ui-polish/14-PATTERNS.md
@.planning/phases/13-trade-check-ui-redesign/13-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: watchlist/page.tsx — 按钮/pill/surface 样式升级</name>
  <read_first>
    - src/app/watchlist/page.tsx (完整阅读全部 535 行，理解 Server Component 结构和 server actions)
    - src/app/trade/TradeClient.tsx (读取 secondaryBtnStyle 常量，约第 87–100 行)
    - .planning/phases/14-platform-wide-ui-polish/14-PATTERNS.md (watchlist 部分 pattern 代码)
    - .planning/phases/14-platform-wide-ui-polish/14-CONTEXT.md (watchlist 决策)
    - .planning/phases/13-trade-check-ui-redesign/13-UI-SPEC.md (Secondary Button CSS 规范)
  </read_first>
  <files>src/app/watchlist/page.tsx</files>
  <action>
    这是 Server Component（无 'use client'）。禁止添加 useState 和 onClick hover 处理器。hover 效果通过注入 `<style>` 标签实现。

    **Step 1：STATUS_COLOR 常量替换 CSS 变量**

    找到文件顶部的 STATUS_COLOR（约第 26–30 行），替换为硬编码值：
    ```typescript
    const STATUS_COLOR: Record<string, string> = {
      listed:     '#ef4444',
      not_listed: '#22c55e',
      unknown:    '#55556a',
    }
    ```

    **Step 2：在 Page 组件 return 内首行注入 hover `<style>` 标签**

    在 WatchlistPage（或主 export default）return 的第一个子元素之前插入：
    ```tsx
    <style>{`
      .wl-row:hover { background: rgba(255,255,255,0.02) !important; }
      .wl-btn:hover { background: #26262e !important; transform: translateY(-1px); }
    `}</style>
    ```

    **Step 3：secondary button 样式（内联对象，复用到所有 action 按钮）**

    在文件顶部常量区域（STATUS_LABEL 附近）添加：
    ```typescript
    const secondaryBtn: React.CSSProperties = {
      background: '#1e1e24',
      color: '#8b8b9a',
      border: '1px solid rgba(255,255,255,0.07)',
      borderRadius: '7px',
      padding: '6px 14px',
      fontSize: '13px',
      fontFamily: 'inherit',
      cursor: 'pointer',
      boxShadow: '0 1px 2px rgba(0,0,0,0.15)',
      transition: 'all 0.12s ease',
    }
    ```

    **Step 4：Refresh 按钮升级**

    找到 `<RefreshButton>` 和 `<WatchedTradesRefreshButton>` 组件使用位置。
    这些是独立 Client Component 文件（RefreshButton.tsx、WatchedTradesRefreshButton.tsx）。
    不修改这些组件文件——仅修改 watchlist/page.tsx 中调用时传入 className="wl-btn" 以获得 hover 效果。
    如果 RefreshButton 接受 style prop，则传入 secondaryBtn style。如果不接受 style prop，则不修改（留给组件自身样式）。

    对于页面内 `<form action={...}><button>` 形式的 Remove/Dismiss 按钮（直接在 page.tsx 渲染的），应用 secondary button 样式：
    ```tsx
    <button
      type="submit"
      className="wl-btn"
      style={secondaryBtn}
    >
      Remove
    </button>
    ```

    **Step 5：sanction status pill 替换**

    找到当前渲染 status 的位置（约第 356–370 行），将纯文本 status 替换为 pill：
    ```tsx
    <span style={{
      fontSize: '11px', fontWeight: 600, letterSpacing: '0.04em',
      textTransform: 'uppercase' as const,
      color: STATUS_COLOR[item.current_sanction_status] ?? '#55556a',
      backgroundColor: `${STATUS_COLOR[item.current_sanction_status] ?? '#55556a'}18`,
      border: `1px solid ${STATUS_COLOR[item.current_sanction_status] ?? '#55556a'}44`,
      borderRadius: '4px',
      padding: '2px 7px',
    }}>
      {STATUS_LABEL[item.current_sanction_status] ?? 'Unknown'}
    </span>
    ```

    **Step 6：表格行添加 hover className**

    找到 watchlist 列表行（`<tr>` 或 `<div>` 行容器），添加 `className="wl-row"`。对于 `<tr>` 元素同时保留现有 style，添加 `transition: 'background 0.1s ease'`。

    **Step 7：surface card 升级**

    找到使用 `var(--bg-surface)` 和 `var(--border-subtle)` 的 card/panel 容器（section 包装器），替换为 TOKEN 硬编码值：
    ```typescript
    backgroundColor: '#111113',
    border: '1px solid rgba(255,255,255,0.07)',
    borderTop: '1px solid rgba(255,255,255,0.09)',
    borderRadius: '10px',
    boxShadow: '0 2px 8px rgba(0,0,0,0.25), inset 0 1px 0 rgba(255,255,255,0.05)',
    ```
    注意：并非所有容器都需要换成 card surface——只改主 section 包装（WatchlistSection 和 WatchedTradesSection 的外层 div）。AlertsSection 的橙色背景警告框保持不变（语义颜色，不是 surface）。

    **不要做：**
    - 不添加 'use client' 到文件顶部
    - 不修改 server actions（removeFromWatchlist、dismissAlert）
    - 不改变表格结构、列顺序
    - 不修改 plan 门控逻辑（plan === 'professional' || plan === 'enterprise'）
    - 不修改 RefreshButton、WatchedTradesRefreshButton 组件文件
  </action>
  <verify>
    <automated>cd /home/hippo/projects/Energy_trade_inspection && npx tsc --noEmit 2>&1 | grep "watchlist/page" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - watchlist/page.tsx STATUS_COLOR 包含 `'listed': '#ef4444'`（硬编码，非 CSS 变量）
    - watchlist/page.tsx 包含 `background: '#1e1e24'`（secondaryBtn 定义）
    - watchlist/page.tsx 包含 `backgroundColor: \`\${STATUS_COLOR`（pill 背景）
    - watchlist/page.tsx 包含 `.wl-row:hover`（hover CSS）
    - watchlist/page.tsx 不包含 `var(--status-listed)` 或 `var(--status-clear)`（已替换为硬编码色值）
    - watchlist/page.tsx 不包含 `var(--bg-surface)`（surface card 已替换）
    - `npx tsc --noEmit` 无 watchlist/page.tsx 相关类型错误
  </acceptance_criteria>
  <done>
    watchlist/page.tsx：STATUS_COLOR 硬编码，action 按钮使用 secondaryBtn 样式，sanction status 显示为 pill，表格行有 hover 效果，主要 surface cards 使用 TOKEN 背景值。
  </done>
</task>

<task type="auto">
  <name>Task 2: ReportsClient.tsx — GhostButton 替换 + 行 hover + RiskBadge pill</name>
  <read_first>
    - src/app/reports/ReportsClient.tsx (完整阅读全部 484 行，理解 GhostButton 使用位置和 RowShell 结构)
    - src/app/trade/TradeClient.tsx (读取 secondaryBtnStyle 常量，约第 87–100 行)
    - .planning/phases/14-platform-wide-ui-polish/14-PATTERNS.md (ReportsClient 部分 pattern 代码)
    - .planning/phases/14-platform-wide-ui-polish/14-CONTEXT.md (reports 决策)
    - .planning/phases/13-trade-check-ui-redesign/13-UI-SPEC.md (Secondary Button + pill 规范)
  </read_first>
  <files>src/app/reports/ReportsClient.tsx</files>
  <action>
    这是 Client Component（有 'use client'）。可以使用 onMouseEnter/onMouseLeave 实现 hover 效果。

    **Step 1：删除 GhostButton 组件**

    找到并完整删除 GhostButton 函数组件（约第 57–84 行）。

    **Step 2：添加 secondaryBtnStyle 常量（在 RISK_LABEL 之后）**
    ```typescript
    const secondaryBtnStyle: React.CSSProperties = {
      background: '#1e1e24',
      color: '#8b8b9a',
      border: '1px solid rgba(255,255,255,0.07)',
      borderRadius: '7px',
      padding: '6px 14px',
      fontSize: '13px',
      fontFamily: 'inherit',
      cursor: 'pointer',
      boxShadow: '0 1px 2px rgba(0,0,0,0.15)',
      transition: 'all 0.12s ease',
      textDecoration: 'none',
      display: 'inline-block',
      lineHeight: 1,
    }
    ```

    **Step 3：danger variant**（用于 Delete 确认的 "Yes" 按钮）：
    ```typescript
    const dangerBtnStyle: React.CSSProperties = {
      ...secondaryBtnStyle,
      color: '#ef4444',
      border: '1px solid rgba(239,68,68,0.4)',
    }
    ```

    **Step 4：替换所有 GhostButton 使用**

    找到所有 `<GhostButton ...>` 调用，逐一替换：

    - View 按钮（Link 类型）：
      ```tsx
      <Link href={report.path} style={secondaryBtnStyle}>View</Link>
      ```

    - PDF 按钮（a 标签类型）：
      ```tsx
      <a href={report.pdfPath} style={secondaryBtnStyle} download>PDF</a>
      ```

    - Delete/Yes 按钮（danger 类型）：
      ```tsx
      <button type="button" onClick={onConfirm} style={dangerBtnStyle}>Yes</button>
      ```

    - Cancel/No 按钮（普通 secondary）：
      ```tsx
      <button type="button" onClick={onCancel} style={secondaryBtnStyle}>No</button>
      ```

    根据 ReportsClient 实际的 GhostButton 调用情况适配，保留 href/onClick/children 语义。

    **Step 5：RowShell 升级**

    找到 RowShell 组件（约第 86–102 行），升级 style：
    ```typescript
    function RowShell({ children }: { children: React.ReactNode }) {
      const [hovered, setHovered] = useState(false)
      return (
        <div
          onMouseEnter={() => setHovered(true)}
          onMouseLeave={() => setHovered(false)}
          style={{
            display: 'flex',
            alignItems: 'center',
            gap: '16px',
            padding: '12px 16px',
            backgroundColor: hovered ? '#1e1e24' : '#111113',
            border: '1px solid rgba(255,255,255,0.07)',
            borderTop: '1px solid rgba(255,255,255,0.09)',
            borderRadius: '10px',
            boxShadow: '0 2px 8px rgba(0,0,0,0.25), inset 0 1px 0 rgba(255,255,255,0.05)',
            transition: 'background 0.1s ease',
            cursor: 'default',
          }}
        >
          {children}
        </div>
      )
    }
    ```
    注意：RowShell 添加了 useState(false) for hover，这在 Client Component 中完全合法。

    **Step 6：RiskBadge 升级为 pill 规范**

    找到 RiskBadge 组件（约第 35–55 行），升级为 pill（rgba 背景 + border）：
    ```typescript
    const RISK_COLOR: Record<string, string> = {
      critical: '#ef4444',
      high:     '#f97316',
      medium:   '#fbbf24',
      low:      '#4ade80',
    }

    function RiskBadge({ risk }: { risk: string }) {
      const color = RISK_COLOR[risk] ?? '#55556a'
      const label = RISK_LABEL[risk] ?? risk.toUpperCase()
      return (
        <span style={{
          fontSize: '11px', fontWeight: 600,
          letterSpacing: '0.04em', textTransform: 'uppercase' as const,
          color,
          backgroundColor: `${color}18`,
          border: `1px solid ${color}44`,
          borderRadius: '4px',
          padding: '2px 7px',
          flexShrink: 0,
        }}>
          {label}
        </span>
      )
    }
    ```
    之前的 RISK_COLOR 使用了 CSS 变量（`var(--status-listed)`），替换为硬编码色值。

    **Step 7：Section heading 样式升级**

    找到 reports 列表的 section 标题（如 "Trade Checks"、"Screening Reports"），将标题样式升级为 section label 规范：
    ```typescript
    fontSize: '11px', color: '#55556a',
    textTransform: 'uppercase', letterSpacing: '0.07em',
    marginBottom: '12px',
    ```

    **不要做：**
    - 不删除 GhostButton 文件（该组件定义在 ReportsClient.tsx 内部，删除内部组件即可）
    - 不修改报告数据加载逻辑
    - 不修改删除报告的 server action 调用
    - 不改变排序、过滤功能（如有）
  </action>
  <verify>
    <automated>cd /home/hippo/projects/Energy_trade_inspection && npx tsc --noEmit 2>&1 | grep "reports/ReportsClient" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - ReportsClient.tsx 不包含字符串 `GhostButton`（组件已删除且不再使用）
    - ReportsClient.tsx 包含 `const secondaryBtnStyle`
    - ReportsClient.tsx 包含 `style={secondaryBtnStyle}`（至少一处使用）
    - ReportsClient.tsx 包含 `backgroundColor: hovered ? '#1e1e24' : '#111113'`（RowShell hover）
    - ReportsClient.tsx 包含 `` backgroundColor: `${color}18` ``（RiskBadge pill 背景）
    - ReportsClient.tsx 的 RISK_COLOR 不包含 `var(--status-listed)` 或 `var(--status-clear)`（已替换为硬编码）
    - `npx tsc --noEmit` 无 ReportsClient.tsx 相关类型错误
  </acceptance_criteria>
  <done>
    ReportsClient.tsx：GhostButton 组件删除，所有按钮改为 secondaryBtnStyle，RowShell 有 hover 效果（#1e1e24），RiskBadge 使用 pill 规范（rgba 背景 + colored border），RISK_COLOR 硬编码。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→server actions | dismissAlert / removeWatchlistItem 通过 form action 调用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | Tampering | watchlist server actions | accept | auth() 验证在 server action 内部已有，本 plan 仅改样式不碰逻辑 |
| T-14-02-02 | Tampering | ReportsClient delete confirm | accept | 删除确认 UI 已有，样式变更不影响确认流程 |
</threat_model>

<verification>
1. `grep -n "GhostButton" src/app/reports/ReportsClient.tsx` — 返回空
2. `grep -n "secondaryBtnStyle" src/app/reports/ReportsClient.tsx` — 返回多行（定义 + 使用）
3. `grep -n "background: '#1e1e24'" src/app/watchlist/page.tsx` — 返回 secondaryBtn 定义行
4. `grep -n "STATUS_COLOR\[" src/app/watchlist/page.tsx` — 返回 pill 背景计算行
5. `grep -n "var(--bg-surface)" src/app/watchlist/page.tsx` — 返回空（已替换）
6. `npx tsc --noEmit` — 通过
</verification>

<success_criteria>
- /watchlist action 按钮（Remove、Dismiss）显示 #1e1e24 背景的 secondary button 样式
- /watchlist sanction status 显示为彩色 pill（listed=红色，not_listed=绿色，unknown=灰色）
- /watchlist 列表行 hover 时有淡 rgba 背景
- /reports 无 GhostButton，View/PDF 按钮使用 secondaryBtnStyle
- /reports 列表行 hover 时 background 变为 #1e1e24
- /reports RiskBadge 使用 pill 规范
- TypeScript 严格模式无错误
</success_criteria>

<output>
完成后创建 `.planning/phases/14-platform-wide-ui-polish/14-02-SUMMARY.md`
</output>
