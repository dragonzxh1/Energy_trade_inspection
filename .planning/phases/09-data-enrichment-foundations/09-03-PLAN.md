---
phase: 09-data-enrichment-foundations
plan: 03
type: execute
wave: 2
depends_on:
  - "09-01"
  - "09-02"
files_modified:
  - src/components/entity/FraudAlertsPanel.tsx
  - src/app/company/[slug]/page.tsx
  - src/app/vessel/[imo]/page.tsx
autonomous: false
requirements:
  - NETDATA-03
  - NETDATA-04

must_haves:
  truths:
    - "公司详情页包含 Fraud Alerts tab，位于 Risk Flags 之后、Offshore Leaks 之前"
    - "船舶详情页包含 Fraud Alerts tab，位于 Risk Flags 之后、PSC History 之前"
    - "FraudAlertsPanel 无 use client 指令（Server Component）"
    - "FraudAlertsPanel 空状态显示：No fraud alerts on record for this entity."
    - "FraudAlertsPanel 包含 ContentLock F3 gating 在两个详情页中"
    - "company page 在 f3Unlocked 条件下调用 getCompanyFraudAlerts(company.name)"
    - "vessel page 在 f3Unlocked 条件下调用 getVesselFraudAlerts(vessel.currentOperator)"
    - "tabs 和 panels 数组索引严格对应（无错位）"
  artifacts:
    - path: "src/components/entity/FraudAlertsPanel.tsx"
      provides: "FraudAlertRow[] props-fed Server Component，完整 UI 规格"
      min_lines: 80
    - path: "src/app/company/[slug]/page.tsx"
      provides: "新增 fraud-alerts tab 和 FraudAlertsPanel panel；fraudAlerts 预取"
      contains: "fraud-alerts"
    - path: "src/app/vessel/[imo]/page.tsx"
      provides: "新增 fraud-alerts tab 和 FraudAlertsPanel panel；vesselFraudAlerts 预取"
      contains: "fraud-alerts"
  key_links:
    - from: "src/app/company/[slug]/page.tsx"
      to: "src/components/entity/FraudAlertsPanel.tsx"
      via: "import + tabs/panels 插入"
      pattern: "FraudAlertsPanel"
    - from: "src/app/vessel/[imo]/page.tsx"
      to: "src/components/entity/FraudAlertsPanel.tsx"
      via: "import + tabs/panels 插入"
      pattern: "FraudAlertsPanel"
---

<objective>
实现 FraudAlertsPanel Server Component，并将其插入公司和船舶详情页的 tab 系统中。

Purpose: 完成 NETDATA-03（公司页欺诈警报面板）和 NETDATA-04（船舶页欺诈警报面板）的用户可见交付。

Output: 新 FraudAlertsPanel.tsx 组件文件；company/vessel page.tsx 中的 tab 插入和数据预取调用。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-data-enrichment-foundations/09-CONTEXT.md
@.planning/phases/09-data-enrichment-foundations/09-UI-SPEC.md
@.planning/phases/09-data-enrichment-foundations/09-PATTERNS.md

<interfaces>
<!-- FraudAlertRow 接口（由 Plan 02 在 repository.ts 中创建） -->
```typescript
export interface FraudAlertRow {
  source: string
  source_name: string
  source_url: string
  company_name: string
  list_type: 'blacklist' | 'whitelist'
  fraud_type: string | null
  description: string | null
  scam_url: string | null
  synced_at: Date
}
export async function getCompanyFraudAlerts(name: string): Promise<FraudAlertRow[]>
export async function getVesselFraudAlerts(operator: string | null | undefined, manager?: string | null): Promise<FraudAlertRow[]>
```

<!-- OffshoreLeaksPanel 结构模板（company/[slug]/page.tsx L462-L533）-->
```tsx
function OffshoreLeaksPanel({ matches }: { matches: IcijMatch[] }) {
  // 空状态：card shell + sectionTitle + emptyState 段落
  // 有数据：card + sectionTitle(count) + item list + footnote
  // 无 use client — Server Component
}
```

<!-- card / sectionTitle / emptyState 样式（company/[slug]/page.tsx 顶部）-->
```tsx
const card: React.CSSProperties = {
  backgroundColor: 'var(--bg-surface)',
  borderRadius: '10px',
  padding: 'var(--space-5)',
  border: '1px solid var(--border-subtle)',
}
const sectionTitle: React.CSSProperties = {
  color: 'var(--text-muted)',
  fontSize: '11px',
  fontWeight: 600,
  letterSpacing: '0.08em',
  textTransform: 'uppercase',
  marginBottom: 'var(--space-4)',
}
// emptyState: { color: 'var(--text-muted)', fontSize: '14px', textAlign: 'center', padding: 'var(--space-8) 0' }
```

<!-- ContentLock 用法 (src/components/entity/ContentLock.tsx) -->
```tsx
import ContentLock from '@/components/entity/ContentLock'
<ContentLock unlocked={f3Unlocked} reason={lockReason}>
  <FraudAlertsPanel alerts={fraudAlerts} />
</ContentLock>
```

<!-- company page Promise.all 现有结构（L741-L747）-->
```typescript
const [watchlistRows, icijMatches, icijOfficerLinks] = await Promise.all([
  session?.user && (plan === 'professional' || plan === 'enterprise')
    ? getEntityWatchState(session.user.id, company.id)
    : Promise.resolve(false),
  f3Unlocked ? getIcijMatches(company.id) : Promise.resolve([]),
  f3Unlocked ? getIcijOfficerNetwork(company.id) : Promise.resolve([]),
])
// Phase 9: 扩展此 Promise.all，追加 getCompanyFraudAlerts 调用
```

<!-- vessel page 数据预取位置（L452 之后）-->
```typescript
const warningHits: WarningHit[] = await getWarningHits(vessel.name, 'vessel')
// Phase 9: 在 warningHits 之后追加：
// const vesselFraudAlerts = f3Unlocked
//   ? await getVesselFraudAlerts(vessel.currentOperator)
//   : []
```

<!-- company tabs 数组（L769-L779）— 当前无 fraud-alerts tab -->
```typescript
const tabs = [
  { id: 'registration',       label: 'Registration' },
  { id: 'directors',          label: 'Directors' },
  { id: 'beneficial-owners',  label: 'Beneficial Owners' },
  { id: 'vessels',            label: 'Vessels' },
  { id: 'flags',              label: 'Risk Flags' },
  // ← 在此插入 { id: 'fraud-alerts', label: 'Fraud Alerts' }
  { id: 'offshore',           label: 'Offshore Leaks' },
  { id: 'intelligence',       label: 'Intelligence' },
  { id: 'domain',             label: 'Domain' },
  { id: 'sources',            label: 'Sources' },
]
```

<!-- vessel tabs 数组（L465-L473）— 当前无 fraud-alerts tab -->
```typescript
const tabs = [
  { id: 'details',       label: 'Vessel Details' },
  { id: 'ais',           label: 'AIS Tracking' },
  { id: 'draft',         label: 'Draft Risk' },
  { id: 'flags',         label: 'Risk Flags' },
  // ← 在此插入 { id: 'fraud-alerts', label: 'Fraud Alerts' }
  { id: 'history',       label: 'PSC History' },
  { id: 'intelligence',  label: 'Intelligence' },
  { id: 'sources',       label: 'Sources' },
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 FraudAlertsPanel Server Component</name>
  <read_first>
    - src/app/company/[slug]/page.tsx (L53-L70 card/sectionTitle/emptyState 样式定义；L462-L533 OffshoreLeaksPanel 完整结构作为模板)
    - .planning/phases/09-data-enrichment-foundations/09-UI-SPEC.md (§1 FraudAlertsPanel 完整规格：layout、states、item layout、source badge、footnote；§ Copywriting Contract)
    - src/lib/server/repository.ts (最后 100 行，确认 FraudAlertRow 接口存在且字段正确)
  </read_first>
  <files>src/components/entity/FraudAlertsPanel.tsx</files>
  <action>
创建新文件 `src/components/entity/FraudAlertsPanel.tsx`，严格按照 UI-SPEC §1 实现。

**关键规则（违反任一将导致 checker 拒绝）：**
- 无 `'use client'` 指令 — 纯 Server Component
- 不在组件内调用 db — 数据通过 props 传入
- 空状态文案必须完全匹配：`"No fraud alerts on record for this entity."`
- 副标题文案：`"Covers Rotterdam Port Blacklist, FuelScamAlert, Ametheus, and other industry sources."`
- 脚注文案：`"Fraud alerts sourced from industry blacklists. Independent of government sanctions lists. Verify against primary sources before taking action."`
- panel section title：无匹配时 "Fraud Alerts"，有匹配时 `"Fraud Alerts ({N})"` 其中 N 为 blacklist 数量

**组件结构：**

```tsx
import type { FraudAlertRow } from '@/lib/server/repository'

interface Props {
  alerts: FraudAlertRow[]
}

// Source key → display label map (UI-SPEC §1 Source badge)
const SOURCE_LABEL: Record<string, string> = {
  storagespoofing: 'Rotterdam Port',
  fuelscamalert: 'FuelScamAlert',
  ametheus: 'Ametheus',
  'glo-innovations': 'Glo-Innovations',
  capitalgaslogistics: 'Capital Gas',
}

// Fraud type → display label map (UI-SPEC Copywriting Contract)
const FRAUD_TYPE_LABEL: Record<string, string> = {
  'storage-spoofing': 'Storage Spoofing',
  'fuel-scam': 'Fuel Scam',
  impersonation: 'Impersonation',
}

export default function FraudAlertsPanel({ alerts }: Props) {
  // ...
}
```

**card 样式（与 company page OffshoreLeaksPanel 一致）：**
```tsx
const card: React.CSSProperties = {
  backgroundColor: 'var(--bg-surface)',
  borderRadius: '10px',
  padding: 'var(--space-5)',
  border: '1px solid var(--border-subtle)',
}
```

**Empty state：**
```tsx
if (alerts.length === 0) {
  return (
    <div style={card}>
      <p style={sectionTitle}>Fraud Alerts</p>
      <p style={{ color: 'var(--text-muted)', fontSize: '14px', textAlign: 'center', padding: 'var(--space-8) 0' }}>
        No fraud alerts on record for this entity.
      </p>
      <p style={{ color: 'var(--text-muted)', fontSize: '12px', textAlign: 'center', marginTop: 'var(--space-2)' }}>
        Covers Rotterdam Port Blacklist, FuelScamAlert, Ametheus, and other industry sources.
      </p>
    </div>
  )
}
```

**Populated state section title：**
```tsx
const blacklistCount = alerts.filter((a) => a.list_type === 'blacklist').length
const titleText = blacklistCount > 0 ? `Fraud Alerts (${blacklistCount})` : 'Fraud Alerts'
```

**Alert item layout（UI-SPEC §1 Alert item layout）：**

每个 alert row 使用 `className="data-row"`（与 OffshoreLeaksPanel 一致，获得 CSS hover 效果）。

左列（flex: 1）：
- Row 1: `company_name`（14px, weight 600, var(--text-primary)）+ source badge（紧邻，gap var(--space-2)）
- Row 2: fraud_type label（若有）+ `· Reported {synced_at}` — 日期格式：`Jan 15, 2025`（en-US locale，`{ year: 'numeric', month: 'short', day: 'numeric' }`）
- Row 3（条件）: description（若非 null 且非空）— 12px italic var(--text-muted)，超过 120 字截断加 `…`
- Row 4（条件）: scam_url — "Fake site:" 前缀（var(--text-muted)）+ URL 链接（var(--accent-primary)，11px，target="_blank" rel="noopener noreferrer"）

右列（flexShrink: 0，marginLeft: var(--space-3)）：
- list_type badge：
  - Blacklist: 文字 "BLACKLIST"，color #f59e0b，backgroundColor rgba(245,158,11,0.1)
  - Whitelist: 文字 "WHITELIST"，color #10b981，backgroundColor rgba(16,185,129,0.1)
  - 共同样式：fontSize 11px，fontWeight 600，textTransform uppercase，letterSpacing 0.06em，padding 2px 6px，borderRadius 4px

Source badge 样式（UI-SPEC §1 Source badge）：
```tsx
// 显示文本：SOURCE_LABEL[alert.source] ?? alert.source.toUpperCase()
// 样式：
{
  fontSize: '10px',
  fontWeight: 600,
  textTransform: 'uppercase',
  letterSpacing: '0.06em',
  color: 'var(--accent-amber)',       // #f59e0b
  backgroundColor: 'rgba(245,158,11,0.1)',
  border: '1px solid rgba(245,158,11,0.15)',
  padding: '1px 6px',
  borderRadius: '4px',
}
```

**Panel footnote（item list 之后）：**
```tsx
<p style={{ color: 'var(--text-muted)', fontSize: '11px', lineHeight: '16px', marginTop: 'var(--space-4)' }}>
  Fraud alerts sourced from industry blacklists. Independent of government sanctions lists. Verify against primary sources before taking action.
</p>
```

**移动端响应（UI-SPEC Responsive Behavior ≤600px）：**
alert item 的 flexDirection 在正常布局为 row，不需要 CSS 媒体查询（inline style 固定 flex row；现有 `.entity-layout` 处理页面整体响应，alert item 按 UI-SPEC 在 ≤600px 时交给现有行为，不新增媒体查询）。
  </action>
  <verify>
    <automated>cd /home/hippo/projects/Energy_trade_inspection && npm run type-check 2>&1 | tail -5</automated>
  </verify>
  <done>
    - src/components/entity/FraudAlertsPanel.tsx 存在，无 use client
    - 包含 FraudAlertRow import from repository
    - 空状态文案精确匹配 UI-SPEC
    - source badge、list_type badge、item layout 按 UI-SPEC 实现
    - npm run type-check 零错误
  </done>
  <acceptance_criteria>
    - `grep "use client" src/components/entity/FraudAlertsPanel.tsx` 返回空（无 use client）
    - `grep "FraudAlertRow" src/components/entity/FraudAlertsPanel.tsx` 返回 import 行
    - `grep "No fraud alerts on record for this entity" src/components/entity/FraudAlertsPanel.tsx` 返回空状态文案行
    - `grep "Covers Rotterdam Port Blacklist" src/components/entity/FraudAlertsPanel.tsx` 返回副标题行
    - `grep "Fraud alerts sourced from industry blacklists" src/components/entity/FraudAlertsPanel.tsx` 返回脚注行
    - `grep "data-row" src/components/entity/FraudAlertsPanel.tsx` 返回 alert item className
    - `grep "accent-amber" src/components/entity/FraudAlertsPanel.tsx` 返回 source badge 颜色行
    - `npm run type-check` 退出码 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 插入 Fraud Alerts tab 到 company 和 vessel 详情页</name>
  <read_first>
    - src/app/company/[slug]/page.tsx (L730-L810 完整数据预取 + tabs + panels 区域；顶部 import 列表)
    - src/app/vessel/[imo]/page.tsx (L440-L496 完整数据预取 + tabs + panels 区域；顶部 import 列表)
    - src/components/entity/FraudAlertsPanel.tsx (确认 Task 1 已完成，组件存在)
    - src/lib/server/repository.ts (确认 getCompanyFraudAlerts 和 getVesselFraudAlerts 已导出)
  </read_first>
  <files>
    src/app/company/[slug]/page.tsx
    src/app/vessel/[imo]/page.tsx
  </files>
  <action>
**修改 1：src/app/company/[slug]/page.tsx**

在文件顶部的 import 区域追加（与其他 entity component import 相邻）：
```typescript
import FraudAlertsPanel from '@/components/entity/FraudAlertsPanel'
```

并在 repository import 行中追加（与 getIcijMatches 等同行或同一 import 块）：
```typescript
// 现有行示例：import { getIcijMatches, getIcijOfficerNetwork, ... } from '@/lib/server/repository'
// 追加 getCompanyFraudAlerts 到此 import
```

**在数据预取区域（L741-L747 的 Promise.all）中扩展：**

将：
```typescript
const [watchlistRows, icijMatches, icijOfficerLinks] = await Promise.all([
  session?.user && (plan === 'professional' || plan === 'enterprise')
    ? getEntityWatchState(session.user.id, company.id)
    : Promise.resolve(false),
  f3Unlocked ? getIcijMatches(company.id) : Promise.resolve([]),
  f3Unlocked ? getIcijOfficerNetwork(company.id) : Promise.resolve([]),
])
```

改为：
```typescript
const [watchlistRows, icijMatches, icijOfficerLinks, fraudAlerts] = await Promise.all([
  session?.user && (plan === 'professional' || plan === 'enterprise')
    ? getEntityWatchState(session.user.id, company.id)
    : Promise.resolve(false),
  f3Unlocked ? getIcijMatches(company.id) : Promise.resolve([]),
  f3Unlocked ? getIcijOfficerNetwork(company.id) : Promise.resolve([]),
  f3Unlocked ? getCompanyFraudAlerts(company.name) : Promise.resolve([]),  // per NETDATA-03, T-9-03
])
```

**在 tabs 数组（L769-L779）中插入 fraud-alerts tab：**

在 `{ id: 'flags', label: 'Risk Flags' }` 之后、`{ id: 'offshore', label: 'Offshore Leaks' }` 之前，插入：
```typescript
{ id: 'fraud-alerts', label: 'Fraud Alerts' },
```

最终 tabs 顺序：
Registration | Directors | Beneficial Owners | Vessels | Risk Flags | **Fraud Alerts** | Offshore Leaks | Intelligence | Domain | Sources

**在 panels 数组（L781-L808）中插入 FraudAlertsPanel（索引必须与 tabs 严格对应）：**

在 `<ContentLock key="flags" ...><RiskFlagsPanel /></ContentLock>` 之后、`<ContentLock key="offshore" ...>` 之前，插入：
```tsx
<ContentLock key="fraud-alerts" unlocked={f3Unlocked} reason={lockReason}>
  <FraudAlertsPanel alerts={fraudAlerts} />
</ContentLock>,
```

---

**修改 2：src/app/vessel/[imo]/page.tsx**

在文件顶部 import 区域追加：
```typescript
import FraudAlertsPanel from '@/components/entity/FraudAlertsPanel'
```

并在 repository import 中追加 `getVesselFraudAlerts`。

**在数据预取区域（warningHits 之后，约 L452）追加：**
```typescript
const warningHits: WarningHit[] = await getWarningHits(vessel.name, 'vessel')
const vesselFraudAlerts = f3Unlocked    // per NETDATA-04, T-9-03
  ? await getVesselFraudAlerts(vessel.currentOperator)
  : []
```

注意：`vessel.manager` 字段不存在（RESEARCH.md Q-1 确认），只传 `vessel.currentOperator`；manager 参数省略（函数签名预留为可选）。

**在 tabs 数组（L465-L473）中插入 fraud-alerts tab：**

在 `{ id: 'flags', label: 'Risk Flags' }` 之后、`{ id: 'history', label: 'PSC History' }` 之前，插入：
```typescript
{ id: 'fraud-alerts', label: 'Fraud Alerts' },
```

最终 tabs 顺序：
Vessel Details | AIS Tracking | Draft Risk | Risk Flags | **Fraud Alerts** | PSC History | Intelligence | Sources

**在 panels 数组（L475-L496）中插入 FraudAlertsPanel（索引必须与 tabs 严格对应）：**

在 `<ContentLock key="flags" ...><RiskFlagsPanel /></ContentLock>` 之后、`<div key="history" ...>` 之前，插入：
```tsx
<ContentLock key="fraud-alerts" unlocked={f3Unlocked} reason={lockReason}>
  <FraudAlertsPanel alerts={vesselFraudAlerts} />
</ContentLock>,
```

**关键约束（避免 Pitfall 5）：**
- tabs[i] 与 panels[i] 必须严格按索引对应
- 在 company page 中，fraud-alerts tab 插入后 tabs 数组共 10 项，panels 数组也必须共 10 项
- 在 vessel page 中，fraud-alerts tab 插入后 tabs 数组共 8 项，panels 数组也必须共 8 项
- 完成后手动数 tabs 和 panels 的条目数，确认两者相等
  </action>
  <verify>
    <automated>cd /home/hippo/projects/Energy_trade_inspection && npm run type-check 2>&1 | tail -5 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - company/[slug]/page.tsx 的 tabs 包含 fraud-alerts（位于 flags 后、offshore 前）
    - company/[slug]/page.tsx 的 panels 包含 FraudAlertsPanel（与 tabs 对应）
    - vessel/[imo]/page.tsx 的 tabs 包含 fraud-alerts（位于 flags 后、history 前）
    - vessel/[imo]/page.tsx 的 panels 包含 FraudAlertsPanel（与 tabs 对应）
    - 两个页面的 f3Unlocked 条件守卫正确
    - npm run build 通过
  </done>
  <acceptance_criteria>
    - `grep "fraud-alerts" "src/app/company/[slug]/page.tsx"` 返回 tab 定义行和 panel ContentLock key 行（至少 2 处）
    - `grep "FraudAlertsPanel" "src/app/company/[slug]/page.tsx"` 返回 import 行和 JSX 使用行
    - `grep "getCompanyFraudAlerts" "src/app/company/[slug]/page.tsx"` 返回 import 和调用行
    - `grep "fraud-alerts" "src/app/vessel/[imo]/page.tsx"` 返回 tab 定义行和 panel ContentLock key 行（至少 2 处）
    - `grep "FraudAlertsPanel" "src/app/vessel/[imo]/page.tsx"` 返回 import 行和 JSX 使用行
    - `grep "getVesselFraudAlerts" "src/app/vessel/[imo]/page.tsx"` 返回 import 和调用行
    - `grep "f3Unlocked" "src/app/company/[slug]/page.tsx"` 在 getCompanyFraudAlerts 调用处返回条件守卫行
    - `grep "f3Unlocked" "src/app/vessel/[imo]/page.tsx"` 在 getVesselFraudAlerts 调用处返回条件守卫行
    - `npm run type-check` 退出码 0
    - `npm run build` 退出码 0
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 03 完成了 FraudAlertsPanel 组件和 tab 插入。Wave 2 全部实现完成，Phase 9 全部代码已就位。需要人工验证 UI 渲染和 F3 gating 行为是否正确。
  </what-built>
  <how-to-verify>
    1. 运行 `npm run dev`，等待服务器启动（http://localhost:3000）
    2. 访问任意公司详情页（如 /company/some-slug）
       - 验证 tab bar 中出现 "Fraud Alerts" tab（位于 Risk Flags 之后、Offshore Leaks 之前）
       - 以免费账号访问：验证 Fraud Alerts tab 内容显示 ContentLock 锁定 UI
       - 以付费账号访问：验证 Fraud Alerts tab 显示面板内容（空状态或警报列表）
       - 验证空状态文案：`No fraud alerts on record for this entity.`
    3. 访问任意船舶详情页（如 /vessel/some-imo）
       - 验证 tab bar 中出现 "Fraud Alerts" tab（位于 Risk Flags 之后、PSC History 之前）
       - 验证同上的 F3 gating 和空状态
    4. 访问任意公司详情页的 "Offshore Leaks" tab
       - 验证 tab 内容仍正常渲染（tab 索引未错位）
       - 若有 ICIJ matches：验证 is_sanctioned=true 的行显示红色 "Sanctioned Entity" badge（需 migration 036 已运行）
  </how-to-verify>
  <resume-signal>
    输入 "approved" 继续（UI 渲染正确），或描述发现的问题
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → company/vessel page.tsx | Next.js Server Component，用户请求经 NextAuth session 验证 |
| page.tsx → FraudAlertsPanel | props 传入，无额外网络请求；数据已在 page.tsx 服务器端预取 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-03 | Elevation of Privilege | company/[slug]/page.tsx FraudAlertsPanel rendering | mitigate | `f3Unlocked ? getCompanyFraudAlerts(company.name) : Promise.resolve([])` — free-plan 用户收到空数组，ContentLock 渲染锁定 UI；双重保护符合 ASVS V4.1 |
| T-9-03 | Elevation of Privilege | vessel/[imo]/page.tsx FraudAlertsPanel rendering | mitigate | `f3Unlocked ? await getVesselFraudAlerts(vessel.currentOperator) : []` — 同上 |
| T-9-02 | Denial of Service | FraudAlertsPanel props | accept | 数据量由 Plan 02 的 LIMIT 50 在 repository 层控制；组件为纯渲染，无额外 DB 查询 |
</threat_model>

<verification>
1. `npm run type-check` — 通过（FraudAlertsPanel props 类型、page.tsx import 类型均正确）
2. `npm run build` — 通过（全量编译，含 SSR 渲染路径）
3. `grep "fraud-alerts" src/app/company/[slug]/page.tsx` — 确认 tab 存在
4. `grep "fraud-alerts" src/app/vessel/[imo]/page.tsx` — 确认 tab 存在
5. `grep "use client" src/components/entity/FraudAlertsPanel.tsx` — 确认无 use client（Server Component）
6. 人工验证：dev server 启动，访问公司/船舶页面，验证 tab 渲染和 F3 gating
</verification>

<success_criteria>
- FraudAlertsPanel.tsx 存在，无 use client，纯 props-fed Server Component
- company 页面 tabs 顺序：Registration | Directors | Beneficial Owners | Vessels | Risk Flags | Fraud Alerts | Offshore Leaks | Intelligence | Domain | Sources（共 10 项）
- vessel 页面 tabs 顺序：Vessel Details | AIS Tracking | Draft Risk | Risk Flags | Fraud Alerts | PSC History | Intelligence | Sources（共 8 项）
- 两个页面的 tabs 和 panels 数组条目数完全相等（无错位）
- f3Unlocked 条件守卫在两个页面的 fraud alert 数据预取处均存在
- npm run build 通过
- 人工验证：tab 可见、ContentLock 在 free-plan 下生效、空状态文案正确
</success_criteria>

<output>
完成后创建 `.planning/phases/09-data-enrichment-foundations/09-03-SUMMARY.md`
</output>
