---
phase: 09-data-enrichment-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/migrations/036_icij_sanctions_linkage.sql
  - scripts/sync-icij-offshore.mjs
  - src/lib/server/repository.ts
autonomous: true
requirements:
  - NETDATA-01
  - NETDATA-02

must_haves:
  truths:
    - "icij_entities 表在 migration 036 后包含 is_sanctioned BOOLEAN 和 sanctions_match TEXT 两列"
    - "ICIJ 同步脚本在 linkToEntities() 之后执行全量制裁重匹配 UPDATE"
    - "IcijMatch 接口新增 isSanctioned? 和 sanctionsMatch? 字段"
    - "getIcijMatches() SQL 查询 SELECT is_sanctioned 和 sanctions_match 并映射到 IcijMatch 对象"
    - "OffshoreLeaksPanel 在 is_sanctioned=true 的行上渲染红色 Sanctioned Entity badge，title 属性显示 sanctions_match"
  artifacts:
    - path: "db/migrations/036_icij_sanctions_linkage.sql"
      provides: "ALTER TABLE icij_entities 添加 is_sanctioned 和 sanctions_match；稀疏索引；全量 UPDATE"
      contains: "ADD COLUMN IF NOT EXISTS is_sanctioned"
    - path: "scripts/sync-icij-offshore.mjs"
      provides: "matchSanctions() 函数，在 main() 末尾调用"
      contains: "matchSanctions"
    - path: "src/lib/server/repository.ts"
      provides: "IcijMatch 接口扩展 + getIcijMatches() 字段扩展"
      exports: ["IcijMatch", "getIcijMatches"]
  key_links:
    - from: "scripts/sync-icij-offshore.mjs:main()"
      to: "icij_entities.is_sanctioned"
      via: "matchSanctions() UPDATE SQL"
      pattern: "matchSanctions"
    - from: "src/app/company/[slug]/page.tsx:OffshoreLeaksPanel"
      to: "IcijMatch.isSanctioned"
      via: "getIcijMatches() 返回字段"
      pattern: "isSanctioned"
---

<objective>
建立 ICIJ↔制裁数据联动的数据基础层：添加 schema 列、在 ICIJ 同步脚本中嵌入全量重匹配逻辑、扩展 repository 层接口，并在 OffshoreLeaksPanel 行内渲染 Sanctioned Entity badge。

Purpose: Phase 10 的红节点着色依赖 is_sanctioned 标记；Phase 9 的 OffshoreLeaksPanel 需要在已有行上显示 badge 以给用户即时可见的制裁穿透信息。

Output: migration 036 SQL 文件、扩展后的 sync 脚本、更新后的 IcijMatch 接口 + getIcijMatches() 函数、含 sanctioned badge 的 OffshoreLeaksPanel。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-data-enrichment-foundations/09-CONTEXT.md
@.planning/phases/09-data-enrichment-foundations/09-RESEARCH.md
@.planning/phases/09-data-enrichment-foundations/09-PATTERNS.md
@.planning/phases/09-data-enrichment-foundations/09-UI-SPEC.md

<interfaces>
<!-- 当前 IcijMatch 接口 (src/lib/server/repository.ts L998-L1010) -->
```typescript
export interface IcijMatch {
  nodeId: string
  name: string
  dataset: string
  entityType: string | null
  countries: string | null
  jurisdiction: string | null
  status: string | null
  incorporationDate: string | null
  address: string | null
  sourceUrl: string | null
  matchConfidence: number
  // Phase 9 需新增：
  // isSanctioned?: boolean
  // sanctionsMatch?: string | null
}
```

<!-- 当前 getIcijMatches() SQL (repository.ts L1013-L1035) -->
```typescript
export async function getIcijMatches(entityId: string): Promise<IcijMatch[]> {
  const { rows } = await db.query(
    `SELECT node_id, name, dataset, entity_type, countries, jurisdiction,
            status, incorporation_date, address, source_url, match_confidence
     FROM icij_entities
     WHERE linked_entity_id = $1
     ORDER BY match_confidence DESC
     LIMIT 20`,
    [entityId]
  )
  return rows.map((r) => ({
    nodeId: r.node_id,
    name: r.name,
    dataset: r.dataset,
    entityType: r.entity_type,
    countries: r.countries,
    jurisdiction: r.jurisdiction,
    status: r.status,
    incorporationDate: r.incorporation_date,
    address: r.address,
    sourceUrl: r.source_url,
    matchConfidence: parseFloat(r.match_confidence ?? '0'),
  }))
}
```

<!-- sync-icij-offshore.mjs linkToEntities 模式 (L370-L389) — matchSanctions() 复制此结构 -->
```javascript
async function linkToEntities() {
  console.log('\nLinking ICIJ entities to local company database…')
  const client = await pool.connect()
  try {
    const { rowCount } = await client.query(`UPDATE icij_entities i SET ...`)
    console.log(`Linked ${rowCount} ICIJ entries to local entities.`)
  } finally {
    client.release()
  }
}

// main() 末尾调用模式 (L429-L434)：
if (!DRY_RUN && total > 0) {
  await linkToEntities()
}
// Phase 9 在 linkToEntities() 之后追加 matchSanctions() 调用
```

<!-- OffshoreLeaksPanel 右列插入点 (company/[slug]/page.tsx L505-L521) -->
```tsx
<div style={{ textAlign: 'right', flexShrink: 0 }}>
  <p style={{
    fontSize: '12px', fontWeight: 600,
    color: m.matchConfidence >= 0.8 ? 'var(--status-listed)' : 'var(--text-muted)',
  }}>
    {Math.round(m.matchConfidence * 100)}% match
  </p>
  {/* ← Phase 9 在此插入 is_sanctioned badge（在 match% 之后、ICIJ link 之前） */}
  {m.sourceUrl && (
    <a href={m.sourceUrl} target="_blank" rel="noopener noreferrer"
       style={{ color: 'var(--accent-primary)', fontSize: '11px', textDecoration: 'none' }}>
      ICIJ
    </a>
  )}
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 036 — 添加 is_sanctioned 列和全量匹配 UPDATE</name>
  <read_first>
    - db/migrations/011_ports_psc_icij.sql (icij_entities 当前 schema，ADD COLUMN 模式)
    - db/migrations/010_opensanctions_schema.sql (sanctions_entries schema，search_text 列确认)
    - src/lib/server/sync/sanctions.ts (word_similarity > 0.72 阈值确认)
  </read_first>
  <files>db/migrations/036_icij_sanctions_linkage.sql</files>
  <action>
创建新文件 `db/migrations/036_icij_sanctions_linkage.sql`，内容如下（严格按此实现，不得简化）：

```sql
-- Migration 036: ICIJ↔Sanctions linkage
-- Adds is_sanctioned and sanctions_match columns to icij_entities,
-- then runs a full re-match against sanctions_entries (word_similarity > 0.72).

ALTER TABLE icij_entities
  ADD COLUMN IF NOT EXISTS is_sanctioned  BOOLEAN NOT NULL DEFAULT FALSE,
  ADD COLUMN IF NOT EXISTS sanctions_match TEXT;

-- Sparse index: only index flagged rows (matches idx_icij_linked pattern)
CREATE INDEX IF NOT EXISTS idx_icij_sanctioned
  ON icij_entities (is_sanctioned)
  WHERE is_sanctioned = TRUE;

-- Full re-match: single-pass LEFT JOIN LATERAL (D-02: full re-match every sync)
-- Threshold: word_similarity > 0.72 (D-03, matches sanctions.ts)
-- This may take several minutes on large icij_entities tables.
UPDATE icij_entities ie
SET
  is_sanctioned  = (m.matched_name IS NOT NULL),
  sanctions_match = m.matched_name
FROM (
  SELECT
    ie2.node_id,
    (
      SELECT se.name
      FROM sanctions_entries se
      WHERE se.sanctions IS NOT NULL
        AND word_similarity(lower(ie2.name), se.search_text) > 0.72
      ORDER BY word_similarity(lower(ie2.name), se.search_text) DESC
      LIMIT 1
    ) AS matched_name
  FROM icij_entities ie2
) m
WHERE ie.node_id = m.node_id;
```

注意事项：
- 使用 `ADD COLUMN IF NOT EXISTS` 保证冪等性（与其他 migration 一致）
- 稀疏索引 `WHERE is_sanctioned = TRUE`（与 idx_icij_linked 模式一致）
- UPDATE 在 migration 时执行一次初始化；后续通过 sync 脚本定期重跑（D-01/D-02）
- 阈值 `word_similarity > 0.72`（D-03，与 sanctions.ts 保持一致）
- 不添加 `se.sanctions IS NOT NULL` 以外的过滤条件（避免遗漏制裁记录）
  </action>
  <verify>
    <automated>grep -n "ADD COLUMN IF NOT EXISTS is_sanctioned" /home/hippo/projects/Energy_trade_inspection/db/migrations/036_icij_sanctions_linkage.sql && grep -n "word_similarity" /home/hippo/projects/Energy_trade_inspection/db/migrations/036_icij_sanctions_linkage.sql && grep -n "0.72" /home/hippo/projects/Energy_trade_inspection/db/migrations/036_icij_sanctions_linkage.sql</automated>
  </verify>
  <done>
    - 文件 db/migrations/036_icij_sanctions_linkage.sql 存在
    - 包含 `ADD COLUMN IF NOT EXISTS is_sanctioned  BOOLEAN NOT NULL DEFAULT FALSE`
    - 包含 `ADD COLUMN IF NOT EXISTS sanctions_match TEXT`
    - 包含 `CREATE INDEX IF NOT EXISTS idx_icij_sanctioned ... WHERE is_sanctioned = TRUE`
    - 包含 `word_similarity(lower(ie2.name), se.search_text) > 0.72`
    - UPDATE 语句使用 LEFT JOIN LATERAL 单次全量重匹配模式
  </done>
  <acceptance_criteria>
    - `grep "ADD COLUMN IF NOT EXISTS is_sanctioned" db/migrations/036_icij_sanctions_linkage.sql` 返回匹配行
    - `grep "ADD COLUMN IF NOT EXISTS sanctions_match" db/migrations/036_icij_sanctions_linkage.sql` 返回匹配行
    - `grep "idx_icij_sanctioned" db/migrations/036_icij_sanctions_linkage.sql` 返回匹配行
    - `grep "WHERE is_sanctioned = TRUE" db/migrations/036_icij_sanctions_linkage.sql` 返回匹配行
    - `grep "0.72" db/migrations/036_icij_sanctions_linkage.sql` 返回匹配行
    - `grep "LEFT JOIN LATERAL\|LATERAL" db/migrations/036_icij_sanctions_linkage.sql` 返回匹配行（或等价 subquery 结构）
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: sync 脚本扩展 + IcijMatch 接口扩展 + getIcijMatches() + OffshoreLeaksPanel badge</name>
  <read_first>
    - scripts/sync-icij-offshore.mjs (完整文件，理解 linkToEntities() 结构和 main() 末尾调用模式)
    - src/lib/server/repository.ts (L996-L1035，IcijMatch 接口和 getIcijMatches() 完整实现)
    - src/app/company/[slug]/page.tsx (L462-L533，OffshoreLeaksPanel 完整实现，找到右列 match% 和 ICIJ link 之间的插入点)
    - .planning/phases/09-data-enrichment-foundations/09-UI-SPEC.md (§2 ICIJ Sanctioned Node Indicator，badge 样式规格)
  </read_first>
  <files>
    scripts/sync-icij-offshore.mjs
    src/lib/server/repository.ts
    src/app/company/[slug]/page.tsx
  </files>
  <action>
**修改 1：scripts/sync-icij-offshore.mjs**

在 `linkToEntities()` 函数之后（约 L390）、`main()` 函数之前，添加 `matchSanctions()` 函数：

```javascript
// ── Match ICIJ entities against sanctions ─────────────────────────────────────

async function matchSanctions() {
  console.log('\nMatching ICIJ entities against sanctions database…')
  const client = await pool.connect()
  try {
    const { rowCount } = await client.query(`
      UPDATE icij_entities ie
      SET
        is_sanctioned  = (m.matched_name IS NOT NULL),
        sanctions_match = m.matched_name
      FROM (
        SELECT
          ie2.node_id,
          (
            SELECT se.name
            FROM sanctions_entries se
            WHERE se.sanctions IS NOT NULL
              AND word_similarity(lower(ie2.name), se.search_text) > 0.72
            ORDER BY word_similarity(lower(ie2.name), se.search_text) DESC
            LIMIT 1
          ) AS matched_name
        FROM icij_entities ie2
      ) m
      WHERE ie.node_id = m.node_id
    `)
    console.log(`Sanctions re-match complete. ${rowCount} rows updated.`)
  } finally {
    client.release()
  }
}
```

在 `main()` 中，找到 `await linkToEntities()` 调用（约 L430），在其后追加 `await matchSanctions()`，守卫条件与 linkToEntities 相同（`!DRY_RUN && total > 0`）：

```javascript
if (!DRY_RUN && total > 0) {
  await linkToEntities()
  await matchSanctions()   // 新增：全量制裁重匹配 (D-01, D-02)
}
```

**修改 2：src/lib/server/repository.ts**

在 IcijMatch 接口（L998-L1010）末尾追加两个可选字段：

```typescript
export interface IcijMatch {
  nodeId: string
  name: string
  dataset: string
  entityType: string | null
  countries: string | null
  jurisdiction: string | null
  status: string | null
  incorporationDate: string | null
  address: string | null
  sourceUrl: string | null
  matchConfidence: number
  isSanctioned?: boolean          // Phase 9: populated from icij_entities.is_sanctioned
  sanctionsMatch?: string | null  // Phase 9: matched sanctions entry name (for tooltip)
}
```

在 `getIcijMatches()` 函数（L1013-L1035）中：
1. SELECT 列追加 `, is_sanctioned, sanctions_match`
2. rows.map() 追加字段映射：
   ```typescript
   isSanctioned: r.is_sanctioned ?? false,
   sanctionsMatch: r.sanctions_match ?? null,
   ```

完整 SQL 改为：
```sql
SELECT node_id, name, dataset, entity_type, countries, jurisdiction,
       status, incorporation_date, address, source_url, match_confidence,
       is_sanctioned, sanctions_match
FROM icij_entities
WHERE linked_entity_id = $1
ORDER BY match_confidence DESC
LIMIT 20
```

**修改 3：src/app/company/[slug]/page.tsx — OffshoreLeaksPanel badge**

在 `OffshoreLeaksPanel` 函数（L505-L521）的右列 `<div>` 中，在 `{Math.round(m.matchConfidence * 100)}% match` 的 `<p>` 之后、`{m.sourceUrl && ...}` 之前，插入以下 badge JSX：

```tsx
{m.isSanctioned && (
  <span
    title={m.sanctionsMatch ? `Matched: ${m.sanctionsMatch}` : undefined}
    style={{
      display: 'inline-block',
      fontSize: '10px',
      fontWeight: 600,
      textTransform: 'uppercase',
      letterSpacing: '0.06em',
      color: 'var(--status-listed)',
      backgroundColor: 'rgba(239,68,68,0.12)',
      border: '1px solid rgba(239,68,68,0.2)',
      padding: '2px 6px',
      borderRadius: '4px',
      marginLeft: 'var(--space-2)',
    }}
  >
    Sanctioned Entity
  </span>
)}
```

样式规格来自 UI-SPEC §2（锁定，不得修改颜色或文案）：
- color: `var(--status-listed)` (#ef4444)
- backgroundColor: `rgba(239,68,68,0.12)`
- border: `1px solid rgba(239,68,68,0.2)`
- 文案: `Sanctioned Entity`（全大写由 textTransform 处理）
- tooltip: `title={`Matched: ${m.sanctionsMatch}`}`（仅在 sanctionsMatch 非 null 时渲染）

确认：OffshoreLeaksPanel 已通过 F3 ContentLock 保护（在 panels 数组中），badge 不需要单独 F3 gating（T-9-03 已由 page.tsx 的 f3Unlocked 守卫覆盖）。
  </action>
  <verify>
    <automated>cd /home/hippo/projects/Energy_trade_inspection && npm run type-check 2>&1 | tail -5</automated>
  </verify>
  <done>
    - scripts/sync-icij-offshore.mjs 包含 matchSanctions() 函数和 main() 中的调用
    - IcijMatch 接口包含 isSanctioned? 和 sanctionsMatch? 字段
    - getIcijMatches() SELECT 包含 is_sanctioned 和 sanctions_match
    - OffshoreLeaksPanel 在右列渲染 {m.isSanctioned && <span>Sanctioned Entity</span>}
    - npm run type-check 零错误
  </done>
  <acceptance_criteria>
    - `grep "matchSanctions" scripts/sync-icij-offshore.mjs` 返回至少 2 处（函数定义 + main() 调用）
    - `grep "isSanctioned" src/lib/server/repository.ts` 返回 IcijMatch 接口字段定义和 rows.map() 映射行
    - `grep "is_sanctioned" src/lib/server/repository.ts` 返回 SELECT SQL 中的列名
    - `grep "Sanctioned Entity" src/app/company/\[slug\]/page.tsx` 返回 1 处
    - `grep "status-listed" src/app/company/\[slug\]/page.tsx` 返回 badge 样式行（应出现在 OffshoreLeaksPanel badge 的 color 属性处）
    - `npm run type-check` 退出码 0
    - `npm run lint` 退出码 0（或仅含预存在 warning，不含 error）
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ICIJ script → PostgreSQL | 批量 UPDATE SQL；参数为数据库内部数据，无外部用户输入 |
| page.tsx → getIcijMatches() | f3Unlocked 条件守卫；entityId 来自路由参数（server-side slug lookup） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-01 | Tampering | `getIcijMatches()` in repository.ts | mitigate | entityId 通过 `$1` 参数化传入，不做字符串拼接；遵循 ASVS V5.3 |
| T-9-03 | Elevation of Privilege | `OffshoreLeaksPanel` in company page | mitigate | badge 渲染位于 `<ContentLock key="offshore" unlocked={f3Unlocked}>` 内部，free-plan 用户无法看到 IcijMatch 数据；page.tsx 仅在 `f3Unlocked` 时调用 `getIcijMatches()` |
</threat_model>

<verification>
1. `npm run type-check` — TypeScript 类型检查通过（包括 IcijMatch 新字段、getIcijMatches() 返回类型）
2. `npm run lint` — 无 ESLint 错误
3. `grep "ADD COLUMN IF NOT EXISTS is_sanctioned" db/migrations/036_icij_sanctions_linkage.sql` — 确认 migration 列存在
4. `grep "matchSanctions" scripts/sync-icij-offshore.mjs` — 确认 sync 脚本包含新函数和调用
5. `grep "isSanctioned" src/lib/server/repository.ts` — 确认接口和映射更新
6. `grep "Sanctioned Entity" "src/app/company/[slug]/page.tsx"` — 确认 badge 文案存在
</verification>

<success_criteria>
- db/migrations/036_icij_sanctions_linkage.sql 存在，包含 ALTER TABLE + 稀疏索引 + 全量 UPDATE
- scripts/sync-icij-offshore.mjs 的 main() 在 linkToEntities() 后调用 matchSanctions()
- IcijMatch 接口有 isSanctioned?: boolean 和 sanctionsMatch?: string | null
- getIcijMatches() SELECT 语句包含 is_sanctioned 和 sanctions_match 列
- OffshoreLeaksPanel 在 is_sanctioned=true 时渲染红色 "Sanctioned Entity" badge，title 为 "Matched: {sanctions_match}"
- npm run type-check 和 npm run lint 均通过
</success_criteria>

<output>
完成后创建 `.planning/phases/09-data-enrichment-foundations/09-01-SUMMARY.md`
</output>
