---
phase: 09-data-enrichment-foundations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/server/repository.ts
autonomous: true
requirements:
  - NETDATA-03
  - NETDATA-04

must_haves:
  truths:
    - "getCompanyFraudAlerts(name) 在 repository.ts 中存在，使用参数化查询、LIMIT 50、包含 synced_at"
    - "getVesselFraudAlerts(operator, manager?) 在 repository.ts 中存在，通过 OR 条件匹配两个名称"
    - "FraudAlertRow 接口在 repository.ts 中导出，包含 synced_at: Date 字段"
    - "两个函数的 SQL 排序为 blacklist 优先，然后按 synced_at DESC"
    - "vessel 函数签名预留 manager? 参数以便未来扩展"
  artifacts:
    - path: "src/lib/server/repository.ts"
      provides: "FraudAlertRow 接口 + getCompanyFraudAlerts() + getVesselFraudAlerts()"
      exports: ["FraudAlertRow", "getCompanyFraudAlerts", "getVesselFraudAlerts"]
  key_links:
    - from: "src/app/company/[slug]/page.tsx"
      to: "repository.ts:getCompanyFraudAlerts"
      via: "f3Unlocked 条件调用"
      pattern: "getCompanyFraudAlerts"
    - from: "src/app/vessel/[imo]/page.tsx"
      to: "repository.ts:getVesselFraudAlerts"
      via: "f3Unlocked 条件调用"
      pattern: "getVesselFraudAlerts"
---

<objective>
在 repository.ts 中实现两个欺诈警报查询函数，供 company 和 vessel 详情页的 FraudAlertsPanel 使用。

Purpose: FraudAlertsPanel（Plan 03）是纯 props-fed Server Component，所有数据来自 page.tsx 服务器端预取。这两个函数是 Plan 03 的数据依赖，必须在 Wave 1 完成以解除阻塞。

Output: repository.ts 中新增 FraudAlertRow 接口、getCompanyFraudAlerts() 和 getVesselFraudAlerts() 函数。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-data-enrichment-foundations/09-CONTEXT.md
@.planning/phases/09-data-enrichment-foundations/09-RESEARCH.md
@.planning/phases/09-data-enrichment-foundations/09-PATTERNS.md

<interfaces>
<!-- checkFraudAlerts() 模板 (src/lib/server/fraud-check.ts L39-L85) — 直接衍生 -->
```typescript
// fraud-check.ts 的 FraudAlert 接口（无 synced_at — Plan 02 要添加 synced_at）
export interface FraudAlert {
  source: string
  source_name: string
  source_url: string
  company_name: string
  list_type: 'blacklist' | 'whitelist'
  fraud_type: string | null
  description: string | null
  scam_url: string | null
}

// fraud-check.ts 的 SQL 查询结构（Plan 02 复用，三处差异见 action）
const { rows } = await db.query<FraudAlert & { sim: number }>(
  `SELECT source, source_name, source_url, company_name,
          list_type, fraud_type, description, scam_url,
          GREATEST(
            similarity(normalized_name, $1),
            word_similarity($1, normalized_name)
          ) AS sim
   FROM fraud_alerts
   WHERE normalized_name % $1 OR $1 %> normalized_name
   ORDER BY sim DESC
   LIMIT 10`,
  [normalized]
)
const hits = rows.filter((r) => r.sim >= SIMILARITY_THRESHOLD)  // 0.45
```

<!-- normalizeEntityName 函数签名 (src/lib/server/normalize.ts L23-L38) -->
```typescript
export function normalizeEntityName(text: string, stripGeneric = false): string
// 调用方式：normalizeEntityName(name, true)  — stripGeneric=true (与 fraud-check.ts L46 一致)
```

<!-- repository.ts 末尾现有结构 (约 L1037+) — 新代码追加在 IcijOfficerLink 之后 -->
```typescript
export interface IcijOfficerLink {
  officerNodeId: string
  officerName: string
  relType: string
  entityNodeId: string
  entityName: string
  entityDataset: string
  entityJurisdiction: string | null
  entityStatus: string | null
}
// ... getIcijOfficerNetwork() 函数
// ↑ Plan 02 的新接口和函数追加在此区域末尾
```

<!-- db 和 normalizeEntityName 在 repository.ts 顶部已导入 -->
```typescript
import { db } from './db'
import { normalizeEntityName } from './normalize'
// 无需新增 import
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 repository.ts 添加 FraudAlertRow 接口和 getCompanyFraudAlerts()</name>
  <read_first>
    - src/lib/server/repository.ts (最后 50 行，确认追加位置；顶部 import 确认 db 和 normalizeEntityName 已导入)
    - src/lib/server/fraud-check.ts (完整文件，理解 checkFraudAlerts() 结构作为模板)
    - db/migrations/028_fraud_alerts.sql (fraud_alerts 表 schema，确认 synced_at 列存在)
  </read_first>
  <files>src/lib/server/repository.ts</files>
  <behavior>
    - getCompanyFraudAlerts('') 返回 []（空名称保护）
    - getCompanyFraudAlerts('a') 返回 []（单字符名称保护）
    - getCompanyFraudAlerts('valid name') 执行参数化查询，LIMIT 50，ORDER BY blacklist 优先
    - FraudAlertRow 包含 synced_at: Date 字段（fraud-check.ts FraudAlert 不含此字段）
    - sim < 0.45 的行被过滤掉（SIMILARITY_THRESHOLD = 0.45）
    - 返回类型为 Promise&lt;FraudAlertRow[]&gt;（不含 sim 字段）
  </behavior>
  <action>
在 repository.ts 末尾（最后一个 export 函数之后）追加以下代码块。

注意：
1. `db` 和 `normalizeEntityName` 已在文件顶部导入，不需要重复 import
2. 不要修改文件中任何已有代码
3. LIMIT 50 是安全上限（D-06 无业务上限，但 T-9-02 需防御性限制）
4. ORDER BY 排序：blacklist=0，whitelist=1，然后 synced_at DESC（Claude 决策，参照 CONTEXT.md 说明）

```typescript
// ── Fraud Alerts ──────────────────────────────────────────────────────────────

const FRAUD_SIMILARITY_THRESHOLD = 0.45  // matches fraud-check.ts SIMILARITY_THRESHOLD

export interface FraudAlertRow {
  source: string
  source_name: string
  source_url: string
  company_name: string
  list_type: 'blacklist' | 'whitelist'
  fraud_type: string | null
  description: string | null
  scam_url: string | null
  synced_at: Date                // UI requires "Reported {date}" display
}

/**
 * Fetch fraud alerts matching a company name via pg_trgm fuzzy search.
 * Used by FraudAlertsPanel on company detail page (F3 gated in page.tsx).
 */
export async function getCompanyFraudAlerts(name: string): Promise<FraudAlertRow[]> {
  if (!name || name.trim().length < 2) return []
  const normalized = normalizeEntityName(name, true)
  if (!normalized || normalized.length < 2) return []

  try {
    const { rows } = await db.query<FraudAlertRow & { sim: number }>(
      `SELECT
         source, source_name, source_url, company_name,
         list_type, fraud_type, description, scam_url, synced_at,
         GREATEST(
           similarity(normalized_name, $1),
           word_similarity($1, normalized_name)
         ) AS sim
       FROM fraud_alerts
       WHERE normalized_name % $1 OR $1 %> normalized_name
       ORDER BY
         CASE list_type WHEN 'blacklist' THEN 0 ELSE 1 END,
         synced_at DESC
       LIMIT 50`,
      [normalized]
    )
    return rows
      .filter((r) => r.sim >= FRAUD_SIMILARITY_THRESHOLD)
      .map(({ sim: _sim, ...r }) => r)
  } catch {
    return []
  }
}

/**
 * Fetch fraud alerts matching a vessel by operator or manager name.
 * Used by FraudAlertsPanel on vessel detail page (F3 gated in page.tsx).
 *
 * D-04: matches by operator OR manager (vessel.manager not yet in schema;
 * parameter reserved for future Phase 11 extension when manager data is available).
 * D-05: SIMILARITY_THRESHOLD = 0.45 (consistent with fraud-check.ts).
 */
export async function getVesselFraudAlerts(
  operator: string | null | undefined,
  manager?: string | null
): Promise<FraudAlertRow[]> {
  const names = [operator, manager].filter((n): n is string => !!n && n.trim().length >= 2)
  if (names.length === 0) return []

  const normalizedNames = names
    .map((n) => normalizeEntityName(n, true))
    .filter((n) => n.length >= 2)
  if (normalizedNames.length === 0) return []

  try {
    // Query each name and union results; deduplicate by (source, company_name)
    const seen = new Set<string>()
    const results: FraudAlertRow[] = []

    for (const normalized of normalizedNames) {
      const { rows } = await db.query<FraudAlertRow & { sim: number }>(
        `SELECT
           source, source_name, source_url, company_name,
           list_type, fraud_type, description, scam_url, synced_at,
           GREATEST(
             similarity(normalized_name, $1),
             word_similarity($1, normalized_name)
           ) AS sim
         FROM fraud_alerts
         WHERE normalized_name % $1 OR $1 %> normalized_name
         ORDER BY
           CASE list_type WHEN 'blacklist' THEN 0 ELSE 1 END,
           synced_at DESC
         LIMIT 50`,
        [normalized]
      )
      for (const row of rows) {
        if (row.sim < FRAUD_SIMILARITY_THRESHOLD) continue
        const key = `${row.source}::${row.company_name}`
        if (seen.has(key)) continue  // deduplicate when operator and manager match same row
        seen.add(key)
        const { sim: _sim, ...alert } = row
        results.push(alert)
      }
    }

    // Final sort: blacklist first, then by synced_at DESC
    results.sort((a, b) => {
      const typeDiff =
        (a.list_type === 'blacklist' ? 0 : 1) - (b.list_type === 'blacklist' ? 0 : 1)
      if (typeDiff !== 0) return typeDiff
      return new Date(b.synced_at).getTime() - new Date(a.synced_at).getTime()
    })

    return results
  } catch {
    return []
  }
}
```

**实现说明（Claude 判断）：**
- vessel 函数使用循环 + Set 去重而非 SQL UNION，因为名称数量最多 2 个，避免复杂动态 SQL（D-06/CONTEXT.md"当 operator 和 manager 同时命中同一条 fraud_alerts 行时是否去重"）
- 去重 key 为 `${row.source}::${row.company_name}`（同一数据源的同一公司只展示一次）
- 如果 operator 和 manager 均为 null/undefined，返回 []
  </action>
  <verify>
    <automated>cd /home/hippo/projects/Energy_trade_inspection && npm run type-check 2>&1 | tail -5</automated>
  </verify>
  <done>
    - repository.ts 末尾包含 FraudAlertRow 接口（含 synced_at: Date）
    - getCompanyFraudAlerts(name) 已导出，使用 LIMIT 50、ORDER BY blacklist 优先
    - getVesselFraudAlerts(operator, manager?) 已导出，逻辑含去重
    - npm run type-check 零错误
  </done>
  <acceptance_criteria>
    - `grep "export interface FraudAlertRow" src/lib/server/repository.ts` 返回匹配行
    - `grep "synced_at: Date" src/lib/server/repository.ts` 返回 FraudAlertRow 接口内的字段行
    - `grep "export async function getCompanyFraudAlerts" src/lib/server/repository.ts` 返回匹配行
    - `grep "export async function getVesselFraudAlerts" src/lib/server/repository.ts` 返回匹配行
    - `grep "LIMIT 50" src/lib/server/repository.ts` 返回至少 2 处（company 和 vessel 各一个）
    - `grep "FRAUD_SIMILARITY_THRESHOLD" src/lib/server/repository.ts` 返回常量定义和至少 1 处使用
    - `grep "manager?" src/lib/server/repository.ts` 返回 getVesselFraudAlerts 函数签名行（预留参数）
    - `npm run type-check` 退出码 0
    - `npm run lint` 退出码 0（或仅含预存在 warning，不含 error）
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page.tsx → getCompanyFraudAlerts() | company.name 来自数据库记录，不是直接用户输入；f3Unlocked 条件守卫（T-9-03） |
| page.tsx → getVesselFraudAlerts() | vessel.currentOperator 来自数据库；manager 参数当前为 undefined |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-01 | Tampering | `getCompanyFraudAlerts()` in repository.ts | mitigate | 名称通过 normalizeEntityName() 规范化后作为 `$1` 参数传入；不做字符串拼接；遵循 ASVS V5.3 |
| T-9-01 | Tampering | `getVesselFraudAlerts()` in repository.ts | mitigate | 同上；每个名称独立参数化查询 `$1` |
| T-9-02 | Denial of Service | `getCompanyFraudAlerts()` fraud_alerts query | mitigate | `LIMIT 50` 防止大结果集；catch 块防止异常阻塞页面渲染 |
| T-9-02 | Denial of Service | `getVesselFraudAlerts()` dual-name query | mitigate | 每次查询独立 `LIMIT 50`；最多执行 2 次查询（operator + manager） |
| T-9-03 | Elevation of Privilege | functions called from page.tsx | mitigate | 调用方 page.tsx 在 `f3Unlocked` 条件下调用；函数本身无权限检查（应用层保证） |
</threat_model>

<verification>
1. `npm run type-check` — 通过（FraudAlertRow 接口、两个函数签名类型正确）
2. `npm run lint` — 通过
3. `grep "export interface FraudAlertRow" src/lib/server/repository.ts` — 确认接口存在
4. `grep "export async function getCompanyFraudAlerts\|export async function getVesselFraudAlerts" src/lib/server/repository.ts` — 确认两个函数存在
5. `grep "LIMIT 50" src/lib/server/repository.ts` — 确认 LIMIT 约束存在
</verification>

<success_criteria>
- FraudAlertRow 接口在 repository.ts 中已导出，包含所有字段（source, source_name, source_url, company_name, list_type, fraud_type, description, scam_url, synced_at）
- getCompanyFraudAlerts(name: string): Promise&lt;FraudAlertRow[]&gt; 已导出
- getVesselFraudAlerts(operator: string | null | undefined, manager?: string | null): Promise&lt;FraudAlertRow[]&gt; 已导出
- 两个函数均使用参数化查询 $1、LIMIT 50、ORDER BY blacklist 优先
- npm run type-check 和 npm run lint 均通过
</success_criteria>

<output>
完成后创建 `.planning/phases/09-data-enrichment-foundations/09-02-SUMMARY.md`
</output>
